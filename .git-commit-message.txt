fix: Canvas poll data类型错误 + Admin弹窗条件渲染

- fix(canvas-poll): pollCanvasAssets 解包类型错误 — API 返回
  CanvasAssetsPollResponse 但被当作 CanvasAssetsPoll 使用，
  导致 pollData.characters 为 undefined 报 "not iterable"
- fix(admin): AdminTaskDetailDialog 内容在无 taskId 时仍渲染
  （Dialog 未用 Portal/Content），包裹 {taskId && ...} 条件渲染
- feat: 添加 .node-version（fnm/nvm 使用 Node 24）
- chore: linter 自动格式化

Co-Authored-By: Claude <noreply@anthropic.com>
