# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### P2-10: 统一错误处理与可观测性口径（2026-06-19）

- **commit `9ec7e135`**: 新增 `apps/client/src/lib/client-logger.ts`，warn/error/info 统一附带 route/action/recordId/taskId/projectId 上下文。更新 sse.ts (12 处)、query-client.ts、workspace.ts、Canvas.tsx。

### P3-11: 收紧 ESLint 规则（2026-06-19）

- **commit `f1846b11`**: `no-console` 规则从 `off` 改为分层管控：server/worker/packages 禁止直接 console（error），client 允许 warn/error，scripts/migration 豁免。

### P3-12: 建立架构决策记录 ADR（2026-06-19）

- **commit `2f65d8cd`**: 新增 `docs/adr/0001-monorepo-layering.md`（两层架构规则）和 `0002-canvas-runtime-adapters.md`（Canvas adapter 注入模式）。

### P2-9: 强化用户输入和资源边界（2026-06-19）

- **commit `991d6a9c`**: 新增 `@excuse/shared input-limits.ts`，集中声明 `PROMPT_LENGTH_LIMITS`、`MAX_STORY_TEXT_LENGTH`、`MAX_CANVAS_SHOTS`、`MAX_REFERENCE_FILES`、`ALLOWED_UPLOAD_MIMES`、温度/Token 范围等限制常量 + `isAllowedMime()`/`checkPromptLengthLimit()` 辅助函数。server `prompt-limits.ts` 改为引用 shared 常量，前后端可引用同一来源。

### P1-6 补齐 package 脚本 + P1-7 清理 TODO 引用（2026-06-19）

- **commit `dcaa6861`**: P1-6 为 17 个 packages/* 补充 `test` + `typecheck` 脚本，新增 `scripts/run-package-tests.ts` 自动发现并运行包测试，根 `test` 脚本简化为 3 路并发（server/worker/packages）。P1-7 清理 5 处失效 `docs/TODO2` / `docs/TODO.md` 引用。验收零回归。

### P0-3: provider observer/guard 注册从入口副作用中隔离（2026-06-19）

- **commit `12a2a0de`**: Server 新增 `bootstrap.ts`，把 provider observer/guard 注册、HTTP listen、SSE 监听、环境检查、优雅退出信号处理全部收敛到 `bootstrapServer()` 内，返回 `start()`/`stop()` 生命周期控制。Worker 新增 `worker-lifecycle.setupLifecycle()`，收敛 observer/guard 注册（含清理函数），增强 graceful shutdown 自动 unregister。`index.ts` 只做 `loadConfig()` + `createContext()` + `createApp()` + `bootstrap.start()`。import 非 index 文件不触发 HTTP/SSE/health server 启动。Worker 配置测试同步更新（`parsePositiveIntEnv` 非法值 throw 含变量名消息）。
  - 验收: typecheck ✅ · lint ✅（4 预存 warnings）· build ✅ · test 551/56/376 ✅ · boundaries ✅

### P0-2: 抽出 server/worker 共享运行时配置解析（2026-06-19）

- **commit `71d1cdbe`**: 新增 `@excuse/shared` env-helpers.ts，提供 `parseProviderConfig()` / `parseMetricsConfig()` / `parseProviderTimeoutConfig()` / `parseStorageConfig()` / `parsePositiveIntEnv()` / `validateProductionBase()` 六个共享 helper。server/worker 的 `loadConfig()` 改用共享 helper，消除 DashScope/OSS/metrics/timeout 配置的重复解析逻辑。`parsePositiveIntEnv()` 可区分缺省/非法值/零负数，错误消息包含变量名。
  - 验收: typecheck ✅ · lint ✅（1 预存 warning）· build ✅ · test 551/0 ✅ · test:client 376/0 ✅ · boundaries ✅

### P0-1: canvas-runtime adapter 化 — 拆出 IO Adapter（2026-06-19）

- **commit `15df5506`**: 定义 `CanvasRuntimeAdapters` 接口（`adapter-types.ts`），含 `llm` / `provider` / `repo` / `storage` / `ffmpeg` 五个适配器。所有 12 个 phase 函数 + `io/` 层不再直接 import `@excuse/db` / `@excuse/provider` / `@excuse/storage` / `@excuse/ffmpeg`，改为通过适配器参数注入 IO 依赖。
  - **canvas-runtime**: 新增 `adapter-types.ts`（翻译层，临时允许 import IO 包），改造 19 个 src 文件（全部 phase + io/ + llm-helpers + normalize + pure/model + index）
  - **worker**: 新增 `canvas-adapter-factory.ts`，改造 12 个 `canvas-*.ts` handler + `canvas-videos.test.ts`
  - **server**: 新增 `modules/canvas/adapter-factory.ts`，改造 11 个 `modules/canvas/*.ts`
  - **scripts**: `check-package-boundaries` 新增 canvas-runtime 规则（`adapter-types.ts` 临时豁免）+ `exclude` 字段支持
  - 验收: typecheck ✅ · lint ✅（1 预存 warning）· build ✅ · test 606/606 ✅ · test:client 376/376 ✅ · boundaries ✅

### §4.4 大文件拆分轮次 + §1.5 Navbar 折叠（2026-06-19）

继续拆分 §4.4 中剩余大文件：

- **commit `db44b176`**: 删除 `admin-dialogs.tsx`（604 行死代码，组件已迁至 shared.tsx/Overview.tsx/Users.tsx）；拆分 `lib/asset-library.ts` → `canvas-deep-link.ts`（Canvas 深链+focus解析，175 行）+ `shot-reference-assets.ts`（参考资产候选+去重+批量应用，185 行），asset-library.ts 退化为 barrel
- **commit `ff570d18`**: §1.5 Navbar 主导航（6 项）+「更多 ▾」折叠菜单（4 项），10 项拥挤导航栏收拢
- **commit `165c49a9`**: 拆分 `NodeDetailPanel.tsx`（565 行）→ `ShotDetailPanel`（178 行）+ `CharacterDetailPanel`（120 行）+ `LocationDetailPanel`（94 行）+ `ProjectDetailPanel`（89 行），NodeDetailPanel 退化为路由层（~105 行）
- **commit `398347fb`**: 抽取 `usePipelineController` hook（~350 行），`PipelineController.tsx` 从 753 行收缩至 ~180 行纯渲染层
- **commit `1fd231bb`**: 拆分 `Assets.tsx`（1136→~740 行），提取 `AssetDetailDialog`（~260 行，原 PreviewModal）+ `AssetTagManager`（~120 行，原 TagManagementModal）至 `components/assets/`

验收：typecheck ✅ · lint ✅（1 预存 warning）· build ✅ · test 551/551 ✅ · test:client 376/376 ✅

### 收尾轮次：代码提交 + 测试修复（2026-06-19）

之前多个 TODO 条目的实现代码留存在工作树中未提交（CHANGELOG 已记录但文件未入库）。本轮将所有改动按逻辑分组提交入 Git：

- **commit `b23cbad0`**: 修复 2 个测试失败 — audit-routes.test.ts 的 `mockResetToPending` 返回空值导致 retry 路由 early return 跳过 audit 调用；assets-page.test.tsx 星标按钮 CSS 变量适配（`fill-yellow-400` → `status-warning-fg`）
- **commit `505c83fb`**: §1.1 骨架屏 + §1.2 空状态 + §4.5 ParameterInput/ReferenceImageUploader/Admin 分页去重（5 个新文件 + 11 个修改文件）
- **commit `6908297b`**: §4.2 dashscope 拆四模块 + §4.4 gateway 拆分 + §3.3 task-engine 去耦 + §3.4 配置表式化 + §5.2 错误脱敏 + §4.5 状态集合收敛（6 个新文件 + 12 个修改文件）
- **commit `cafd093e`**: 归档删除 `docs/TODO2.md`（全部架构审计项已完成）
- 验收通过：typecheck ✅ · lint ✅（1 预存 warning）· build ✅ · test 551/551 ✅ · test:client 376/376 ✅

### P2 治理 + 去重轮次 #4（2026-06-19）

#### 大文件拆分（§4.3）
- **§4.3 admin.ts 路由按子域拆** — `routes/admin/` 目录下按 10 子域拆为独立 handler 文件（`overview`/`tasks`/`users`/`providers`/`asset-retention`/`projects`/`audit-logs`/`api-keys`/`gateway-clients`/`credit`）+ `helpers.ts`（原 `admin-helpers.ts`）+ `index.ts`（barrel 路由注册）。Admin 鉴权从 derive 块下沉为 `resolve` 守卫（非管理员直接 throw ForbiddenError），17 个 handler 手写 `if (!adminAllowed) return adminDenied()` 归零。handler 函数可独立单测。35 个 admin 测试全绿。

### P2 治理 + 去重轮次 #3（2026-06-19）

#### 架构治理（§3.2, §3.4）
- **§3.2 dashscope-client endpoint switch 注册表化** — `generate()` 中 4-case switch 替换为 `CATEGORY_GENERATORS` 方法映射表，新增 category 时在表中登记一行即可。
- **§3.4 rate-limit per-route 插件接入** — 替换 `elysia-rate-limit` 为自定义 Elysia `onRequest` 插件，通过 `matchRouteRateLimit()` 匹配请求路径 → 查找 `DEFAULT_ROUTE_RATE_LIMITS` 表 → 覆盖全局默认 → `SlidingWindowRateLimiter.check()` 判定。移除 `elysia-rate-limit` 依赖。

#### 组件去重（§4.5）
- **ParameterInput 组件** — 新增 `components/generation/ParameterInput.tsx`，覆盖 text/number/select/boolean/mediaUpload 五种参数形态。Workspace.tsx 与 ModelLab.tsx 均已接入，删除约 170 行重复渲染逻辑。
- **ReferenceImageUploader 组件** — 新增 `components/generation/ReferenceImageUploader.tsx`，标准化虚线框上传区 + 缩略图网格。Workspace.tsx 与 ModelLab.tsx 均已接入。
- **AdminPaginationFooter 组件** — 新增 `Admin/shared.tsx` 中共享分页底部栏（"第 X-Y 条 / 共 Z 条" + 上一页/下一页）。ApiKeys.tsx 与 Users.tsx 均已接入，删除约 35 行逐字复制。
- **ApiKeyTable 组件** — 新增 `Admin/shared.tsx` 中共享 API Key 表格，通过 `showName`/`showCreatedAt`/`showActions` 控制差异化列。`AdminGatewayKeysTable`（ApiKeys）与 `AdminUserApiKeysSection`（Users）均已委托给共享组件，删除约 100 行重复表格渲染。

#### 大文件拆分（§4.4）
- **gateway/index.ts 拆三模块** — 469 行单文件拆为：`errors.ts`（错误码常量 + createOpenAIError + 9 个语义化错误工厂，172 行）、`protocol.ts`（normalizeOpenAIChatRequest + 响应/流式构建，200 行）、`usage.ts`（mapGatewayUsageItem + aggregateGatewayUsage，108 行）。index.ts 退化为 barrel re-export。既有 77 个 gateway 测试全绿。

### TODO 核查清理 + P0/P1 修复轮次（2026-06-18）

对 `docs/TODO.md` 全 ~40 项做了逐项源码验证，清理 2 项已修复条目，完成 7 项修复。

#### 清理（已修复、未记录）
- **§1.3 流水线 UI 12 阶段** — `PHASE_UI` 已是 `Record<CanvasPipelinePhase, ...>` 覆盖全部 12 阶段。从 TODO 删除。
- **§2.3 task 锁 heartbeat** — `heartbeat.ts` 已有 3 次重试 + `task-ownership.ts` 中途检查。从 TODO 删除。

#### P0 修复（生产风险）
- **§2.1 FFmpeg 超时** — 新增 `ffmpeg-spawn.ts`：`spawnFfmpeg()` 统一超时保护（默认 10min，env `FFMPEG_TIMEOUT_MS`），`FfmpegTimeoutError` 被 task-engine 分类为 retriable/timeout。compose/audio-extractor/subtitle-burner 全部迁移。
- **§2.2 状态机漂移 reconcile** — 新增 `reconcile.ts`（worker）：每轮 poll 后 JOIN 查询 task↔run 状态不一致的对，自动修复漂移（run 补标终态）。append-only guard 确保幂等。

#### P1 修复（drift / 体验 / 可靠性）
- **§5.1 traceId 贯穿** — `tasks` 表加 `traceId` 列（migration 0044）；server→worker→NOTIFY→SSE 全链路透传；pipeline auto-advance 传播 traceId。
- **§2.4 SSE 死连接回收** — `UserEventHub` 加 lastActivity 跟踪 + `sweepStaleConnections(60s)`；SSE route 在 30s heartbeat 中调用 sweep。
- **§1.4 上传错误提示** — `workspace.ts` 的 `uploadReferenceFiles`/`uploadMediaParam` 现在 toast.error 上传失败（草稿持久化仍待后续）。

### P1 修复补充 + P2 治理轮次（2026-06-19）

#### P1 修复
- **§4.1 generate.ts 去重** — 新增 `modules/generation/orchestration.ts`（`orchestrateGeneration` + `serializeRecord`），提取 POST `/generate` 与 POST `/records/:id/retry` 中 ~110 行重复的 estimate→prepare→execute→video-task 编排。generate.ts 从 483 行缩减到 ~280 行。既有 45 个测试全绿。
- **§2.5 rate-limit 加固** — `buildRateLimitKey` 改为尽力 JWT 无验证解码提取 `sub`（userId），无效 token 统一落到 IP bucket（消除伪造 token 无限 bucket 绕过）；全局限流插件 `maxSize` 从 5000 提升到 50000。rate-limit 包 6 个测试全绿。
- **§1.4(b) 草稿保护** — 新增 `lib/draft-storage.ts`（sessionStorage 持久化 + beforeunload 拦截）；Canvas 故事 textarea 与 Workspace prompt 均已接入，成功提交后清除草稿。

#### P2 治理
- **§3.2 category 注册表** — 新增 `CATEGORY_META` 注册表（`@excuse/shared/src/models.ts`），含 label/assetKind/notify 文案/sync 等 category 元数据；`notifications.ts` 通知文案改用 `CATEGORY_META[category]`，参数类型从 `'text' | 'image'` 拓宽为 `ModelCategory`；`assets/service.ts` `genCategoryToKind` 改用 `CATEGORY_META`，删除静默 `default: 'text'` 兜底（未知 category 改为显式 throw）；`AssetLibraryKind` / `NotificationMeta.category` 补 `'audio'` 成员。dashscope-client endpoint switch / client 端标签地图等接触时渐进替换。

### P2 治理 + 体验打磨轮次 #2（2026-06-19）

#### UX 打磨
- **§1.5 Toast 位置** — Toaster 从 `top-center` 移至 `bottom-right`，避免遮挡状态栏/标题。
- **§1.5 a11y aria-label** — RecordCard 复制按钮、Navbar 通知铃/退出登录按钮补 `aria-label`。

#### 架构治理
- **§4.2 dashscope-client.ts 拆四模块** — 988 行单文件拆为：`provider-hooks.ts`（observer/guard registry，127 行）、`dashscope-request-builder.ts`（applyMappings + buildRequestBody 纯函数，147 行）、`dashscope-sse.ts`（iterSSEEvents + 两 SSE 解析器，151 行）。Client 收缩至 515 行；4 个 sync fetch 方法通过 `withErrorHandling` 模板消除传输层 try/catch 重复。重导出保持下游 import 路径不变。既有 provider 142 个测试全绿。
- **§4.5 status 集合收敛** — `ACTIVE_GENERATION_STATUSES` / `GEN_RUNNING_STATUSES` 收敛至 `@excuse/shared/src/generation.ts`（与 `GenerationStatus` 类型同源）。generation/service、generation-records.repo、assets/service 三个散落定义统一为单一来源。

### P2 治理 + 体验打磨轮次（2026-06-19）

#### UX 体验
- **§1.1 骨架屏** — 新增 `ui/skeleton.tsx`（shadcn 风格 Skeleton 组件）+ `RecordCardSkeleton` / `RecordCardSkeletonList`。Workspace 记录列表加载态、Billing 全页加载态、Canvas 列表页加载态、App.tsx 路由切换 fallback 全部替换为骨架屏，替代裸「加载中...」文字。
- **§1.2 空状态引导** — 新增共享 `EmptyState` 组件（图标 + 标题 + 可选描述 + CTA slot）。Workspace 空状态指引「← 在左侧输入 Prompt 开始生成」；Billing 三处「暂无数据」统一用 EmptyState；Navbar 通知空状态用 EmptyState + Bell 图标。

#### 架构治理
- **§3.3 task-engine 去耦** — 为 `getTaskPriority` / `computeRetryDelay` 引入声明式策略表：`TaskPriorityPolicy` / `TaskBackoffPolicy` + `DEFAULT_PRIORITY_POLICY` / `DEFAULT_BACKOFF_POLICY` 常量。函数接受可选 policy 参数，新增业务 type 在策略表登记一行即可。移除 `taskType.includes('video')` 子串匹配隐性耦合。错误码分类同步表式化（`ERROR_CODE_REGISTRY` 替代 if 链）。28 个 task-engine 测试全绿。
- **§3.4 配置表式化** — task-engine 错误码分类从 if 链迁移至 `ERROR_CODE_REGISTRY` 声明式表；rate-limit 包新增 `DEFAULT_ROUTE_RATE_LIMITS` per-route 声明式配置表 + `matchRouteRateLimit()` 纯函数匹配（插件消费待后续）。
- **§4.3 admin.ts 轻量重构** — 共享 helper（percentile/latency/serializeApiKey/toHealthSummary）提取至 `admin-helpers.ts`；admin.ts 保持单文件（Elysia derive 类型链约束），但按 9 个子域清晰分段。

#### 安全加固
- **§5.2 错误消息脱敏** — `@excuse/shared` 新增 `sanitizeErrorMessage()`（500 字符截断 + 敏感 JSON 嵌入移除）。`parseDashScopeError` 返回前、`markTaskFailed` / `markGenerationFailed` / `cancelGenerationRecordIfActive` 入库前统一脱敏。

### TODO2 核查修正轮次（2026-06-18）

对 `docs/TODO2.md` 原声称「全部 27 项已完成验收」的标记做了独立代码核查，发现多项标记不实，对真实缺口完成修复。

#### 安全性

- **§5.1 CSP 安全头（P0）** — 补 `Content-Security-Policy-Report-Report-Only`（nginx+Elysia 双层）+ `/api/csp-report` 违规上报端点（`csp-report.ts`）；切 enforce 待验证零违规后
- **§5.5 登录账号锁定** — per-account 失败计数锁定（5 次失败→15 分钟锁定，成功清零）+ per-IP 限流互补
- **§4.3 空 catch 全仓清零** — 25 处 `.catch(()=>{})` / `.catch(()=>[])` 全部补 `logger.warn`；`sendPasswordResetEmail` 补 `logger.error`

#### 运行时健壮性

- **§4.4 全局 React Query 错误处理** — `QueryCache` + `MutationCache` onError：mutation 无本地 handler 时兜底 toast，query 仅上报+console；`handleApiError` 加 401/403 跳过 + 3s 去重窗口；`reportClientError` 共享 helper 提取至 `lib/error-report.ts`
- **§4.5 Canvas loadProject 去重** — debounce `scheduleReload` + `versionAtMountRef` 防 mount 双触发；`onPhaseComplete` 统一走 debounce 入口

#### 用户体验

- **§2.2 ModelLab SPA 路由守卫** — `useConfirmNavigation` hook（beforeunload + `<Link>` click 拦截）；声明式 `<Routes>` 下 `useBlocker` 不可用，注记需 data router 迁移后才可

#### 架构

- **§3.1 client.ts 桶重构** — `client.ts` 28 行 barrel + `api-core.ts`(112行) + `billing-api.ts` + `gateway-api.ts`；消除循环依赖（领域文件导入 `api`/`unwrapEden` 从 api-core）；canvas-api/admin 深拆延迟

#### 核查结论

- 22 项经代码核查确认为真修复
- §4.6 Canvas useEffect — 原问题被夸大（内联 async pattern 不触发 exhaustive-deps 警告），标记为无需改动
- §3.1 canvas-api (347行) / admin (231行) 深拆 — 延迟
- §3.2 workspace.ts 表单/业务拆分 — 延迟（循环引用已消除，但表单未拆出）

#### 验收

- typecheck: ✅ (server/client/worker/e2e)
- lint: ✅ (0 新增错误)
- build: ✅
- server tests: 550 pass, 1 fail (pre-existing)
- client tests: 375 pass, 1 fail (pre-existing)

### Fixed

- **Canvas 阶段注册表统一 + 前端全 12 阶段可见（TODO §3.1 🔴）**：阶段元数据收敛为 `@excuse/shared/canvas-phases.ts` 单一权威注册表（`CANVAS_PHASE_ORDER` + `CanvasPipelinePhase` 类型 + `CANVAS_PAUSE_BEFORE` + helper 函数），所有消费者从此派生——workflow-engine re-export（保持公开 API）、db pgEnum 经编译期双向断言 `AssertCanvasPhaseSync` 强制同源（新增/遗漏阶段 → 编译失败并提示「补 pgEnum + db:generate」）、前端 `PipelineController` 用 `Record<CanvasPipelinePhase, ...>` 强制覆盖全部阶段（漏一个即编译失败）。前端 PHASES 从 9 项补齐为 12 项（新增 dialogue/bgm/assemble），`pauseBefore` 从共享注册表 `CANVAS_PAUSE_BEFORE` 派生而非手抄；`CostPanel` 的 `PHASE_ORDER` 同样由注册表派生。`CanvasCostPhase` 改为 `CanvasPipelinePhase` 的类型别名。新增 3 个客户端 API 函数（`generateCanvasDialogue` / `generateCanvasBgm` / `assembleCanvas`）。验收：typecheck + lint + build + worker/workflow-engine/shared 测试全绿；漂移断言经注入虚假阶段证实有效（编译失败并显示精确错误信息）。

- **前端按钮触摸目标与标题层级 token（TODO §1.1）**：`Button` 基础尺寸从 32/36px 提升为 default 40px、lg 44px，icon 尺寸同步抬高，核心「开始生成」CTA 因 `size="lg"` 达到 44px；新增 `text-title-lg` / `text-title` / `text-body` 工具类，并把 Workspace、Canvas、Billing 的核心标题接入统一层级。至此 `docs/TODO.md`「前端设计 / 美观度」大项全部完成并移除。验收：client typecheck 通过；相关文件 eslint 通过。

- **前端状态色 token 单一来源（TODO §1.1）**：新增 `apps/client/src/lib/status-tokens.ts` 与 `--status-info/success/warning/danger/neutral/accent-*` CSS 变量，把 generation record、Canvas 项目/状态栏、ShotNode、TaskQueuePanel、PipelineController、Billing、字幕状态、连接指示器、资产历史等业务状态颜色从散落的 Tailwind palette 类收敛到语义 token；`CATEGORY_CONFIG` 与 Billing 类别条也改用品牌/category token，删除多份 `STATUS_COLORS`/`CATEGORY_COLORS`/`TX_TYPE_COLORS` 手抄映射。验收：`rg "bg-(red|green|yellow|blue)-[0-9]|text-(red|green|yellow|blue)-[0-9]|border-(red|green|yellow|blue)-[0-9]" apps/client/src` 无命中；client typecheck 通过；相关文件 eslint 通过。

- **前端深色模式端到端接线（TODO §1.1）**：复用已安装的 `next-themes`，新增 `ThemeProvider` 并在 client 入口以 `attribute="class"` 接管根节点 `.dark`，让现有 dark token 与 `dark:` 变体真正生效；Navbar 新增浅/深色切换按钮（带 `aria-label` / `title`），Sonner Toast 不再硬编码 light，而是跟随当前 resolved theme。验收：client typecheck 通过；相关文件 eslint 通过。

- **前端品牌 token 与主 CTA 渐变（TODO §1.1）**：补充 `PRODUCT.md` 记录产品型设计上下文；将 client 全局 shadcn 默认灰度 token 改为带品牌倾向的 OKLCH 色系，`--primary` / `--accent` / `chart-*` 不再是纯灰，并新增 `--gradient-brand`、`--brand-text` / `--brand-image` / `--brand-video` 等品牌 token。新增 `.brand-cta` 与 `.brand-auth-shell`，工作台「开始生成」和登录/注册/忘记密码/重置密码主按钮统一使用品牌渐变，认证页背景加入克制的品牌氛围层。验收：client typecheck 通过；相关文件 eslint 通过。

- **Canvas 成本阶段补齐 12 阶段（TODO §2.1 局部）**：`CanvasCostPhase` 从 9 阶段补齐为完整 12 阶段，新增 `dialogue` / `bgm` / `assemble`；`CostPanel` 同步补中文标签和展示顺序，避免后续成本 rollup 出现这些阶段时被类型或 UI 漏展示。`TODO §2.1` 已收窄为剩余的阶段注册表/前端 PipelineController 同源问题。验收：client typecheck 通过；相关文件 eslint 通过。

- **Canvas pipeline run 状态 union 去除幽灵 paused（TODO §2.2）**：`@excuse/workflow-engine` 的 `PipelineRunStatus` 删除 DB/shared 均不支持的 `'paused'`，`WorkflowCommand` 收敛为当前实际接线的 `cancel | retry`，避免类型层允许写入一个 PostgreSQL enum 无法持久化的状态。同步修正 `canvas-pipeline-runs` schema、worker stepper、canvas phase route 中残留的「9 阶段」注释为 12 阶段，并确认目标范围 grep 无 `paused`/`resume` 残留。验收：workflow-engine test 全绿；pipeline-stepper test 全绿；相关文件 eslint 通过。

- **生成 submit/retry 幂等收口（TODO §1.6）**：`POST /generate` 在 `dedupe_key` 唯一冲突（23505）时不再抛错或继续执行，而是查回同用户同 dedupeKey 的既有记录并返回 `duplicated: true`，避免并发提交穿透到重复 credit reserve/provider 调用；`POST /records/:id/retry` 的 `resetGenerationToPending` 改为原子条件更新 `failed/cancelled -> pending` 并返回更新行，连点时只有抢到状态转换的请求会继续预留额度和调用 provider，其余请求返回当前记录 `duplicated: true`。验收：`generate-routes-retry-cancel.test.ts` 全绿；新增 submit 唯一冲突与 retry 连点测试通过；本次 touched files eslint 通过。

- **regenerate 恢复参考图传递（TODO §4.1）**：`workspace store.regenerate` 此前只传 `model` + `parameters` 给 `generate()`，静默丢弃 `referenceFileIds`——用户用参考图生成失败后点重新生成会得到不同于原始输入的结果。修复：store 的 `regenerate` 增加 `referenceFileIds` 参数并透传给 `generate()`；Workspace.tsx 从 `record.inputParams.referenceFileIds` 提取并传入。验收：client typecheck / vitest 376 pass。

- **credit reserve 信用对账 job（TODO §1.3）**：新增 `findStaleReservedCredits`（`credit.repo.ts`）扫描 `credit_transactions` 中 reserve 超过 1h 但无 debit/refund 收尾的孤立记录；worker 新增 `startCreditReconciliation` 周期任务（与孤儿清扫同频，默认 60s），自动 refund 冻结资金并审计。防止 server/worker 崩溃或流式中断导致用户余额永久 frozen。验收：worker typecheck 通过。

- **prompt 零长度限制 + 超额计费保护（TODO §1.2）**：新增 `apps/server/src/utils/prompt-limits.ts`，定义 `PROMPT_LENGTH_LIMITS`（text 100k / image 8k / video 8k），通过 `assertPromptWithinLimit` 在 POST `/generate` 和 `/records/:id/retry` 路由层校验 prompt 长度超限返回 422；`assertGatewayMessagesWithinLimit` 在 OpenAI 兼容网关 `/v1/chat/completions` 校验 messages 总长度（上限 100k）。billing 层保护：`executeGeneration`（同步任务完成）和 `settleGatewaySuccess`（网关成功结算）在 `actualCost > estimatedCost × 1.5` 时拒绝扣款、自动退款并标记失败，防止预估严重不足导致余额穿负。验收：server typecheck / lint 通过。

- **provider fetch 全链路超时（TODO §1.1）**：DashScope / ASR 此前所有 `fetch` **零超时**——provider 端 hang 会让请求连接常驻、流式 ReadableStream 永不 close（连接泄漏）、worker 同步 task（text/image）永不完成（仅 video 有 4h 业务超时兜底）。新增 `packages/provider/src/http-timeout.ts`：同步调用加 `AbortSignal.timeout(ms)`（默认 60s，env `PROVIDER_HTTP_TIMEOUT_MS`）；流式调用加「每 chunk 之间空闲超时」控制器（默认 30s，env `PROVIDER_STREAM_IDLE_TIMEOUT_MS`），覆盖「连接 + 首字节」与「chunk 间隔」两阶段。超时/中断错误经 `FailedProviderResult.code`（`'TIMEOUT'`/`'ECONNRESET'`）→ canvas-runtime `cause.code` 透传 → `@excuse/task-engine.classifyTaskError` 进入可重试分类，避免一次性 provider 抽风让任务永久失败。`ServerConfig`/`WorkerConfig` 增加 `providerHttpTimeoutMs`/`providerStreamIdleTimeoutMs`，context 注入到 `DashScopeClient`/`ASRClient`。ASR `queryTask` 的 UNKNOWN/FAILED 路径同步透传 `errorCode`（与 video handler 一致）。验收：provider(142)/canvas-runtime(62)/task-engine(25)/worker(55) 测试全绿；新增 `http-timeout.test.ts`（4 例，含 hang→TIMEOUT 端到端，signal-aware mock + 2s 硬兜底防挂死）。

### Changed

- **TODO 优先级调整：前端 UI/UX 提到最前**：按当前迭代重心重排 `docs/TODO.md`，将「前端设计 / 美观度」与「用户体验 / 可访问性」提升为第一、第二章节，运行时可靠性与架构治理后移；同步重编号条目和内部引用，便于后续 Claude/Codex 优先处理 UI 调整。

- **Admin/Billing/Developers refetchInterval 受页面可见性控制（TODO2 §4.2）**：7 处 `refetchInterval` 从固定数字改为函数形式 `() => document.hidden ? false : N`，后台 tab 时停止轮询避免浪费服务端资源。涉及 Admin index/Users/ApiKeys/Providers/Audit + Billing + Developers。验收：client build 通过。

### Added

- **client.ts 776 行拆分为领域模块（TODO2 §3.1）**：`client.ts` 缩减至 ~180 行（核心 Eden 实例 + unwrapEden + token 管理 + billing/gateway）。领域 API 拆分到 `auth-api.ts`（6 函数）、`generation-api.ts`（10 函数）、`canvas-api.ts`（35+ 函数）、`subtitle-api.ts`（8 函数）、`asset-api.ts`（11 函数）。admin overview/tasks 移入 `admin.ts`。client.ts 通过 `export *` re-export 保持向后兼容。验收：build 通过。

- **忘记密码 / 重置密码页面（TODO2 §1.2）**：新增 `ForgotPassword.tsx`（输入邮箱发送重置链接）和 `ResetPassword.tsx`（从 URL 取 token + 输入新密码提交）两个公开页面。后端 `POST /forgot-password` / `POST /reset-password` 已就绪。client.ts 新增 `forgotPasswordRequest` / `resetPasswordRequest` API 函数。Login.tsx 密码字段旁添加“忘记密码？”链接。App.tsx 注册 `/forgot-password` 和 `/reset-password` 路由。form-schemas.ts 新增 `forgotPasswordSchema` 和 `resetPasswordSchema`。验收：build 通过。

- **MediaPreviewDialog 支持视频/音频预览（TODO2 §1.1）**：根据 URL 后缀自动检测媒体类型，图片→`<img>`、视频→`<video controls autoPlay>`、音频→`<audio controls autoPlay>`。验收：client build 通过。

- **安全响应头插件 + nginx 双重保障（TODO2 §5.1）**：新增 `apps/server/src/plugins/security-headers.ts`，为所有 HTTP 响应添加 `X-Frame-Options: DENY`（防 clickjacking）、`X-Content-Type-Options: nosniff`（防 MIME 嗅探）、`Referrer-Policy: strict-origin-when-cross-origin`、`X-XSS-Protection: 0`、`Permissions-Policy: camera/mic/geolocation 禁用`。Elysia 层 `.derive` 设置 + nginx 层 `add_header` 双重保障，覆盖开发和生产环境。验收：server typecheck / index.test.ts(5) / auth-plugin.test.ts(11) 通过。

### Fixed

- **错误反馈分级策略（TODO2 §2.1）**：新增 `handleApiError(err, fallback)` 工具函数，根据 HTTP 状态码分级展示有意义的错误提示——402 余额不足、429 请求频繁、5xx 服务异常、无 status 网络错误。替换 workspace.ts（3 处）、Canvas.tsx（3 处）、subtitle.ts（8 处）的硬编码 `toast.error('xxx失败')`，统一提取服务端返回的具体错误消息。验收：client build 通过。

- **ModelLab 未保存更改离开确认（TODO2 §2.2）**：表单修改后通过 `beforeunload` 事件阻止意外离开（关闭 tab/刷新/导航），保护用户精心调整的参数。使用 react-hook-form 的 `formState.isDirty` 检测变更。验收：client build 通过。

- **Auth cookie 生产环境启用 __Host- 前缀（TODO2 §5.2）**：生产环境 cookie 名从 `auth_token` 改为 `__Host-auth_token`，防止子域覆盖或篡改。同步将 `path` 从 `/api` 改为 `/`（`__Host-` 要求）。开发环境保持不变。验收：server typecheck 通过，auth(24) 全绿。

- **React Query 全局 retryDelay + mutations 不重试（TODO2 §4.4）**：`query-client.ts` 补充 `retryDelay` 指数退避（1s→2s→4s... max 10s）和 `mutations: { retry: false }`（写操作不应自动重试避免重复提交）。与 §4.1 unwrapEden 401/403 互补。验收：client build 通过。

- **Canvas loadProject SSE 触发去重（TODO2 §4.5）**：`CanvasEditor.tsx` projectVersion 变化时之前调用 `loadProject` 两次（立即 + 800ms 延迟），立即调用可能拿到未提交旧数据。修复：仅保留 800ms 延迟加载，每个 SSE phase 事件只触发 1 次 API 请求。验收：client build 通过。

- **服务端关键路径空 catch 块补 logger（TODO2 §4.3）**：4 处空 catch 补充日志——`plugins/auth.ts` resolveActiveUserId 失败 → `logger.warn`；`routes/auth.ts` 注册赠送初始额度失败 → `logger.error`（资损路径）；forgot-password token 写入失败 → `logger.warn`；`modules/generation/service.ts` provider 取消失败 → `logger.warn`。排除合理静默（health.ts 健康检查、reference-assets.ts URL 工具函数）。验收：server typecheck 通过，auth(24) + generate-retry-cancel(15) 全绿。

- **unwrapEden 401/403 自动清理登录态并跳转登录页（TODO2 §4.1）**：`client.ts` 的 `unwrapEden` 注释声称处理 401/403 认证问题但实际代码未实现——cookie 过期后用户看到满屏错误但不会被引导到登录页。修复：检测 `edenErr.status === 401|403` 时调用 `setAuthToken(null)` 断开 SSE 连接，并派发 `auth:unauthorized` 自定义事件；`AuthProvider` 新增 `useEffect` 监听该事件，自动清理用户态并 `navigate('/login')`。验收：build 通过。

### Changed

- **workflows 死代码彻底清理 + Admin 拆分 + subjects/import 提取 + 死函数清理（TODO2 §八 补遗）**：删除 `packages/db` 的 workflows repo（8 fn、零调用方）与 schema（`workflows`/`workflow_steps` 两张表 + 2 个 pgEnum），附带迁移 `0041_good_machine_man`（DROP TABLE + DROP TYPE）；从 `packages/shared` 清理 `WorkflowPayload`/`WorkflowInput`/`WorkflowOutput`/`WorkflowStepInput`/`WorkflowStepOutput` 类型。拆分 `apps/client/src/pages/Admin/index.tsx`（1927→121 行）为 `Overview.tsx` / `Users.tsx` / `ApiKeys.tsx` 三个 sibling，`shared.tsx` 补 `generationRecordMatchLabel` / `recentRecordExecutionLabel`。`canvas/index.ts` 的 `/subjects/import` 内联 handler 提取到 `handlers-resources.ts`，修复两处 `as any` 为 `CharacterProfile | null | undefined` / `LocationProfile | null | undefined`。额外清理 5 个零调用 repo 函数（`findAssetFavorite`/`listCanvasAssetsByProject`/`getActiveCanvasAssetByTarget`/`markCanvasAssetCancelled`/`cleanupExpiredTokens`），`countCanvasShotsReferencingAsset` 取消公开导出。验收：typecheck / lint / build / test(549+376+108) / check:boundaries 全绿。

- **provider storage/ffmpeg 门面迁完 + Elysia catalog 固定（TODO2 §3.1）**：采用 TODO2 §3.1 解法 A，`apps/server`、`apps/worker`、`packages/canvas-runtime` 中的 storage/ffmpeg 消费点改为直连 `@excuse/storage` / `@excuse/ffmpeg`，删除 `@excuse/provider` 下 4 个纯 re-export shim（`storage.ts` / `subtitle-burner.ts` / `audio-extractor.ts` / `compose.ts`）及迁移后的 provider storage 测试，相关测试 mock 按真实来源拆分；`@excuse/provider` 不再声明 `@excuse/storage` / `@excuse/ffmpeg` 依赖。顺带用 Bun catalog 将 server 的 `elysia` 从 `latest` 固定为 `catalog:`，root catalog 锁到 `1.4.28`，避免安装时 lockfile 漂移；清理本地 stale `node_modules/.bun/elysia@1.4.29...`。修复 `packages/storage` 的 Windows 路径测试断言，改用平台路径期望。验收：typecheck / lint / build / test / test:client / check:boundaries 全绿。
- **legacy video/ASR 轮询补 claim 锁（TODO2 §1.2）**：先采用带外修复切口，消除多 worker 重复处理同一行的真实竞态。`generation_records` 与 `subtitle_projects` 增加 `locked_by` / `locked_until` 列和索引（迁移 `0039_awesome_silver_sable`）；`pollPendingVideoTasks` / `pollPendingASRProjects` 改为原子 `UPDATE ... SELECT ... FOR UPDATE SKIP LOCKED` claim；worker 传入真实 `workerId` 和 `claimTtlMs`，处理完每条 legacy video/ASR 后释放自己持有的 active 行锁，崩溃时靠锁过期接手。新增 generation/subtitle repository claim-release 测试。说明：这只完成 §1.2 的 claim/锁，不代表 video/ASR 已迁入统一 `tasks` 队列，§1.1/§1.4 仍待继续收敛。
- **legacy video/ASR 显式带外收敛（TODO2 §1.1/§1.4）**：确认生产路径不创建 `generate.video` task 后，删除 `task-engine.computeRetryDelay` 的 `generate.video` 分支、`tasks.ts` 误导性 docstring 示例，并把 task-engine / workflow-engine / admin 测试 fixture 改为真实 task type。继续沿用带外路线时，给 `generation_records` 与 `subtitle_projects` 增加 `provider_failure_count` / `next_poll_at`（迁移 `0040_cute_wallow`），provider FAILED 为限流/超时/连接类可重试错误时复用 `task-engine.decideTaskFailureAction` 计算退避并重新排队轮询；预算耗尽或永久错误才退款、通知并标记失败。claim/release 不再刷新 ASR `updatedAt`，避免破坏 ASR 超时锚点。验收：task-engine/workflow-engine、worker video/ASR retry、DB claim-release 测试通过。
- **server 账本编排收敛（TODO2 §3.2）**：新增 `apps/server/src/services/billing-ledger.ts`，把 generation 与 OpenAI gateway 两套重复的 reserve/debit/refund + credit audit 细节收敛到 `reserveAndTrack` / `debitReservedAndTrack` / `refundReservedAndTrack`。业务层仍各自负责失败收尾和协议映射：generation 保留 result-style + `markGenerationFailed`，gateway 保留 OpenAI 错误响应与 `gateway_call` audit。验收：generate/gateway/audit 相关路由测试通过。
- **server 错误协议收敛（TODO2 §3.5）**：新增 `OpenAIGatewayAppError` bridge，把 `@excuse/gateway` 的 OpenAI 兼容错误包装进 server 统一 `AppError`/`errorHandlerPlugin` 管线，同时保持 `{ error: { message, type, code, hint } }` 响应体不变；`openai-gateway.ts` 不再手写 gateway 业务错误的 `set.status`，auth resolve 守卫改抛 `UnauthorizedError`。验收：`apps/server` typecheck、openai-gateway/auth/api-keys 路由测试、`@excuse/gateway` 包测试通过。
- **client API 错误解包收敛（TODO2 client 补遗）**：`api-keys.ts`、`notifications.ts`、`admin.ts` 统一复用 `client.ts` 的 `unwrapEden<T>`，删除 admin 本地复制的 unwrap 与手搓 `.data/.error` 分支，避免绕过统一 401/403 和服务端错误消息处理。验收：client typecheck / client test 通过。
- **client SSE 启动竞态补刷（TODO2 client 补遗）**：`realtime-sync` 的 SSE `onOpen` 回调除刷新 generation records 外，也 invalidate Canvas assets polling 与 pipeline-runs polling 查询，补偿首次连接或重连前可能丢失的 `pipeline_node_update` 事件；不重排 AuthProvider/App 生命周期，保留现有连接职责。验收：client typecheck / client test 通过。

- **P3 处置批次：§1.5 标记完成 / §4.1 接受现状 / §4.2 文档化策略表 / §3.6 暂不批量处理**（TODO2）：① §1.5（CLAUDE.md 4→3 workload + 删 `pollExportingProjects`）已在先前 commit 完成，本批补标记；② §4.1 `@excuse/auth` 32 行包——采用「保留但接受现状」（纯度边界成立且被边界检查器强制，§4.3 不建议大动包结构），零代码改动；③ §4.2 `getTaskPriority`/`computeRetryDelay` 给两函数加文档注释，明确同属 task-engine 持有的「策略表」（权衡后接受字符串特判，新增慢阶段一处登记）；④ §3.6 client 78 处手动 memo——按解法自身的谨慎前提（「仅保留经 profiling 确认」「先开 compiler eslint 插件校验」），无 profiling 基线下不批量删除（stale-closure/re-render 回归风险），改为接触密集组件时顺手清理。验收：task-engine test(25)/lint 全绿。

### Fixed

- **优雅关停 drain 统一任务队列（不只视频）**（TODO2 §1.3）：此前 `currentTaskPromiseRef` 只在视频轮询源赋值，统一队列最长的 `canvas.assemble`（FFmpeg 合成，数分钟）收到 SIGTERM 被 `process.exit(0)` 直接砍掉，task 卡 `running`、锁被带飞，只能等 5 分钟孤儿回收——「最长的活反而最不安全」。修复：`createTaskPollSource` 把 in-flight promise（提取为 `executeClaimedTask`）写入共享 `currentTaskPromiseRef`，`setupGracefulShutdown` 关停时 await 它，drain 所有 poll source。`currentTaskPromiseRef` 类型从 `Promise<TaskResult>` 放宽为 `Promise<unknown>`（两源 promise 形状不同），视频源改用本地 typed 变量保留 `TaskResult` 推断。新增 `poll-sources.test.ts`（drain 行为 + 无任务不触碰 ref）。顺带 worker `test` 脚本加 `--isolate`（与 root 调用 + server 一致，worker 套件含 `mock.module` 需隔离）。验收：worker typecheck/lint/test(108, +2) 全绿。§1.1/§1.2/§1.4 仍待 §一 A/B 决策。

### Changed

- **CLAUDE.md 文档同步 + 边界检查器补强**（TODO2 §五 + §六）：代码已走在文档前面，逐项对齐。① CLAUDE.md「9 阶段」→ **12 阶段**（补 dialogue/bgm/assemble 的正确顺序）；② pause-before 门槛 → **3 个**（`storyboard`/`videos`/`assemble`），同步修 `packages/workflow-engine` 的 `isPauseBeforePhase` doc 注释；③ Worker「4 个 workload」→ **3 个 PollSource**（字幕导出已迁 `media.burn-subtitle` task）；④ 关键领域类型真身指向 `packages/shared/src/domain-types.ts`（db 下已是 re-export shim）；⑤ `generate.video` 幽灵 type → 改为「video 仍走 generation_records 旧轮询」说明并指向 §一决策；⑥ provider 门面说明改为「零消费者直连」并指向 §3.1。⑦ **边界检查器补强**：`error-recovery` 加入纯包规则；新增 domain 规则覆盖 `canvas-engine`/`prompt-engine`（禁 db/provider/storage/ffmpeg/apps）；补 3 条 `DEFAULT_BOUNDARY_RULES` 违规拦截测试；顺带修复脚本 `relative()` 在 Windows 输出反斜杠、与 CI/*nix 不一致的预存在缺陷（归一化为正斜杠，连带修好一条 Windows-only 失败的既有 boundary 测试）。⑧ e2e fixture 补 `asrStaleTimeoutMs`（§2.5 新增 config 字段的遗漏）。验收：typecheck（含 e2e）/ lint / check:boundaries / boundary test(5) 全绿。

- **generate 路由编排下沉 service — submit/retry 共享 prepareGeneration**（TODO2 §2.4）：`generate.ts`（441 LOC）此前的 POST `/generate` 与 POST `/records/:id/retry` 各自重复「预估费用 + 创建/重置记录 + credit 预留 + 构造执行上下文」编排块，违反 route→service→repo 分层、retry 与 submit 校验漂移风险高。修复：在 `modules/generation/service.ts` 新增三个函数——`estimateGenerationCost`（封装 `calculateCost` + `extractBillingParams`）、`createGenerationRequest`（封装 `createGenerationRecord` + 标准 estimated cost 信封）、`prepareGeneration`（预留 credit + 构造 `GenerationContext`，result-style `{ ok, context | message }` 保持 service 无 HTTP 语义契约）。submit 与 retry 共享 `prepareGeneration`（`creditSource: 'generate' | 'retry'` 仅区分审计描述「生成任务预留 / 重试生成任务预留」），余额不足仍由 route 抛 `PaymentRequiredError(402)`。route 不再直接调 `createGenerationRecord` / `calculateCost` / `reserveCredit`（仅注释提及），generate.ts 440→426 行；`resetGenerationToPending` 保留在 retry route（retry 专属、非重复）。设计取舍：未把 PaymentRequiredError 引入 service——延续既有 `reserveGenerationCredit` 的 result-style 契约，HTTP 码映射仍是 route 职责。验收：typecheck/lint 通过；generate-routes-retry-cancel(15) 全绿，`mockReserveCredit`/`mockResetToPending`/`mockDebitCredit`/`mockRefundCredit` 调用参数断言不变。

### Fixed

- **server 域模块 51 处裸 `throw new Error` → AppError 子类**（TODO2 §2.3）：`errorHandlerPlugin` 只特殊处理 `AppError`，其余裸 `Error` 一律扁平化为 500，导致「项目不存在」「镜头不存在」「只能重试失败的镜头」「画布布局字段非法」等语义上 404/409/422 的业务错误在同步路径上变成不透明 500。修复：把 `modules/canvas/*`、`modules/generation/service.ts`、`modules/subtitle/service.ts` 共 51 处转换为 `NotFoundError`(404) / `ConflictError`(409) / `ValidationError`(422) / `BadRequestError`(400) / `InternalError`(500)。映射口径：资源不存在→404；项目未分析/正在生成/镜头状态前置不满足→409 Conflict；布局字段与 `validateAndMerge` 参数校验→422；未知模型 id/不支持音频类模型→400；fetch 后 DB 更新意外返回 null→500（honest 5xx）。`项目不存在或未分析` 这类二义条件拆为 NotFoundError + ConflictError 两段，语义更精确。`fireAndForget` 阶段（analysis/characters/.../videos pipeline）的转换属一致性收敛（其 throw 本就被 `.catch` 吞掉，且路由层已先行 NotFound 校验），不影响当前 HTTP 行为但为未来同步调用方就位。**唯一保留** [openai-gateway.ts:229](apps/server/src/routes/openai-gateway.ts#L229) 的 `throw new Error(res.error)`——经核对它被 `handleGatewayChatCompletion` 的 catch 捕获并经 `generationFailedError` 整形为 OpenAI 形态错误（非裸 500），属 §3.5 gateway 错误协议范畴，不在此项转 AppError。验收：typecheck/lint/build 通过；新增 ConflictError(409)/ValidationError(422) 的 statusCode 单测；canvas-service-helpers(19)/subtitle-service(9)/canvas-layout(13) 直接相关测试全绿；保留消息（「项目正在生成中…」「视频文件不存在…」）断言不变。（注：全量 server test 在本机环境存在与本次改动无关的预存在超时/404-vs-403 失败——已用 `git stash` 在干净 commit 70ba9df 上复现同一批失败确认非本次引入。）

- **ASR 字幕轮询超时守卫 + 失败收尾收敛**（TODO2 §2.5）：此前 `processASRTask` 的 PENDING/RUNNING 分支只 log 后返回，无超时上限（视频路径有 4h，ASR 无），且 poll-sources 的 catch 只 log，瞬时 fetch/queryTask 失败让 `subtitle_project` 行每 5s 被重查到天荒地老。修复：① `processASRTask` 新增超时守卫（镜像视频路径 `task-processor.ts`），锚点 `subtitle_projects.updatedAt`（= 进入 `asr_processing`/提交 ASR 的时刻，PENDING/RUNNING 轮询期间不更新），超 `asrStaleTimeoutMs` 即标记 project failed + record failed + SSE + 用户通知；② 新增 config `asrStaleTimeoutMs`（默认 1h，ASR 远快于视频；env `WORKER_ASR_STALE_TIMEOUT_MS`）；③ 抽出 `failAsrTask` 收尾序列，FAILED 与超时两路共享，消除重复；④ poll-sources 传入 `ctx.config.asrStaleTimeoutMs`。瞬时错误仍由 catch 留 project 在 `asr_processing` 下一轮重试，持续失败时由超时守卫收口为 failed。验收：worker typecheck/lint/test(106, +2 超时用例) 全绿。

- **死代码清理：pollExportingProjects / createClient / createVideoPollSource `_processor` 形参**（TODO2 §八 死代码清单）：删除三处零调用方的死代码。① `packages/db/src/repositories/subtitle-projects.repo.ts` 删除 `pollExportingProjects`（字幕导出早已迁到 `media.burn-subtitle` task，该轮询函数零调用方；`processExportTask` 此前已删）；② `apps/server/src/modules/canvas/service-helpers.ts` 删除 `createClient`（被 `ServerContext`/`WorkerContext` 构造期注入取代，全仓库零引用）及其唯一消费者 `DashScopeClient` import；③ `apps/worker/src/poll-sources.ts` 的 `createVideoPollSource` 删除未使用的 `_processor: unknown` 形参（函数内部自行 `createTaskProcessor(ctx)`，外部传入的 processor 从未使用），同步移除 `apps/worker/src/index.ts` 的 `const processor = ctx` 与对应实参。验收：typecheck/lint/worker 104 test 全绿。



- **client 10 处手写 fetch() 收敛为 Eden treaty**（TODO2 §3.4）：此前 `asset-library.ts`（7 处）与 `SubjectLibrary.tsx`（3 处）绕过 Eden treaty 手写 `fetch()`，丢失端到端类型安全与 `unwrapEden` 的 401 清理。修复：① `client.ts` 新增 `hideAsset` / `toggleFavoriteAsset` / `assignTagToAsset` / `unassignTagFromAsset`（资产操作）、`listAssetTags` / `createAssetTag` / `deleteAssetTag`（标签 CRUD）、`listSubjects` / `deleteSubject` / `toggleSubjectFavorite`（主体资产库）共 10 个 Eden treaty wrapper 函数，导出 `unwrapEden` 供复用；② `asset-library.ts` 退化为薄 re-export，移除 `parseError` helper 与全部 `fetch()` 调用；③ `SubjectLibrary.tsx` 3 处 `fetch()` 替换为 typed Eden import。验收：typecheck/lint/build/client test(376)/server test(547) 全绿。

- **adapter 仪式清理：删除 pause/resume 死代码 + 未使用的 can* 守卫**（TODO2 §3.3）：`task-engine` 删除 `TaskPauseAdapter` / `PauseTaskWithAdapterInput` 接口、`pauseTaskWithAdapter` / `resumeTaskWithAdapter` 函数、`canPauseTask` / `canResumeTask` / `canRequeueTask` / `canCancelTask` 守卫（均零生产调用方）；`workflow-engine` 删除 `canRetryPipelineRun` / `canPausePipelineRun` / `canResumePipelineRun` / `canResumeFromPhase`（均零生产调用方）。保留 `canCancelPipelineRun`（被 handlers-phases.ts 使用）与 `isRetryablePipelineRun`（工具函数）。验收：typecheck/lint/build/boundaries/test(task-engine 25/workflow-engine 32/server 547) 全绿。

- **canvas.dialogue/bgm/assemble 跑过 markRunRunning/markRunSucceeded 信封**（TODO2 §2.2）：此前这 3 个后加阶段被内联在 `task-handler.ts`，成功时不标记 `canvas_pipeline_runs` 状态也不发 PG NOTIFY，导致前端进度条卡死在上一阶段。修复：创建 `canvas-dialogue.ts`/`canvas-bgm.ts`/`canvas-assemble.ts` 三个 execute 文件，在 `canvas-handlers.ts` 新增 3 个 handler 走统一 `markRunRunningAndNotify → execute* → markRunSucceededAndNotify` 信封，`task-handler.ts` 改为懒加载委派（与另 9 阶段一致）。验收：typecheck/lint/build/worker test/server test/client test/boundaries 全绿。

- **Drizzle 迁移 journal 补齐 0034–0038**（TODO2 §2.1）：journal 仅 34 条（末尾 0033），磁盘有 0034–0037 四个手写 SQL 未入 journal + 无 snapshot，`db:migrate` 从空库拉不出当前 schema（缺 subject_library、dialogue/bgm/assemble 阶段列、计费 numeric 列等）。额外发现 `password_reset_tokens` 表从未有迁移。修复：① journal 加入 0034_subject_library / 0035_dialogue_bgm_assemble / 0036_assemble_final_video / 0037_billing_fractional_cents + 新增 0038_password_reset_tokens（手写 SQL）；② 生成 0037 + 0038 snapshot（全 schema 快照）；③ 验证：空库 `db:migrate` → 27 表齐全、计费列 numeric(20,4)、phase enum 12 值。`db:generate` 报 "No schema changes"（snapshot 与 schema TS 一致）。

- **events 测试 addConnection 返回类型漂移（仓库预存在债，本提交）**：`packages/events/test/index.test.ts` 仍断言 `hub.addConnection(...)` 返回数字（`.toBe(1)` / `.toBe(2)`），但 `UserEventHub.addConnection` 早已演进为返回 `AddConnectionResult = { accepted, userCount, totalCount, reason? }`（新增全局/单用户连接上限检查）。生产侧 `apps/server/src/routes/sse.ts` 已正确消费新契约（`result.accepted` 拒绝时 503），仅测试过时。修复：更新断言为完整 `AddConnectionResult` 形状；补两处此前**完全未覆盖**的连接上限拒绝行为单测（单用户上限 3、全局上限，验证 `accepted:false` + `reason` + 原连接不受影响）——这正是返回类型变更的动机，属真实风险（SSE DoS 防护）。验收：events 测试 19/0（此前 1 失败）。无生产代码改动。

### Changed

- **计费列改 numeric(20,4) 支持 sub-cent 定价（TODO §四 计费冲突，本提交）**：解决 E2E 冒烟实测触发的「integer 计费列 vs sub-cent 文本/音频定价」冲突——`calculateCost` 对文本（按 1M token）与音频（按秒）产出小数分（如 qwen-max 1000/500 token → 0.72 分），落库到 integer 列抛 `22P02 invalid input syntax for type integer`，导致 `/api/generate` 文本与 `/v1/chat/completions` gateway 任何非整分成本都失败（视频/图片为整分不受影响）。**方案：credit ledger 与计费冗余列统一改 `numeric(20,4)`（精度 4 对齐 currency.js），而非 storage 边界取整（取整会让 ASR / fun-music 按秒计费损失真实收入）。** ① **schema 11 列 integer → numeric `mode: 'number'`**（4 个 schema 文件）：`generation_records.total_price_cents`、`canvas_assets.total_price_cents`、`credit_accounts.{available,frozen}_cents`、`credit_transactions.{amount,balance_after,frozen_after}_cents`、`usage_events.{reserved,debited}_cents`、`api_keys.{quota_max,total_spend}_cents`。`mode: 'number'` 让 drizzle 在 **ORM 层** 把 numeric 映射为 number（`.$type<number>()` 只改 TS 类型、不改运行时，实测分值列经 drizzle 读回仍是 string，`mode` 才是运行时正解）。② **`packages/db/src/db.ts` 注册 numeric type parser**（导出 `numericTypeParser`，测试 helper 复用）：drizzle `mode: 'number'` 只覆盖**列读**（select/returning），但 admin 仓储的**裸 SQL 聚合**（`coalesce(sum(...),0)`，非列引用）仍绕过 ORM 映射——故在 `postgres` 驱动层注册 numeric(1700)→`Number.parseFloat` parser，统一覆盖列读与裸聚合（否则聚合结果以 string 返回，`totalSpendCents + actualCost` 退化为字符串拼接）。③ **`credit.repo.ts` `assertPositiveAmount` 放宽**：从「正整数分」改为「正数分」（拒绝 0/负数/NaN，允许小数）。④ **admin 聚合去 `::int` 截断**：`providers/overview/users/gateway` 仓储中金额类 `coalesce(sum(...),0)::int` 去掉 `::int`（`count(*)::int` 计数保留），保留 sub-cent 聚合精度。⑤ **迁移 `0037_billing_fractional_cents.sql`**（bare SQL，匹配 0034-0036 不入 journal、走 db:push 的约定；integer→numeric 无损上转，无 USING 子句，既有整数分原样保留为 4 位小数）。⑥ **E2E 验证**：fake 文本 usage 由 0/0 改为 1000/500 token（注册赠送 1000 分覆盖 sub-cent debit），text-gen 旅程新增断言记录落库后 `cost.totalPriceCents > 0`（0.72 分小数分完整 reserve→debit 落库——正是此前会 22P02 崩溃的值）。`calculate.test.ts` 原本就断言 fractional cents（0.72/0.0012/0.004），无需改。验收：typecheck/lint/build/boundaries + test:db + e2e 全绿。对应 TODO §四 计费条目已标记完成。

### Added

- **端到端冒烟测试 + 可测应用工厂（TODO 三、1，本提交）**：落地「provider 依赖注入地基」的可验证闭环——真实 server + worker 处理器 + Postgres + SSE 桥接跑关键用户旅程，provider 由 fake 适配器注入，全程不访问真实 DashScope。① **抽出 `apps/server/src/app.ts` `createElysiaApp(config, ctx)`**：把中间件链 + 全部路由装配从 `index.ts` 的入口副作用中剥离（此前任何 import 都触发 `app.listen` + SSE + observer 注册，无法可控起停）。生产 `index.ts` 调它后照常 `listen` + 起 SSE + 注册 provider observer/guard；E2E 拿到「装配好但未监听」的 app 自行 `listen(0)` + 注入 fake + `stop()`。行为保持（中间件顺序/路由集合/OpenAPI 挂载条件完全一致）；额外支持绝对 `storageRoot`（临时目录）解析。② **`e2e/` 目录**：`fixtures/fake-provider.ts` `FakeDashScopeClient`（实现 `DashScopeClient` 全部公开方法面，返回确定性 文本/图片/视频任务/音频/任务查询 结果 + 记录调用，经 `as unknown as DashScopeClient` 单点转换注入 overrides）；`fixtures/stack.ts` `startTestStack()`（临时本地存储 → `createServerContext`/`createWorkerContext` 注入 fake → `createElysiaApp` → `listen(0)` → `startSSEListener` 真实 PG LISTEN → 驱动 worker `createTaskProcessor`）；`smoke.test.ts` 8 个旅程。③ **覆盖旅程**：健康就绪 / 注册登录（Bearer 第 3 通道 + 401）/ 文本生成（server 侧 fake，同步 succeeded）/ API Key + Gateway chat（gateway 侧 fake，OpenAI 形态）/ Canvas analyze（in-process fire-and-forget，fake 返 NovelAnalysis JSON → run succeeded）/ 视频生成（worker 处理器 fake `queryTask` + reserve→debit 完整计费闭环）。每旅程断言 `control.calls.*` 增长即 provider 注入生效。④ **脚本/类型**：根 `test:e2e` + `typecheck:e2e`（`e2e/tsconfig.json`，`@excuse/*` paths 映射；纳入根 `typecheck` 并发链）；CI 新增 `e2e` job 复用 test job 的 postgres service + db:push 口径。⑤ **冒烟实测发现并记录（后续提交已修复，见上「计费列改 numeric(20,4)」）既有计费冲突**：`generation_records.total_price_cents` 与 credit ledger 均为 `integer`，而 `calculateCost` 对文本/音频产出 fractional cents（如 0.0216 分），非整分成本落库抛 `22P02`——导致 `/generate` 文本与 gateway 任何非整分成本失败（视频/图片整分不受影响）。验收：typecheck（含 e2e）/ build / boundaries 全绿；E2E 8/8 且跨 3 次运行幂等；server 547 / worker 104 / client 376 / 全 packages 绿（仅 packages/events 1 处预存在失败，与本提交无关）。对应 TODO 三、1 已删除。
- **Phase 11 assemble — FFmpeg 视频拼接 + BGM 叠加（产品二、2，本提交）**：补齐对话式音视频流水线终段。① **`packages/ffmpeg/src/compose.ts`**：`concatVideos`（`-f concat -safe 0` demuxer + `libx264`/`aac` 重编码兜底，兼容同项目镜头分辨率/编码微小差异，保留各镜头 HappyHorse 原生对话音频）+ `hasAudioStream`（ffprobe 探测）+ `mixBgmTrack`（视频有声轨→`amix` 保留对话原声并混入降音量 BGM；无声轨→BGM 作唯一音轨 mux；`-stream_loop -1` 循环铺满 + `-shortest` 跟随视频时长）。② **`packages/storage`** 新增 `localCopyPath`（存储 URL→本地副本路径，OSS URL 也解析到 `saveLocal` 副本，避免重复下载）+ `downloadToFile`（本地副本缺失时回退）。③ **`canvas-runtime/phases/assemble.ts` `runAssemblePhase`**：物化镜头视频（按 shotIndex 排序，优先本地副本）→ concat → 可选 BGM 叠加 → 上传最终视频 → 清理临时目录；修复 `pathExists` 误用（`Bun.file().exists()` 返回 boolean 不抛错，原 try/catch 恒真）。④ **db** `canvas_projects.final_video_url` 列 + `0036` 迁移；server `assembleProject` 模块 + `handleAssemblePhase`；worker `canvas.assemble` handler。⑤ 测试：`localCopyPath` 单测（本地/OSS/自定义 base/无法识别）+ 真实 ffmpeg 命令冒烟（concat 产 4s 音视频、amix/mux 两分支均通过）。对话音频合成 = 拼接保留镜头原生音频（无需独立 TTS）。验收：typecheck / build / server 547 / worker 104 / canvas-runtime 62 / client 376 全绿；boundaries 通过。对应 TODO 二、2 assemble 已删除。

- **R2V builder — buildR2VRequest 多参考图预算 + reference_media 接入（产品二、2，本提交）**：让 R2V 多主体一致性真正按预算规范组装参考图。① **`shared/domain-types` 新增 `R2VReferenceMedia`**（kind: turnaround/portrait/scene/extra + characterId/locationId + imageNumber）。② **`canvas-runtime/pure/r2v.ts`**：`buildR2VRequest`（按「主要角色 turnaround → 次要角色 portrait → 场景 → 预留额外」优先级排序，裁剪到 DashScope 上限 9，firstFrame 排除属 I2V 语义）+ `extractSpeakingCharacterIds`（dialogueJson 说话者名→角色 ID，`unknown` 入参运行时安全收窄）。③ **dialogue 阶段产出 `reference_media`**：每镜头解析参考图 + 用 dialogueJson 说话者做主要角色优先，写入 `shot.reference_media`（server 模块 + worker handler 持久化）。④ **videos 阶段消费**：R2V 复用 dialogue 产出的预算参考图（≤9、按说话者优先），缺失回退全量解析。⑤ **db** `canvas_shots.referenceMedia` 列补 `$type<R2VReferenceMedia[]>`。⑥ 测试：buildR2VRequest/extractSpeakingCharacterIds 单测（优先级、裁剪、firstFrame 排除、非受信输入收窄）。验收：typecheck / build / server 547 / worker 104 / canvas-runtime 62 / client 376 全绿。对应 TODO 二、2 R2V builder 已删除。

- **Phase 10 bgm — fun-music-v1 模型配置 + provider audio 支持 + BGM 阶段（产品二、2，本提交）**：为对话式音视频接入 BGM 生成。① **shared**：`ModelCategory` 新增 `audio`、`RequestType` 新增 `audio`。② **provider**：`fun-music-v1` 配置（`AUDIO_MAPPING`；邀测价 0.002元/1000秒=0.0002分/秒，按秒小数计费）+ `DashScopeClient.generateAudio`（同步，解析 `output.audio.url` + `usage.duration`）+ `AudioProviderResult/Output` 类型 + `generate()` 路由 + `FunMusicResponse`；`/api/models` 过滤 audio（内部专用），generate 路由 category 守卫拒绝 audio（不进通用生成流），`executeGeneration` 类型收窄兜底。③ **db** `canvas_projects.bgmUrl` 列补入 TS schema（0035 SQL 已声明）。④ **prompt-engine `buildBgmPrompt`**（故事摘要 + 镜头主导情绪 → FunMusic 音乐描述，情绪→风格/配器映射）。⑤ **canvas-runtime `runBgmPhase`**（生成 + 转存 OSS + 返回 url/时长）。⑥ server `generateBgm` + `handleBgmPhase`；worker `canvas.bgm` handler。⑦ 测试：`generateAudio` / `buildBgmPrompt` 单测；修正 `pipeline-stepper` 过期断言（先前阶段枚举提交新增 dialogue/bgm/assemble 后未更新：9→12 阶段、pause size 2→3）；pricing-snapshot 放宽 audio 按秒小数定价。验收：typecheck / build / server 547 / worker 104 / provider 64 / prompt-engine 93 / client 376 全绿。对应 TODO 二、2 bgm 已删除。


- **大文件拆分 — Admin.tsx 拆为 Admin/ 目录（地基一、2 继续，本提交）**：按 TODO 拆分方向将 2417 行的 `Admin.tsx` 拆为目录结构。`Admin/index.tsx` 保留 main Admin 组件 + overview/users/gateway 三个 Tab（因使用父组件状态/数据查询而紧密耦合）；`Admin/Providers.tsx` / `Admin/Projects.tsx` / `Admin/Audit.tsx` 分别承载三个自包含 Tab；`Admin/shared.tsx` 承载 Tab 间共享的常量、工具函数与小组件（`StatCard` / `StatusList` / `TaskTable` / `formatDate` / `shortId` 等）。验收：typecheck / build / client 376 test 全绿。
- **大文件拆分 — task-processor + canvas-runtime（地基一、2 继续，本提交）**：按 TODO 拆分方向继续处理两个文件。① **task-processor.ts**（414→320 行）：抽离 `task-processor-utils.ts`（`extractVideoUrl`/`extractVideoDuration`/`refundReservedCredit`/`updateCanvasShotAndProject`/`checkProjectCompletion`），主 processTask 逻辑保留在原文件。② **canvas-runtime/index.ts**（411→14 行 barrel）：拆为 `pure/references.ts`（`resolveShotVideoReferences`/`toPromptReferenceEntries`）+ `pure/model.ts`（`recommendCanvasVideoModel`/`getCanvasVideoModel`）+ `io/asset.ts`（`runCanvasAssetStep`/`generateCanvasImageAsset`）+ `io/video.ts`（`submitCanvasShotVideo`/`prepareCanvasVideoParams`）+ `io/types.ts`，index.ts 退化为干净 barrel。
- **大文件拆分 — worker/index.ts + canvas.ts（地基一、2 部分完成，本提交）**：按 TODO 拆分方向处理两个接触到的文件。① **worker/index.ts**（301→60 行编排）：抽离 `worker-lifecycle.ts`（健康服务器/优雅退出/孤儿清扫）+ `poll-source.ts`（PollSource 接口）+ `poll-sources.ts`（三个轮询源实现：统一任务队列/遗留视频/ASR），主循环简化为 `for (const source of pollSources)` 遍历。② **canvas.ts**（921→0 行，替换为 `canvas/` 目录）：拆为 `helpers.ts`（fireAndForget/task-driven 模式）+ `handlers-project.ts`（项目 CRUD/管道查询/Layout/模型偏好）+ `handlers-phases.ts`（9 阶段 + cancel-active）+ `handlers-resources.ts`（角色/场景/镜头 PATCH/DELETE/retry/regenerate/资产）+ barrel `index.ts`（Elysia 链式注册，handler 逻辑在子文件）。行为不变，对外 API 不变，barrel 保持 `createCanvasRoutes` 导出。验收：typecheck/lint/build 全绿，server 547 测试 / worker 104 测试 0 失败。

- **provider / worker 依赖注入（地基一、1 任务 2+3，本提交）**：完成 Server 侧所有 `DashScopeClient` / `AssetStorage` / `ASRClient` 构造期注入改造，与上次提交的 WorkerContext 形成完整对称。① **新增 `apps/server/src/context.ts`** — `ServerContext` 接口 + `createServerContext(config, overrides?)` 工厂，与 `WorkerContext` 对称设计，overrides 为 E2E 测试 fake provider 注入预留注入口。② **5 个 route factory 改造**：`generate.ts` / `openai-gateway.ts` / `subtitle.ts` / `canvas.ts` 统一通过 `ctx: ServerContext` 参数接收共享实例，移除各自 `new DashScopeClient`/`new AssetStorage`/`new ASRClient` 的散点构造。③ **7 个 canvas service module 改造**：`analysis.ts` / `characters.ts` / `locations.ts` / `storyboard.ts` / `references.ts` / `videos.ts` / `regenerate.ts` 的函数签名从接收 `config`（含 dashscopeApiKey/dashscopeBaseUrl）改为直接接收 `client: DashScopeClient`（references 另加 `storage: AssetStorage`），消除内部 `createClient(config)` 和 `new AssetStorage(...)` 调用。④ `service-helpers.ts` 的 `createClient` 辅助函数不再被任何模块调用（保留不删，向后兼容）。⑤ **11 个测试文件适配**：所有 `createCanvasRoutes(config)` / `createGenerateRoutes(config)` / `createOpenAIGatewayRoutes(config)` 调用点增加 `createServerContext(testConfig)` 创建的 ctx 参数。⑥ 验收：typecheck / lint / build / server test(547 pass) / client test(376 pass) 全绿。对应 TODO 地基一、1 任务 2+3 已完成。

- **Phase 2.1 HappyHorse 对话 Prompt 优化（产品阶段二，本提交）**：把 Canvas 镜头视频从「无声纯视觉」升级为「带对话/环境音频」，并让有角色参考图的镜头真正用上 R2V 多主体一致性 + `[Image N]` 指代。不改数据模型（对话内容复用 storyboard 既有 `narrative` 字段）。① **对话音频编排** `packages/shared/src/canvas.ts` 新增纯函数 `hasDialogueAudio(narrative)`（启发式判定 narrative 是否含成对中英文引号对白，client/server/worker 共用；刻意不匹配 `Name:` 冒号形式避免时间戳 `5s:` 误判）；`packages/prompt-engine/src/prompt-builder.ts` 的 `buildShotVideoPrompt` 新增「Audio: dialogue & sound effects」段——有对白时要求模型按 narrative 引号文本生成对白语音、语气对齐 `continuity.emotionStart/End`，无对白时标注 `no character dialogue`，环境音效从 `environment.backgroundMotion` 派生 `ambient sound`（既有 8 段措辞不变，audio 段插在 Current shot 与 timeline 之间）；`PromptShot` 增可选 `hasDialogue` 字段，`buildShotVideoPromptEntity` 用 `hasDialogueAudio` 预判显式传入。② **storyboard LLM 让 narrative 带对白** `packages/prompt-engine/src/prompts.ts` 的 `buildStoryboardPrompt` system 新增「# 对话与音频」章节（对白用中文引号「」、标明说话者与语气、单镜头 2-3 轮上限、自然带出环境音效，给正/反示例）+ user 要求清单追加 narrative 须交织对白+音效；ShotDraft/shotDraftsSchema/validateShotDrafts 完全不动（narrative 仍是 string，无 JSON 字段结构变更）。③ **R2V 角色优先 turnaround + `[Image N]` 指代烘焙** `CanvasVideoReference` 增可选 `characterId`/`locationId`（角色/场景自动解析时填充，向后兼容 role 不变）；`resolveShotVideoReferences` 角色图优先取 `turnaroundSheetUrl`（三视图，跨镜头一致性更好，对齐阶段二风险笔记）回退 `referenceImageUrl`，ref 附 id；新增 `toPromptReferenceEntries(refs)` 把带 id 的 ref 映射为 `PromptReferenceEntry[]`（imageNumber = 数组 1-based 位置）；`buildShotVideoPrompt` 增可选 `references` 参数，有指代的角色/场景在 consistency 段写 `[Image N]`，无指代/缺省则纯文字；worker `executeCanvasRebuild`（live 路径）与 server `rebuildShotPrompts`（FF 路径）循环内调 `resolveShotVideoReferences` 构建 references 传给 builder——顺序一致性：rebuild 烘焙编号 = resolveShotVideoReferences 顺序 = submit referenceUrls 顺序 = provider media[] 顺序（同一纯函数消解漂移）。④ **前端音频指示器** `ShotNode` 用 `hasDialogueAudio(shot.narrative)` 在「时长」旁显示 🔊 对话音频 / 🔇 无对白 徽章。测试：shared `hasDialogueAudio` 8 条、prompt-builder/canvas-prompt-builder 各加对话音频段 + `[Image N]` 指代断言、video-recommend 补 turnaround 优先 + ref 附 id + toPromptReferenceEntries、pure-compute 补 `[Image N]` 烘焙/无指代、prompts/canvas-prompts 补 storyboard 对话音频指令。验收（对齐 TODO）：生成的视频带对话/环境音频（audio 段进入 video_prompt）；R2V 在有参考图时自动启用（既有调度链路 + 本阶段补 turnaround 优先 + `[Image N]` 烘焙）；前端 shot 卡片显示音频标识。分 4 个子提交（音频编排 / storyboard 对白 / R2V turnaround+[Image N] / ShotNode 指示器）。对应 TODO Phase 2.1 已删除。

- **admin-dialogs.tsx 清理未用导入与 lint，修复客户端构建（用户请求，本提交）**：`apps/client/src/pages/admin-dialogs.tsx` 此前携带一批未使用导入（`AdminGatewayClientDetail` / `FormEvent` / `Pencil` / `useEffect` / gateway 相关 query+fetch / `AlertDialog*` / `Select*`），在 strict + `noUnusedLocals` 下导致 client typecheck/build 失败（TS6196/TS6133/TS6192）——这些符号是为尚未编写的 gateway client 对话框预留的死导入（全文件零引用）。删除全部未使用导入 + eslint --fix 规整 TaskTable 内 onClick arrow 为多语句多行（`style/max-statements-per-line`）+ 清理 trailing space，行为/组件树不变。验收：admin-dialogs lint 干净；client `tsc -b` 零错误；client build 通过；client test 376 全绿。

- **OpenAI Gateway 路由继续抽薄：stream 编排下沉 service（代码治理 backlog，本提交）**：把 `routes/openai-gateway.ts` 的 stream 路径业务编排下沉 `services/gateway-service.ts`，与非 stream 编排器对称。① **service 新增 `handleGatewayStreamChatCompletion(input, onChunk)`**：与既有的 `handleGatewayChatCompletion`（非 stream）对称，复用同一套 `setupGatewayCall` / `settleGatewaySuccess` / `settleGatewayFailure` 原语；provider 逐批增量经 `callProvider: () => AsyncIterable<GatewayStreamProviderChunk>` 拉取，service 聚合 fullText 用于成功结算（计费用 usage），每个增量 + 收尾（finishReason='stop'）通过 `onChunk: StreamChunkSink` 回调交给 route。service **不耦合 Web Streams API**——只产出领域 `GatewayStreamChunk`（delta/finishReason/isFirst/usage），ReadableStream 构造与 OpenAI SSE 编码（createOpenAIStreamChunk + serializeOpenAIStreamChunk）留 route。② **route `handleStreamChatCompletions` 收薄**：构造 ReadableStream，把 onChunk 实现为「领域 chunk → OpenAI SSE 事件 → controller.enqueue」，setup 失败（余额不足）发结构化 error 事件后结束。route 仅剩 HTTP 层（参数解析 / API Key scope+quota 访问控制 / stream 分支 / 响应塑形）。③ **顺手修复 gateway-service.ts 两处既有 TS6133 unused 错误**（前序提交遗留）：`assertCreditLedgerPolicy` import 未用（移除）、`settleGatewaySuccess` 解构的 `estimatedCost` 未用（移除）——这两处此前导致 client typecheck（经 App 类型图传递编译 server）报错。④ service 不引入 OpenAI SSE helper 依赖（编码属 route HTTP 契约），保持与非 stream 编排器的边界一致。验收：server typecheck 通过；openai-gateway 测试 25 pass / 0 fail（覆盖 stream/非 stream/credit/quota/SSE 结构）；全量 server 测试 547 pass / 0 fail（较前 543 增 4，因 gateway-service 既有错误修复后相关套件稳定通过）；eslint 通过。对应 TODO openai-gateway.ts 已删除。

- **assets 路由/服务拆分（代码治理 backlog，本提交）**：把 `routes/assets.ts`（708 行）的三来源合并查询与序列化业务逻辑抽到新 `modules/assets/service.ts`，route 收薄为 HTTP 层。① **新增 `modules/assets/service.ts`（436 行）**：集中三来源（generation_records / canvas_assets / uploaded_files）合并逻辑——查询参数规整导出函数（`resolveSort` / `clampInt` / `parseDateParam` / `parseTagIdFilter`）、来源映射（`CANVAS_CATEGORY_KIND` + category/status 映射）、来源规划（`resolveSourcePlan`：source/kind/status/model → 需查哪些表）、行→DTO 序列化（`mapGenerationRecord/CanvasAsset/UploadedFile`，previewUrl 优先稳定 publicUrl、不暴露 provider 临时 URL）、以及主函数 **`listAssetLibrary(accountId, query)`**（并行查三来源 + 收藏/标签全量 key 集合，映射合并、排序、favorite/tag 注入与过滤、轻量分页 hasMore）。② **`routes/assets.ts` 收薄至 341 行**：GET `/assets` 的 ~120 行列表逻辑收敛为 query 规整 + 一次 `svc.listAssetLibrary(userId, {...})` + 响应塑形；隐藏/恢复/软删/收藏/标签等 mutation handler 保留（归属校验 + audit + 轻量 DB 调用，本就是干净的 HTTP 层）。③ **分层**：service 不 import Elysia，不抛 AppError（`listAssetLibrary` 返回 `AssetLibraryPage`，HTTP 响应/audit 由 route 负责），对齐既有 generation service 的边界风格；查询规整函数（`clampInt` 等）导出供 route 解析 query 后传入。④ **依赖修正**：`list*` 查询函数属 `@excuse/db`（shared 是 BASE 层不可依赖 db），仅 `isImageOutput/isVideoOutput/parseOutputResult` 来自 `@excuse/shared`。验收：server typecheck 通过；assets-routes + asset-tags-routes 测试 78 pass / 0 fail（覆盖三来源合并、source/kind/status/model/search 过滤、favorite/tagIds 注入与过滤、轻量分页、隐藏/软删/恢复/收藏/标签 mutation）；全量 server 测试 543 pass / 0 fail；eslint 通过。对应 TODO assets.ts 已删除。

- **generate 路由/服务边界清理 + credit 预留去重（代码治理 backlog，本提交）**：消除 POST `/generate` 与 `/retry` 两 handler 重复的「credit 预留 + audit + 余额不足失败路径」22 行块（含 `CreditError.INSUFFICIENT_BALANCE` → notify + markGenerationFailed + 抛 PaymentRequiredError 的特殊映射）。① **新增 service 函数 `reserveGenerationCredit(opts)`**（`modules/generation/service.ts`）：封装 reserveCredit + audit('credit_reserve') + 失败收尾（notifyInsufficientBalance + markGenerationFailed），返回**result-style** `CreditReservationResult = { ok: true } | { ok: false, reason: 'insufficient_balance', message }`——保持本 service「不涉及 HTTP 语义」的既有契约（对齐 `executeGeneration` 返回 `GenerationResult` 的风格），HTTP 402 映射仍由 route 负责。② **POST /generate 与 /retry 各自的 22 行预留块**收敛为一次 `svc.reserveGenerationCredit({...})` + `if (!reservation.ok) throw new PaymentRequiredError(...)`，零额度（totalPriceCents ≤ 0）短路返回 `{ ok: true }` 保持原行为。③ **generate.ts 收薄**：移除 4 个不再直接使用的 import（`CreditError` / `markGenerationFailed` / `notifyInsufficientBalance` / `reserveCredit`）；校验、视频限流、审计仍留 route（HTTP 语义，service 不抛 AppError）。④ **service.ts** 新增 `reserveCredit` / `CreditError` / `audit` / `notifyInsufficientBalance` import。设计取舍：未采纳「整条流程下沉」方案——会把 ValidationError/RateLimitError/PaymentRequiredError 等 HTTP 错误引入 service、打破分层、回归面最广；result-style 是去重与分层尊重的平衡点。验收：server typecheck 通过；generate-routes 测试 28 pass / 0 fail（覆盖 dedupe / credit 预留 / 余额不足 / retry / cancel）；全量 server 测试 543 pass / 0 fail；eslint 通过。对应 TODO generate.ts + generation/service.ts 已删除。

- **admin 仓储按业务域拆分（代码治理 backlog，本提交）**：把 1178 行的 `packages/db/src/repositories/admin.repo.ts` 拆为子目录 + barrel，参照 `modules/canvas/service.ts` 的 barrel 拆分模式。① **新增 `repositories/admin/` 子目录**，按 6 个业务域各一文件，单一职责：`overview.ts`（getAdminOverview + 5 个总览接口 + mapStatusCounts）、`tasks.ts`（listAdminTasks / getAdminTaskDetail / requeueAdminTask / cancelAdminTask + task 接口 + task 专属 helper/常量 serializeAdminTask / buildAdminTaskFilters / fetchTaskGenerationRecords / 状态枚举）、`users.ts`（listAdminUsers / getAdminUserDetail + 6 个用户接口 + buildAdminUserListFilters）、`providers.ts`（getAdminProviderStats + AdminProviderStatsDbRow，从原 users 段归位）、`projects.ts`（listAdminProjects + AdminProjectDbRow）、`gateway.ts`（listAdminGatewayClients / getAdminGatewayClientDetail + 5 个 gateway 接口）。② **`admin/internal.ts`** 收纳跨域私有 helper（`numberValue` / `iso`），DRY 且**不**经 barrel 再导出——保持内部，不污染对外 API；各域文件按需 import。③ **每个域文件只 import 自己真正用到的 drizzle helper / schema 表 / 外部 repo**（逐函数核对的 minimal dependency，如 providers 仅需 `desc, sql`，gateway 不含 `desc` 因排序走 raw sql）。④ **`admin.repo.ts` 退化为纯 barrel**（`export * from './admin/{overview,tasks,users,providers,projects,gateway}'`），`repositories/index.ts` 的 `export * from './admin.repo'` 与 3 个测试文件的 `import from '../src/repositories/admin.repo'` 完全不用改，对外 API 零变更；行为/SQL 原样搬迁。验收：server typecheck 通过；server 测试 543 pass / 0 fail（覆盖 admin 路由经 mock 的 barrel 导出解析）；函数体逐字搬迁、无逻辑变更（PG 集成的 3 个 admin db 测试因本地 test DB 未起未跑，SQL 未改）。对应 TODO admin.repo.ts 已删除。

- **notifications 路由/服务拆分（代码治理 backlog，本提交）**：消除「业务模块 / 其它路由反向 import 通知路由」的层级倒置。① **新增 `apps/server/src/services/notifications.ts`**：承载创建并 SSE 推送通知的领域逻辑（`pushNotification` + `notifyInsufficientBalance` / `notifySyncTaskCompleted` / `notifySyncTaskFailed` / `notifySubtitleTask` / `notifyCanvasPhaseFailed` / `notifyApiKeyRevoked` / `notifyApiKeyQuota` / `notifyProviderFailure`），逻辑原样搬迁、行为不变；依赖 `@excuse/db` 的 `notifyNotification` + `services/notification-cooldown` 的冷却策略。② **`routes/notifications.ts` 收薄为纯 HTTP CRUD**（列表 / 未读数 / 标记已读 / 全部已读），不再承载业务辅助函数。③ **8 处调用方改从 service 导入**：业务模块 `modules/generation/service.ts`、`modules/subtitle/service.ts`、`services/gateway-service.ts`，路由 `routes/admin.ts`、`routes/canvas.ts`、`routes/generate.ts`、`routes/openai-gateway.ts`，以及 `plugins/auth.ts`（原动态 `import('../routes/notifications')` 仅为规避 route→auth 循环依赖；改指向 service 后循环消失，转静态导入）。④ **顺带修复 `subtitle-service.test.ts` 既有的加载期失败**：原 `modules/subtitle/service.ts` 经 `routes/notifications.ts` 间接拉入 `serialize`，而该测试的 `mock.module('@excuse/db')` 未提供 `serialize` → `SyntaxError: Export named 'serialize' not found`；改指 service 后该导入路径不再触达 `serialize`，535 pass+1 fail → 537 pass 全绿。验收：typecheck / build / test:client(367) / 受影响 server 套件全绿（537 pass），eslint 复核通过。对应 TODO notifications.ts 已删除。

- **删除死代码 `apps/server/src/utils/crypto.ts`（仓库清理，本提交）**：该文件仅 1 行 re-export `export { hashApiKey } from '@excuse/auth'`，且全仓库零 import（`apps/server/src/plugins/auth.ts`、`routes/api-keys.ts` 均已直接从 `@excuse/auth` 导入 `hashApiKey`）。审计建议的「可选内联」实际无需内联——调用方早已直连源，shim 是纯粹死代码，直接删除。typecheck 复核无回归。对应 TODO 仓库清理 crypto.ts 已删除。

- **`.claude/settings.local.json` 退出 git 跟踪（仓库清理，本提交）**：该文件约定为机器本地配置（Claude Code 的 per-machine 权限/路径等），却被 git 跟踪且未忽略，会把个人本地配置混入团队仓库。`.gitignore` 新增**精确**忽略条目 `.claude/settings.local.json`（不整目录忽略 `.claude/`，保留团队共享的 `settings.json` / hooks / skills 未来入库），`git rm --cached` 将该文件移出索引但保留磁盘副本。`git check-ignore` 复核：`settings.local.json` 命中忽略、`settings.json` 仍可被跟踪。对应 TODO 仓库清理 settings.local.json 已删除。

- **序列化函数统一（代码治理 backlog 横切面 #12，本提交）**：消除路由层各自手写的 Date→ISO serialize 函数。① **统一运行时 `serialize<T>()`** `packages/db/src/types.ts`：紧挨既有的 `Serialize<T>` 类型工具新增其运行时对应物，递归把值中的 `Date` 转 ISO 字符串（数组 / 普通对象逐层遍历，`null`/原始类型原样返回，非 Date 的 JSONB 内容结构不变），纯函数无 IO 依赖。② **删除 8 个纯 Date→ISO serialize 函数**（`serializeTag` / `serializeNotification` / `serializeCreditTransaction` / `serializeApiKey`(api-keys) / `serializeUploadedFile` / `serializePipelineRun` / `serializeProject`）+ 清理因此变成未使用的 `*DTO` / `*Row` import，调用点改为 `serialize(row)`，行为与原 `{ ...row, createdAt: row.createdAt.toISOString() }` 完全等价（仅多递归嵌套 Date，JSONB 无 Date 故无害）。③ **保留含派生/重命名的 serializer**：`serializeRecord`（recovery + providerCancelStatus 枚举收窄）改为 `{ ...serialize(record), providerCancelStatus, recovery }`，移除 4 行手写 `.toISOString()`；admin `serializeApiKey`（`totalSpendCents ?? 0` 默认）改为委托 `serialize` 处理日期、保留默认逻辑；canvas `mapper.ts`（JSONB 列重命名）、admin `toHealthSummary`（epoch-ms→ISO + 计算字段）属正当显式映射不动。④ **新增 `packages/db/test/serialize.test.ts`** 纯函数单测 7 条（顶层/嵌套/可空 Date、数组、原始类型、JSONB 透传），无 PG 依赖可独立运行。验收：typecheck / build / test:client(367) / 受影响 8 路由 server 测试全绿；serialize 单测 7/7。对应 TODO 序列化函数统一 已删除。

- **计费入账闭环 (P0-1，本提交)**：补齐计费系统只有扣款没有入账的缺口。① **管理后台充值端点** `POST /api/admin/credit/add`（目标账号 + 金额 + 备注），写 `credit_transactions` type=`credit`，附带 `admin_action` 审计，仅 JWT + ADMIN_USER_IDS 可访问。② **新用户初始额度赠送**：注册成功时自动赠送 1000 cents（幂等，不阻塞注册流程）。③ **Admin 前端充值入口**：用户详情对话框余额卡旁增加「充值」按钮，弹出充值对话框（输入金额 + 备注），充值成功后刷新用户详情和列表。④ **Billing 页面余额与交易流水 (P1-1 合并)**：顶部新增余额卡片（可用/冻结/总计），底部新增交易流水列表（reserve/debit/refund/credit/admin_adjust 按时间倒序，含类型图标、描述、金额变化）。对应 TODO P0-1 / P1-1 已删除。

- **密码重置 (P0-2，本提交)**：新增账号自助恢复能力。① **`password_reset_tokens` 表**（schema + repository）：token 哈希存储、短时效（30 分钟）、一次性使用（原子 `consumePasswordResetToken`）。② **`POST /api/auth/forgot-password`**：接收邮箱，生成 reset token 发送邮件（当前 dev 模式打印到控制台），不暴露「该邮箱是否存在」。③ **`POST /api/auth/reset-password`**：校验 token + 新密码，更新账户密码，写审计日志。④ **邮件服务 `services/email.ts`**：开发环境打印重置链接到控制台，生产环境预留 SMTP 接入点。⑤ **`updateAccountPassword`** 仓库函数。对应 TODO P0-2 已删除。

- **上传文件真实类型校验 + 单用户频次限制 (P1-3，本提交)**：① **Magic bytes 校验**：上传时读取文件头检测真实 MIME 类型（PNG/JPEG/WebP/GIF/MP4/WebM/MOV/AVI），与客户端声明不符则拒绝，DB 存储探测值而非客户端声明。② **单用户上传频次限制**：复用 `@excuse/rate-limit` `SlidingWindowRateLimiter`，per-account 维度每分钟最多 10 次上传（与全局限流分开）。对应 TODO P1-3 已删除。

- **SSE 连接数上限 (P1-4，本提交)**：`UserEventHub` 新增全局上限（默认 10_000）和单用户上限（默认 3），`addConnection` 超限返回 `{ accepted: false, reason }`，SSE 路由据此返回 503 `error` 事件。复用既有 `getConnectionCount()` 判定。对应 TODO P1-4 已删除。

- **nginx 上传体积上限 + HTTPS 终止说明 (P1-2，本提交)**：`nginx.conf` 增加 `client_max_body_size 200m`（与 upload.ts MAX_FILE_SIZE 对齐）；`docs/deployment.md` 增加 TLS 终止位置说明（CDN/LB 终止 vs Nginx 直接终结 vs 裸部署），Nginx 配置参考块增加 `client_max_body_size` 与 HSTS 头。对应 TODO P1-2 已删除。

- **统一错误处理中间件（代码治理 backlog，本提交）**：引入 Elysia `onError` 全局钩子 + 自定义错误类体系，消除 10 个路由中 152 处手写 `set.status` 响应。① **错误类** `apps/server/src/utils/app-errors.ts`：`AppError` 基类（statusCode + message + details）及子类 `BadRequestError`(400)、`UnauthorizedError`(401)、`PaymentRequiredError`(402)、`ForbiddenError`(403)、`NotFoundError`(404)、`ConflictError`(409)、`ValidationError`(422)、`RateLimitError`(429 + Retry-After 头)、`InternalError`(500)、`ServiceUnavailableError`(503)。所有子类有默认消息。② **onError 插件** `apps/server/src/plugins/error-handler.ts`：统一序列化 AppError→JSON、捕获 Elysia 内置 VALIDATION 错误（422 + 提取第一条消息）、未知错误 500 + 日志记录。③ **逐路由替换**（10 个文件、152 处）：所有 `return errorFunc(set, 'msg')` 改为 `throw new ErrorClass('msg')`，移除路由 handler 中不再需要的 `set` 参数。④ **插件注册**：`createRequireAuthPlugin` 和 `createAuthPlugin` 内部加载 errorHandlerPlugin，保证独立路由模块（含测试）也有错误处理；主 `index.ts` 保留全局备份。⑤ **副作用**：`utils/errors.ts` 已删除。客户端 `api-keys.ts`、`notifications.ts` 增加 Eden 响应类型断言。旧 `ModelLab.tsx` 前置 lint/类型错误一并修复。

- **OpenAPI 文档生产环境门禁 (P1-3，本提交)**：生产环境（`NODE_ENV==='production'`）不再挂载 `/openapi` 端点（Elysia openapi 插件条件注册），避免向匿名调用者泄露全部路由形状、schema 与鉴权方案。非生产环境（开发 / 测试）保持可用。类型链保持稳定（`App` type 不因条件分支而分裂），客户端 Eden treaty 不受影响。补 `apps/server/test/index.test.ts` 1 条（非生产环境 GET /openapi → 200）。对应 TODO P1-3 已删除。

- **资产中心复用验收复核 (P2-2，验收通过)**：复核 P2-2 四条验收标准均已满足，唯一待办（标签颜色/重命名/使用计数扩展）是显式条件项（"如果进入产品需求"），`asset_tags` schema 已声明 v1 不做颜色/图标/重命名（删除后重建即可），属延迟决策而非缺口。验收细节：① **找到素材** — Assets 页面聚合 generation_records / canvas_assets / uploaded_files 三来源，含搜索 / source+status+model+日期过滤 / 收藏 / 标签 CRUD（`apps/client/src/pages/Assets.tsx`）；② **回到 Canvas** — `getCanvasAssetUrl`（`apps/client/src/lib/asset-library.ts`）按 projectId + focus 参数生成项目级 / 实体级深链，PreviewModal 提供定位按钮；③ **删除不误删** — `getAssetReferences`（`packages/db/src/repositories/asset-lifecycle.repo.ts`）在软删前 JSONB `@>` 引用守卫 + `retained` 返回 + GC 侧全局复核（err on the side of retaining）；④ **多参考图模型兼容** — `recommendCanvasVideoModel`（`packages/canvas-runtime`）i2v→r2v→t2v 变体回退 + `submitVideoTaskWithFallback`（`packages/provider`）运行时 fallbackModel 回退（r2v→t2v）。对应 TODO P2-2 标记已验收。

- **JSONB 查询索引与性能基线 (P3-1，本提交)**：`generation_records.input_params` 与 `canvas_shots.reference_assets_json` 的 JSONB 过滤/包含查询此前全部走顺序扫描，数据量增长后资产中心、gateway /v1/usage、管理后台任务诊断、资产删除引用守卫会变慢。migration `0033_jsonb_diagnostic_indexes.sql`（drizzle-kit generate 产出，schema `index()` 双处声明同步）补齐：① **btree 表达式索引** `(account_id, input_params->>'source')`（gateway/admin 账户聚合，复合优于单独 source）、`(input_params->>'projectId')`（资产中心 Canvas 项目维度）、`(input_params->>'workerTaskId')` + `(input_params->>'pipelineRunId')`（管理后台失败任务深度诊断精确关联，TODO 点名的关键诊断路径）；② **GIN 索引** `generation_records(input_params)`（referenceFileIds `@>` 包含，uploaded_files 用量 / GC retain）、`canvas_shots(reference_assets_json jsonb_path_ops)`（资产删除引用守卫 `@>` 全表扫描热点；jsonb_path_ops 更小更快，本列查询全是 `@>`）。`requestedModel` 经核查从不进 SQL WHERE（仅 JS 映射），无需索引；`shotId` 仅投影非过滤，无单独索引。新增 `docs/db-indexes.md`：索引策略表 + EXPLAIN 复核方法（含空表 `enable_seqscan=off` 验证索引可用、生产种子数据 EXPLAIN ANALYZE p95 基线）+ 新增 JSONB 查询的索引策略声明要求。已用 `SET enable_seqscan=off` 确认 6 个索引均可被 planner 使用（Index Scan / Bitmap Index Scan）。补 DB 集成测试 123 pass 无回归。对应 TODO P3-1(DB) 已删除。

- **CI 与本地验收同口径 (P4-1，本提交)**：CI 此前只跑 server/worker/provider/billing/shared 五类测试，漏跑 14 个 package，且不跑 build / check:boundaries / test:db / Docker build。① **CI 复用根脚本**：`.github/workflows/ci.yml` 拆为 typecheck / lint / boundaries / build / test / test-db / client-test / docker 八个 job，全部调根 `package.json` 脚本，与 `docs/TODO.md` 验收命令一致。② **全量测试**：`test` job 由分散的 `bun test apps/server/test ...` 改为根 `bun run test`（concurrently 跑 server + worker + 全部 19 个 package），覆盖此前漏跑的 auth / canvas-engine / canvas-runtime / error-recovery / events / gateway / metrics / provider-health / rate-limit / storage / subtitle-engine / ffmpeg / task-engine / workflow-engine / prompt-engine。③ **DB 集成测试进 CI**：新增 `test-db` job 跑 `bun run test:db`（packages/db repo 测试此前从未在 CI 运行）；为此 `packages/db/scripts/test-db.ts` + `test/helpers/test-db.ts` 改为优先读 `DATABASE_URL`（CI postgres service 端口 5432），回落本地 Docker 5433；`terminateStaleTestConnections` 在 CI 模式跳过（CI postgres 每 job 全新、无独立 admin 库）。④ **Docker build job**：`docker` job 用 buildx 验证 server / worker image target 能 build，Dockerfile 变更不再到发布时才暴露。对应 TODO P4-1(CI) 已删除。

- **JSONB task input 运行时校验 + 分类失败 (P3-1，本提交)**：消除 worker 对 `tasks.input` JSONB 的裸 `as` cast，使非法输入在进入业务逻辑前被解析为结构化校验失败（validation / 永久失败，不重试），而非延迟到使用点抛未结构化 TypeError。① **纯函数 parser `@excuse/shared/task-input.ts`**（无 IO）：`parseMediaExtractAudioInput` / `parseMediaBurnSubtitleInput` 返回 `{ ok, input | error }`，覆盖缺字段 / 类型错 / null / 数组 / 多余字段透传；`parseGenerationInputParamsMeta` 安全提取 source/projectId/shotId/requestedModel/referenceFileIds 元字段（避免 `String(undefined)`），多余模型参数不解析（由 provider 层 `validateAndMerge` 声明式校验）。② **task-engine `TaskInputError`**：新错误类，`classifyTaskError` 识别为 `validation` / `retriable=false`（重试不会自愈，直接永久失败）；与既有 `TaskNotImplementedError` 对称。③ **worker 接线**：`media-handlers.ts` 两类 handler 由 `task.input as {...}` 改为 parser + `TaskInputError`；`task-processor.ts` canvasMeta 提取改用 `parseGenerationInputParamsMeta`。路由层运行时校验复核：所有 POST/PATCH route 已用 Elysia TypeBox schema，`generate.ts` 的 parameters bag 由 `validateAndMerge` 声明式校验（非裸信任），Canvas references 已由 canvas-engine zod schema 解析。补 parser 16 条 + task-engine TaskInputError 分类 1 条 + 更新 media-handlers 缺字段断言。对应 TODO P3-1 已删除。

- **生产探针补全 live/ready/db 三类探针 (P4-1，本提交)**：补齐 server/worker 健康探针语义，明确 liveness / readiness / DB 三类探针，使 DB 故障摘流不误杀进程、worker poll 卡死可被发现。① **worker 新增 `/health/live` + `/health/ready`**：`/live` 进程能响应即 200（不依赖 DB，K8s 据此判断是否重启）；`/ready` 当 `lastPollError` 非空（ECONNREFUSED / 表不存在等致命错误，worker 已 break 循环）或 `lastPollAt` 超过 `readyStaleMs`（轮询循环卡死但进程未崩）时返回 503，启动 grace 期内从未 poll 返回 `starting`。`readyStaleMs` 由 worker 按 `pollIntervalMs × 4` 注入，默认 120s。② **server 新增 `/api/health/db`**（仅探测 DB 的聚焦 readiness）+ **`/ready` 增加本地存储可写探测**（`checkStorageWritable`：目录不存在则创建、存在则 `accessSync(W_OK)`；OSS 场景跳过，返回 null）。`createHealthRoutes` 现接收 config 以取 storageRoot/oss。③ **Dockerfile** worker HEALTHCHECK 由综合 `/health`（poll error 也返回 200）改为 `/health/ready`，使容器 health 状态能反映 DB claim 失败。④ **部署文档** `docs/deployment.md` 探针接线章节：区分 liveness（失败重启）/ readiness（失败摘流）语义 + K8s `livenessProbe`/`readinessProbe` manifest 示例 + systemd watchdog / Nginx 主动健康检查建议 + 明确综合端点不适合做 readiness 判断。补 worker health 8 条（live/ready 各场景）+ server index 3 条（live/db/ready）。对应 TODO P4-1 已删除。

- **用户错误恢复体验统一 (P2-3，本提交)**：建立统一失败 → UX action 分类映射，覆盖 Workspace / Canvas / Subtitle / Gateway 各处零散的失败展示。① **纯规则包 `@excuse/error-recovery`**（只依赖 TypeScript，无 IO）：`classifyRecovery(input)` 由 errorMessage / code / category / status / traceId / billingMode / entityId / source，按优先级（cancelled → 结构化码 → category → 文案关键词 → system 兜底）得出 domain（8 个失败领域：balance/content/network/storage/cancel/provider/validation/system）+ action（7 个下一步动作：retry/edit_prompt/change_model/top_up/contact_support/wait/none）+ label/suggestion/recharges/diagnostics/recoverable。新增 gateway 专用错误码（`api_key_quota_exceeded`→balance / `api_key_scope_not_allowed`→validation / `generation_failed`→provider）。100% 测试覆盖 + 31 条用例。② **Canvas 失败分类委托**：`packages/shared/canvas-failure.ts` 内部委托 `classifyRecovery` 并投影 `FailureDomain` 回 7 个 `CanvasFailureKind`（`validation`→`system`），保留公共 API 以避免回归。③ **GenerationRecord DTO recovery 字段**：`GenerationRecord` 新增 `recovery: RecoveryClassification | null`，仅 failed/cancelled 时填充（server `serializeRecord` 调用 `classifyRecovery`）；`OpenAIErrorResponse.error` 新增可选 `hint` 字段，gateway 错误工厂自动从 `classifyRecovery` 填充用户建议。④ **RecordCard 恢复指引 UI**：失败卡片显示领域徽章、下一步建议（蓝色框 + 💡）、重扣费提示（橙色框 + ⚠️）、一键复制诊断信息按钮；`classifyCanvasFailure` 历史测试 100% 通过无回归。⑤ **SSE 客户端本地计算 recovery**：generation store `updateRecordFromSSE` 对 failed/cancelled 事件本地调用 `classifyRecovery` 合入 record，无需改 server/worker SSE 管线。对应 TODO P2-3 已删除。

- **资产生命周期可审计的删除/恢复/GC 策略 (P1-2，本提交)**：定义统一资产状态机 active / hidden / deleted / retained，区分「用户不可见」与「物理删除」，保证删除仍被引用的资产不破坏 Canvas 预览与后续生成。① **状态机**：canvas_assets / generation_records / uploaded_files 三表新增 `deleted_at` 列（migration `0032_asset_lifecycle_deleted_at`，含 GC 扫描索引）；`hidden`（既有 hiddenAt）/ `deleted`（新 deletedAt）/ `retained`（deleted 但仍被引用，GC 不清除）/ `active` 四态。资产中心三来源 list 查询统一排除 `deleted_at IS NOT NULL`（软删除永不进列表）。② **统一删除端点** `DELETE /api/assets/:source/:id`（source ∈ generation_record / canvas_asset / uploaded_file）：删除前调用 `getAssetReferences` 引用守卫——canvas_asset 检查 `canvas_shots.referenceAssetsJson @>` JSONB 引用与是否为 `isActive` 当前版本；uploaded_file 复用既有 subtitle_projects.videoFileId + generation_records.referenceFileIds 计数并新增 shots 引用计数；generation_record 视为叶子产物不破坏引用。任一引用 > 0 → 软删除标记 `retained=true`（GC 不物理清除其存储文件，Canvas 预览不破裂）；queued/running 的 canvas_asset 拒绝删除（409）。③ **恢复** `POST /api/assets/:source/:id/restore`（清 deletedAt，GC 物理清除前可恢复）+ `POST /api/assets/:source/:id/unhide`（清 hiddenAt，恢复误隐藏资产，接通既有但无路由的 unhide 仓库函数）。④ **retention GC** `apps/server/src/services/asset-retention.ts` `runAssetRetentionCleanup({ dryRun, graceDays })`：扫描 deletedAt 超过 grace（默认 30 天）的资产，对未引用者 `AssetStorage.deleteFile` 物理清除存储 + hard delete DB 行（generation_records 无存储仅删行）；GC 走全局 SQL 引用复核（不限定 accountId，retained 是安全方向）。dry-run 仅扫描与计数、不删除、不写审计；真实执行写 admin_action 审计（含各来源清除数与 retained 数）。新增 `POST /api/admin/asset-retention/run`（dryRun/graceDays query）。补 db repo 真实 PG 集成 9 条（引用守卫 shots/isActive、软删除/恢复、跨用户拒绝、retention 候选 + retained 复核 + 物理清除）+ server assets 路由 8 条（retained/未引用删除、引用 retained、生成中拒绝、404、restore、unhide）。对应 TODO 已删除。

- **Provider 连续失败自动降级策略 (P1-1，本提交)**：新增断路器降级，Provider 模型连续失败达到阈值时进入降级冷却窗口，新任务在窗口内快速失败（抛 `ModelDegradedError`），避免用户在已知不可用的模型上空等长视频提交。① **纯策略包 `@excuse/provider-health`**（只依赖 `@excuse/shared`）：`applyProviderOutcome` 在「当前健康状态 + 一次调用结果」上纯函数计算新状态与跳变，含半开（half-open）语义——冷却过期后允许一次真实探测，成功立即恢复，失败沿用累计计数重新降级；冷却期内的重复失败不延长窗口；`isDegraded` / `degradedRemainingMs` 给 guard/admin/metrics 判定；`resolveDegradationConfig` 从 `PROVIDER_DEGRADATION_FAILURE_THRESHOLD` / `PROVIDER_DEGRADATION_COOLDOWN_MS` 读配置（默认连续 3 次降级、冷却 2 分钟）。② **跨进程持久化**：`provider_model_health` 表（migration `0031_provider_model_health`，按 model 唯一，记录 consecutive/total 失败成功计数 + degradedUntil 冷却截止）+ repository `recordProviderOutcome` 用 drizzle `db.transaction()` + `SELECT ... FOR UPDATE` 串行化同一 model 的并发更新（server + worker 共享），写值由纯策略决定；`restoreProviderModelHealth` 供管理员强制恢复；`listProviderModelHealth` / `getProviderModelHealthMap` 供 admin/metrics。③ **Guard 注入**：`@excuse/provider` 新增全局 `registerProviderCallGuard` registry + `ModelDegradedError`（`code='MODEL_DEGRADED'`），`DashScopeClient`（chat/image/video/stream）与 `ASRClient` 在发起真实调用前跑 guard；`submitVideoTaskWithFallback` 捕获主模型 `ModelDegradedError` 后切 fallback 模型（与「切 fallback」语义对齐）。④ **task-engine** `extractErrorCode` 同时识别 error 自身的 `code`，`MODEL_DEGRADED` 分类为可重试 `provider_error`——在途任务冷却过期后重试有机会恢复，而非永久失败。⑤ **server/worker 接线**：各自 `services/provider-health.ts`（平行）维护进程内健康缓存（3s TTL，启动 warm），注册 guard + 在既有 provider observer 上追加 `recordProviderCallOutcome`；server `services/metrics.ts` 移除被取代的进程内连续失败计数。⑥ **可见性**：admin `GET /api/admin/providers` 每条模型附带 `health` 摘要（status/blocking/连续失败计数/冷却剩余秒数），新增 `GET /api/admin/provider-health`（列表）与 `POST /api/admin/provider-health/:model/restore`（管理员手动恢复 + admin_action 审计）；Prometheus 新增 `excuse_provider_model_degraded{model}`（仅输出当前阻断模型）与 `excuse_provider_consecutive_failures{model}`，由 server `/metrics` 从 DB 派生输出（worker 不重复输出避免 federation 双计）。补 `@excuse/provider-health` 纯策略 14 条、task-engine MODEL_DEGRADED 分类、metrics `aggregateProviderHealthMetrics`、db repo 真实 PG 集成 7 条、admin 路由 provider-health 端点 7 条。附带修复预先存在的 `@excuse/workflow-engine` 未声明 `@excuse/task-engine` 依赖（导致 `bun run typecheck` 报 Cannot find module）。对应 TODO 已删除。

- **Provider 连续失败自动降级策略 (P1-1，本提交)**：新增断路器降级，Provider 模型连续失败达到阈值时进入降级冷却窗口，新任务在窗口内快速失败（抛 `ModelDegradedError`），避免用户在已知不可用的模型上空等长视频提交。① **纯策略包 `@excuse/provider-health`**（只依赖 `@excuse/shared`）：`applyProviderOutcome` 在「当前健康状态 + 一次调用结果」上纯函数计算新状态与跳变，含半开（half-open）语义——冷却过期后允许一次真实探测，成功立即恢复，失败沿用累计计数重新降级；冷却期内的重复失败不延长窗口；`isDegraded` / `degradedRemainingMs` 给 guard/admin/metrics 判定；`resolveDegradationConfig` 从 `PROVIDER_DEGRADATION_FAILURE_THRESHOLD` / `PROVIDER_DEGRADATION_COOLDOWN_MS` 读配置（默认连续 3 次降级、冷却 2 分钟）。② **跨进程持久化**：`provider_model_health` 表（migration `0031_provider_model_health`，按 model 唯一，记录 consecutive/total 失败成功计数 + degradedUntil 冷却截止）+ repository `recordProviderOutcome` 用 drizzle `db.transaction()` + `SELECT ... FOR UPDATE` 串行化同一 model 的并发更新（server + worker 共享），写值由纯策略决定；`restoreProviderModelHealth` 供管理员强制恢复；`listProviderModelHealth` / `getProviderModelHealthMap` 供 admin/metrics。③ **Guard 注入**：`@excuse/provider` 新增全局 `registerProviderCallGuard` registry + `ModelDegradedError`（`code='MODEL_DEGRADED'`），`DashScopeClient`（chat/image/video/stream）与 `ASRClient` 在发起真实调用前跑 guard；`submitVideoTaskWithFallback` 捕获主模型 `ModelDegradedError` 后切 fallback 模型（与「切 fallback」语义对齐）。④ **task-engine** `extractErrorCode` 同时识别 error 自身的 `code`，`MODEL_DEGRADED` 分类为可重试 `provider_error`——在途任务冷却过期后重试有机会恢复，而非永久失败。⑤ **server/worker 接线**：各自 `services/provider-health.ts`（平行）维护进程内健康缓存（3s TTL，启动 warm），注册 guard + 在既有 provider observer 上追加 `recordProviderCallOutcome`；server `services/metrics.ts` 移除被取代的进程内连续失败计数。⑥ **可见性**：admin `GET /api/admin/providers` 每条模型附带 `health` 摘要（status/blocking/连续失败计数/冷却剩余秒数），新增 `GET /api/admin/provider-health`（列表）与 `POST /api/admin/provider-health/:model/restore`（管理员手动恢复 + admin_action 审计）；Prometheus 新增 `excuse_provider_model_degraded{model}`（仅输出当前阻断模型）与 `excuse_provider_consecutive_failures{model}`，由 server `/metrics` 从 DB 派生输出（worker 不重复输出避免 federation 双计）。补 `@excuse/provider-health` 纯策略 14 条、task-engine MODEL_DEGRADED 分类、metrics `aggregateProviderHealthMetrics`、db repo 真实 PG 集成 7 条、admin 路由 provider-health 端点 7 条。附带修复预先存在的 `@excuse/workflow-engine` 未声明 `@excuse/task-engine` 依赖（导致 `bun run typecheck` 报 Cannot find module）。对应 TODO 已删除。

- **统一任务队列优先级策略 (P1-2 部分完成，本提交)**：`@excuse/task-engine` 新增纯函数 `getTaskPriority()`，明确 `claimNextTask` 的 priority 口径：字幕媒体任务（`media.extract-audio` / `media.burn-subtitle`）优先级 3，普通 Canvas 自动阶段优先级 5，批量长视频阶段 `canvas.videos` 优先级 6，避免长视频批量任务挤占用户等待的小任务。Canvas 手动阶段、workflow 自动推进、Subtitle 创建/导出任务全部改用统一策略；补 task-engine 策略测试与 workflow `videos` 优先级测试。TODO 已收窄为剩余 provider 自动降级策略。

- **Provider 取消状态可见化 (P1-1，本提交)**：`generation_records` 新增 `cancel_requested_at` 与 `provider_cancel_status`（migration `0030_steady_cancel_visibility`），取消路径会记录 `no_task` / `succeeded` / `failed`，即使 provider 取消失败也保留本地取消和退款语义；`GenerationRecord` DTO 与序列化补出 `cancelRequestedAt`、`providerCancelStatus`。Workspace 生成记录卡片在 cancelled 状态下展示“本地已取消 / provider 已确认取消 / provider 取消失败，后续结果会被忽略”等提示。补 server cancel route 测试覆盖 provider 取消成功、无 provider task、provider 取消失败三类路径。对应 TODO 已删除。

- **Canvas videos worker 提交幂等 (P1-1 部分完成，本提交)**：`canvas_assets` repository 新增 `findReusableCanvasAssetForPipelineTarget()`，worker 执行 Canvas `videos` phase 时按 `pipelineRunId + shotId + category` 复用已有非失败 `shotVideo` asset；如果既有 asset 已绑定 provider task、shot 已有 `videoTaskId` 或资产已成功，则跳过再次调用 provider，避免 worker 崩溃重试时为同一 run/shot 生成重复 Canvas asset 或重复提交已绑定任务。若已有 queued asset 但尚未绑定 provider task，则复用同一 asset 继续提交。补 `apps/worker/test/canvas-videos.test.ts` 覆盖已绑定跳过、queued 复用、新建提交三条路径；TODO 已收窄为剩余 provider 取消状态可见化。

- **生产数据迁移和资产一致性门禁 (P0-4，本提交)**：新增只读 `bun run check:assets` 脚本，扫描 `generation_records` / `canvas_assets` / `uploaded_files` 与本地 `STORAGE_ROOT` 的一致性，报告 `missing_file`、`dangling_file`、`hidden_but_referenced`，并支持 `--json`、`--fail-on-issues`、`--storage-root`；纯函数覆盖 URL 提取、本地路径映射、shot 引用收集和综合报告。`docs/deployment.md` 补生产 migration-only 发布顺序：发布前 `pg_dump` 备份、只运行 `db:migrate`、新进程启动前跑资产检查、失败时停止发布或按备份 `pg_restore` 回滚，并明确高风险 schema 变更使用 expand/migrate/contract。对应 TODO 已删除。

- **Credit 正式计费策略闭环 (P0-3，本提交)**：新增 `@excuse/billing` 纯计费策略模块，声明 `workspace.generate` 与 `openai.gateway.chat` 为正式 `credit-ledger` surface，统一要求 `reserve -> debit | refund`、`generation_records` 与 `usage_events` 关联；Canvas 前置流水线明确为 `beta/free quota`，Subtitle ASR 明确为 `cost-only`，避免误判为已进入用户余额扣款。普通生成、Gateway non-stream 编排器和 Gateway stream 路径接入 `assertCreditLedgerPolicy`，新增策略单元测试与 `docs/billing.md` 资金状态机文档。对应 TODO 已删除。

- **生产安全基线门禁 (P0-2，本提交)**：server 新增可单测的 `validateProductionConfig()`，`NODE_ENV=production` 下强制 `DATABASE_URL` / `DASHSCOPE_API_KEY` / `FRONTEND_URL` / `JWT_SECRET` 显式配置，拒绝开发默认 JWT 和短密钥；server/worker 在 metrics CIDR 公开到 `0.0.0.0/0`、`::/0` 或 `*` 时强制要求 `METRICS_ACCESS_TOKEN`。认证层在 JWT/API Key 成功解析后查询账号并拒绝不存在或已禁用用户；管理后台仅允许 JWT 会话且 account.id 位于 `ADMIN_USER_IDS`，API Key 即使属于管理员也被拒绝。补 server/worker 配置测试、auth-plugin 活跃/禁用账号测试、admin API Key/禁用管理员拒绝测试；`.env.example` 与 `docs/deployment.md` 同步生产安全基线、上传限制和反向代理建议。对应 TODO 已删除。

- **生产部署产物可靠化 (P0-1，本提交)**：`Dockerfile` 改为 build/runtime 分离并提供 `server` / `worker` / `client` 三个 target；build 阶段安装完整依赖并构建前端，server runtime 只安装生产依赖，worker runtime 额外内置 FFmpeg，启动命令统一为 `bun --env-file .env apps/{server,worker}/src/index.ts`，并补容器 healthcheck。新增 `.dockerignore` 避免 `.env`、上传文件、缓存、CodeGraph 索引进入镜像；`docker-compose.yml` 保留默认开发 Postgres，同时新增 `prod` profile 编排 server、worker、Nginx client、共享 uploads volume 和 Postgres healthcheck；`docker-compose.prod.yml` 同步为主 compose 的生产覆盖文件；`docs/deployment.md` 同步 Docker target、`docker compose --profile prod up --build`、健康检查与生产命令。对应 TODO 已删除。

- **Canvas generation record 诊断元数据**：Canvas worker 提交镜头视频时，将 `workerTaskId` / `pipelineRunId` / `canvasAssetId` 写入 `generation_records.inputParams`，管理后台任务详情优先按这些 JSONB 元数据精确关联生成记录（`worker-task` / `pipeline-run`），仅在缺失诊断元数据时回退到原 `accountId + 时间窗口` 候选。Admin UI 同步区分「统一任务」「Pipeline」「候选·时间窗口」三类来源；补 DB repo 测试和 canvas-runtime 副作用测试。

- **Legacy provider task 诊断标识**：`generate` 域旧视频记录仍使用 `generation_records.taskId` 保存 provider task id，本轮不强行迁入统一 `tasks` 队列，先在管理后台用户详情的最近生成记录中显式暴露 `providerTaskId`，并按 `inline` / `legacy-provider-task` / `canvas-worker` / `gateway` 标识执行来源，避免运营把 provider task id 误判为统一 task id；补 DB repo executionKind 矩阵测试。

- **Canvas 参考资产复用提示增强**：镜头参考资产区显示当前已使用数量；手动 URL 添加复用统一 merge 逻辑，按 URL 阻止重复添加；批量应用预览显示每个目标镜头的新增、去重、截断数量，让用户在提交前看到哪些参考图会被实际写入。补 `shot-reference-assets` 客户端测试覆盖重复 URL 和批量预览去重提示。

- **Package 边界治理第一阶段**：`@excuse/shared` 恢复为 base layer，跨包 domain types 迁移到 `packages/shared/src/domain-types.ts`，`packages/db/src/domain-types.ts` 保留兼容 type barrel；shared DTO 不再从 `@excuse/db` / `@excuse/subtitle-engine` 派生类型，字幕 ASS 测试迁移到 `packages/subtitle-engine`。新增 `bun run check:boundaries` 与脚本级测试，自动拦截 shared / pure packages 对 DB、provider、runtime packages 或 apps 的 forbidden import。

- **管理后台取消任务跨业务级联 generation_record (P3.2)**：修复「跨业务状态联动」中可安全落地的取消子集（commit: `3c83220`）。此前 `cancelAdminTask` 只更新 `tasks` 行，queued 态取消任务后关联的 `generation_records` 长期滞留 `processing`（worker 永不运行）—— 真实悬挂态 bug。`packages/db/src/repositories/generation-records.repo.ts` 新增 `cancelGenerationRecordIfActive(id, msg='管理员取消任务')`：仅当记录处于非终态（`pending/submitting/processing/saving_output`）时置 `cancelled` 并返回是否实际取消；`packages/db/src/repositories/admin.repo.ts` `cancelAdminTask` 取消任务后若 `task.generationRecordId` 非空则 best-effort 级联调用（`.catch` 静默降级，任务已取消不受级联失败影响）。**active-state guard 解决 running 态竞态**：running 态取消任务时 worker 可能已完成并 `markGenerationSucceeded`，此时记录已 `succeeded`，级联跳过、不覆盖成功产物（与无 guard 的 `cancelGenerationRecord` 区别正在于此）。补 `packages/db/test/admin-task-mutations.repo.test.ts` 4 条真实 PG 用例（queued 取消级联置 cancelled / succeeded 不被覆盖 / 无 generationRecordId 不报错 / 已终态任务返回 null）。

- **管理后台重排任务跨业务级联 generation_record (P3.2)**：`cancelGenerationRecordIfActive` 的对偶，收口「跨业务状态联动」取消/重排对称设计（commit: `8758940`）。`packages/db/src/repositories/generation-records.repo.ts` 新增 `requeueGenerationRecordIfRequeueable(id)`：仅当记录处于终态 `failed/cancelled` 时重置为 `pending`（清 errorMessage / 递增 retryCount / 清 dedupeKey）并返回是否实际重置；`packages/db/src/repositories/admin.repo.ts` `requeueAdminTask` 重排任务（failed/retrying/queued → queued）后若 `task.generationRecordId` 非空则 best-effort 级联调用（`.catch` 静默降级）。**对偶 guard 设计**：取消级联只动 active 记录（`IfActive`），重排级联只动终态记录（`IfRequeueable`）——active（processing 等）与 succeeded 记录一律不动，避免把活跃记录回退为 pending 或覆盖成功产物；requeue 后 worker 重跑会自然推到终态，此处仅为重跑窗口内 UI 一致性（使记录反映"正在重试"而非滞留 prior 终态）。补 `packages/db/test/admin-task-mutations.repo.test.ts` 5 条真实 PG 用例（failed 级联重置 pending + retryCount 递增 / succeeded 不被回退 / processing 活跃态不被回退 / 无 generationRecordId 不报错 / 已 succeeded 任务返回 null）。
- **管理后台任务详情级联 generation record 诊断 (P3.2)**：补齐「失败任务深度诊断 — generation record 级联」（此前因数据模型阻塞列为待补：`tasks.generationRecordId` 字段已声明但代码库无写入路径恒 null，`generation_records.taskId` 是 provider 字符串 ID 非 tasks uuid，两表无法直接 JOIN）。采用两段策略（commit: `dc7983b`）：① **direct** —— `task.generationRecordId` 非空时精确命中（subtitle 烧录导出在 `apps/server/src/routes/subtitle.ts` 回填该字段，建立此前缺失的写入路径）；② **time-window** —— 无直接关联时按 `accountId + 任务执行时间窗口(±2min)` 返回候选（`matchReason='time-window'`），覆盖 canvas 等任务（其 generation_records 由 worker 执行期间创建、无 task 列直接关联），命中 `idx_gen_records_account_created` 索引，limit 10。`packages/shared/src/admin.ts` 新增 `AdminTaskGenerationRecord` + `AdminTaskDetail.generationRecords`；`packages/db/src/repositories/admin.repo.ts` `getAdminTaskDetail` 新增 `fetchTaskGenerationRecords`（direct / time-window 双策略，复用既有 `iso()` 序列化）+ `AdminTaskGenerationRecordRow`；`apps/client/src/pages/Admin.tsx` 任务详情 dialog 新增「关联生成记录」区（model/status/cost/分类/创建时间/错误 + 直接关联/候选·时间窗口 badge，候选记录提示并发误匹配）。补 `packages/db/test/admin-task-detail.repo.test.ts` 4 条真实 PG 用例（direct 命中 / dangling id 容错 / time-window 候选 / 窗口外排除）+ `apps/server/test/admin-routes.test.ts` 透传断言。剩余局限：canvas 阶段任务为 1:N 间接关联、generate 域不经统一 task 队列，无 task 行可级联。
- **Metrics follow-up 收口 (P2.5)**：补齐 P2.5「跨进程聚合」遗留的两条 follow-up。① **ASRClient 接入 provider observer**（commit: `ec4c0dd`）：`packages/provider/src/dashscope-client.ts` 将 `notifyProviderCallObservers` 由 private 提升为包内 export 供 ASRClient 复用（不进 public barrel，仅 `registerProviderCallObserver` 公开）；`packages/provider/src/asr-client.ts` `submitTranscription` 加 `startTime` 计时并在成功/HTTP 非 200/缺 task_id/网络异常四出口经 `notifyProviderCallObservers('paraformer-v2', …)` 上报，与 `DashScopeClient.submitVideoTask` 一致；`queryTask` 轮询不计入，避免廉价轮询稀释 paraformer-v2 真实 latency。② **admin Provider tab 跨进程 latency 聚合**（commit: `0d94b14`）：canvas 全链路 provider 调用发生在 worker，admin「Provider」tab 此前只读 server 进程内 latency，对 worker 发起的调用恒为 null。新增 worker health `GET /provider-calls` JSON 端点（`apps/worker/src/health.ts`，返回原始 `ProviderCallStats` 快照，`evaluateMetricsAccess` 保护，与 `/metrics` 共用访问策略）；`packages/metrics/src/provider-derived.ts` 新增纯函数 `mergeProviderCalls`（计数相加、durations 原始样本拼接 —— 保原始样本以便精确重算跨进程 p50/p95，已聚合的 quantile 无法跨进程合并）；`apps/server/src/config.ts` 新增 `WORKER_METRICS_URL`（env，默认关闭）；新增 `apps/server/src/services/worker-metrics.ts` `fetchWorkerProviderCalls`（best-effort，2s 超时 + 可选 token + 异常/non-2xx/解析失败一律降级为空 map，含 durations 长度裁剪防污染）；`apps/server/src/routes/admin.ts` `/providers` 改为并行抓 server 进程内快照 + worker `/provider-calls`，`mergeProviderCalls` 合并后再算 avg/p50/p95，worker 不可达时仅反映 server（降级不报错）；前端 `apps/client/src/pages/Admin.tsx` Provider tab 说明文案改为「聚合 server + worker 进程内 metrics」；`.env.example` 补 `METRICS_ACCESS_TOKEN` / `WORKER_METRICS_URL`。补 `packages/provider/test/asr-client.test.ts` observer 测试 4 条（成功/HTTP 错/缺 task_id/网络异常）+ `packages/metrics/test/provider-derived.test.ts` `mergeProviderCalls` 5 条（空/并集/计数相加+durations 拼接/跨进程 p95 精确重算/纯函数）+ `apps/worker/test/health.test.ts` `/provider-calls` 6 条（回环 200/无快照空/非回环 403/token 200/错 token 401/非 GET 404）+ `apps/server/test/admin-routes.test.ts` 2 条（worker durations 合并后 p95=50000/avg=9750、worker 不可达仅 server p95=3000）。
- **系统风险通知 (P2.2)**：补齐 Gateway / API Key 系统风险通知闭环，覆盖「API Key 失效」「额度不足」「异常调用」三类系统风险。`packages/db/src/schema/notifications.ts` 新增 `api_key_quota` / `provider_anomaly` 两个 `notification_type` 枚举值（migration 0029）+ `domain-types.ts` `NotificationMeta` 扩展 `keyId` / `model` / `percent` 三个可选字段；`apps/server/src/routes/notifications.ts` 新增 `notifyApiKeyQuota`（单 emitter 覆盖「即将用尽 80%」与「已用尽 100%」两档，已用尽优先，分别 24h / 6h 冷却去重）并将既有但从未被调用的 `notifyProviderFailure` 从 `system` 重定向到 `provider_anomaly`；`apps/server/src/services/notification-cooldown.ts` 新增 `apiKeyQuota` (6h) / `apiKeyQuotaApproaching` (24h) 冷却常量。触发点接线：`apps/server/src/routes/openai-gateway.ts` 额度检查拒绝（429）时 reactive 触发 + 调用成功后按投射用量（snapshot + 本次成本）proactive 触发 80% 预警（stream / 非 stream 两路一致），`apps/server/src/services/gateway-service.ts` provider 抛错 catch 块触发 `provider_anomaly`，`apps/server/src/routes/admin.ts` 管理员撤销 Key 时主动通知 key 所属用户（既有「尝试使用已撤销 key」的被动检测保留）。客户端：`apps/client/src/components/Navbar.tsx` `TYPE_META` 新增 `api_key_quota`（Gauge / orange）+ `provider_anomaly`（AlertTriangle / red）图标，`apps/client/src/lib/notification-target.ts` 新增 `api_key_quota → /api-keys`、`provider_anomaly → /developers` 跳转。补 `apps/server/test/notification-emitters.test.ts`（emitter 阈值 / 冷却 / 去重 / 已用尽优先 14 条）+ 扩 `notification-cooldown.test.ts`（新常量）+ 扩 `openai-gateway.test.ts`（provider 失败触发 provider_anomaly、API Key 额度已用尽 429 + api_key_quota 通知、额度即将用尽 80% 预警 3 条，补 `notifyNotification` / quota / spend mock）+ 扩 `apps/client/test/notification-target.test.ts`（新类型路由 2 条）。
- **Gateway 客户管理 (推荐执行顺序 #6)**：补齐 Gateway / API Key 开放实现最后一块管理面 —— 顶层「Gateway 客户」tab + 跨账户聚合列表 + 客户详情（含 Key 配置/重置额度/撤销）+ 审计。后端（commit `dfc9af6`）：`packages/shared/src/admin.ts` 新增 `AdminGatewayClientItem` / `AdminGatewayClientListQuery` / `AdminGatewayClientListResponse` / `AdminGatewayClientSummary` / `AdminGatewayRecentRecord` / `AdminGatewayClientDetail` / `AdminGatewayClientDetailResponse` DTO；`packages/db/src/repositories/admin.repo.ts` 新增 `listAdminGatewayClients`（`accounts JOIN api_keys` 聚合，活跃/总 key、Key 消耗、额度上限、最近活动，任一 key 无限额则 `totalQuotaCents=null`，search + limit clamp 1~100 + offset 分页）与 `getAdminGatewayClientDetail`（`Promise.all` 并行 summary + 复用 `listAdminApiKeysByAccount` + 复用 `listGatewayUsageRecords` 映射最近 50 条）；`packages/db/src/repositories/api-keys.repo.ts` 新增 `revokeApiKeyAdmin`（去 owner 校验，已撤销/不存在返回 null）；`apps/server/src/routes/admin.ts` 新增 `GET /gateway-clients` / `GET /gateway-clients/:accountId` / `POST /api-keys/:id/reset-quota`（复用 `resetApiKeySpend`）/ `POST /api-keys/:id/revoke`，并给既有 `PATCH /api-keys/:id/config` 补 `admin_action` / `api_key_revoke` 审计（test 环境自动禁用）。前端（本次）：`apps/client/src/api/admin.ts` 新增 `fetchAdminGatewayClients` / `fetchAdminGatewayClientDetail` / `resetApiKeyQuota` / `revokeApiKeyAdmin`（POST 命名避开用户侧 `revokeApiKey` 冲突）+ `adminGatewayClientsQueryKeys`，复用既有 `fetchAdminUpdateApiKeyConfig`；`apps/client/src/pages/Admin.tsx` 新增 `'gateway'` tab + `AdminGatewayClientsTab`（搜索 + 分页 + 点击行打开详情，镜像 `AdminUsersTab`）+ `AdminGatewayClientDetailDialog`（账户摘要 + 可管理 keys 表 + 底部最近 Gateway 调用记录 + rate-limit 单进程说明）+ `AdminGatewayKeysTable`（每行「配置 / 重置额度 / 撤销」，已撤销 key 只显示状态）+ `AdminApiKeyConfigDialog`（scope Select + rateLimitPerMinute/quotaMaxCents Input，元↔分换算，submit 调 `fetchAdminUpdateApiKeyConfig`）+ 单个 `AlertDialog` 统一确认 reset/revoke（`useMutation` + 成功后 invalidate `adminGatewayClientsQueryKeys`）；用户 tab 既有的只读 `AdminUserApiKeysSection` 保持不动。附带修复 `apps/client/src/pages/Admin.tsx` 多处「全部」筛选项用空字符串 value 喂给 `@radix-ui/react-select@2.3.0`（禁止空 value）导致整页渲染崩溃的 pre-existing bug：改为 `value: 'all'` sentinel 并在消费处映射回 `undefined`（`TASK_STATUS_OPTIONS` / `TASK_DOMAIN_OPTIONS` / `USER_STATUS_OPTIONS` / 项目状态 / 审计操作五个 Select）；补 `apps/client/test/admin-page.test.tsx` 缺失的 `fetchAdminUserApiKeys` / `adminUserApiKeysQueryKeys` mock（此前被上述崩溃掩盖）+ provider windowHours 测试改用 Radix combobox/option 交互（替代 native `selectOptions`）+ 3 条 Gateway tab 测试（列表渲染、详情 dialog、撤销走确认弹窗）。纯增量，无 schema migration（`api_keys` 列已齐全），不引新依赖。
- **Metrics 跨进程聚合 (P2.5)**：worker 进程此前不在 `excuse_provider_calls_total` 中，canvas 视频/图片/文本生成等异步 provider 调用对 Prometheus 不可见。本轮让 worker 也成为可抓取 target —— 新增 `packages/metrics/src/access.ts`（`isAllowedIp` 从 server route 搬迁 + 新增纯函数 `evaluateMetricsAccess`，封装 IP/token 访问策略）+ `packages/metrics/src/worker-derived.ts`（`aggregateWorkerMetrics`，输出 8 个 `excuse_worker_*` family：uptime/polling/busy/tasks_claimed_total/tasks_processed_total/orphan_sweeps_total/last_poll_ok/last_poll_timestamp_seconds）；新增 `apps/worker/src/services/metrics.ts`（worker 本地 `MetricsCollector` + `recordProviderCall`/`getProviderCallsSnapshot`）；`apps/worker/src/index.ts` 启动时 `registerProviderCallObserver` 接入 DashScopeClient 全链路；`apps/worker/src/health.ts` 抽出纯函数 `handleHealthRequest` 并新增 `/metrics` 分支（复用 `aggregateProviderMetrics` + `aggregateWorkerMetrics` + `serializePrometheus`），`createHealthServer` 第三参数注入 `providerCallsSnapshot`/访问策略；`apps/worker/src/config.ts` 新增 `metricsAccessToken`/`metricsAllowedCidrs`（读 `METRICS_ACCESS_TOKEN`/`METRICS_ALLOWED_CIDRS`，默认回环）。server 侧 `apps/server/src/routes/metrics.ts` 改用 `evaluateMetricsAccess`（行为不变，访问策略下沉到包共享）。跨进程聚合靠 Prometheus 多 target 抓取（server:5007 + worker:5100），由自动 `instance` label 区分，`sum by (model)(...)` 自然聚合。补 `packages/metrics/test/access.test.ts` + `worker-derived.test.ts` + `apps/worker/test/health.test.ts`（`handleHealthRequest` 直调，覆盖 /health 不变、/metrics Prometheus 输出、访问策略 200/403/401/www-authenticate、注入 provider 快照、注入 now 断言 uptime）。
- **媒体处理任务化 (P4.2)**：将字幕管线的耗时媒体操作迁移到统一 task 队列。新增 `apps/worker/src/media-handlers.ts` 提供 `handleMediaExtractAudio` + `handleMediaBurnSubtitle` 两个 task handler；`apps/server/src/modules/subtitle/service.ts` 重构 — 创建项目时不再同步调用 ffmpeg 提取音频（曾阻塞 HTTP 请求数秒），改为创建 `media.extract-audio` task 后立即返回；`POST /api/subtitle/projects/:id/export` 路由创建 `media.burn-subtitle` task 替代原独立轮询；`apps/worker/src/task-handler.ts` 注册两个新 task 类型；`apps/worker/src/subtitle-processor.ts` 移除 `processExportTask`；`apps/worker/src/index.ts` 移除 `pollExportingProjects` 独立轮询循环。所有媒体操作现在都通过统一 task queue 调度，享有 retry/heartbeat/orphan-sweep/Admin 可见性。新增 `apps/worker/test/media-handlers.test.ts` 6 条 handler 测试。

### Fixed

- **客户端测试套件修复（10 → 0 失败，commit: `77604d1`）**：消除 `bun run test:client` 既存的 10 条红色失败（此前多轮被当作「pre-existing 基线」跳过，实为可自主修复的测试 bug）。① `apps/client/test/developers-page.test.tsx`：`Developers.tsx` 新增 `UsageSection`（`useQuery` 拉 `GET /v1/usage`）后，测试未包 `QueryClientProvider`，9 条用例渲染即抛 `No QueryClient set` —— 补 `QueryClientProvider` 包装（`retry: false`）+ `vi.stubGlobal('fetch')` mock 用量返回（测试只断言静态页面内容）；崩溃解除后暴露 2 条与实现漂移的断言并校正：`qwen-plus` 在定价表与多处 action 文案重复出现改 `getAllByText`（与既有 `qwen-max` 同），`stream_not_supported` action hint 由旧文案「关闭 stream 参数」更新为当前实现的「部分旧模型不支持 stream」（错误响应板块重构后文案变更）。② `apps/client/test/client-api.test.ts`：错误地从 `bun:test` 导入 `describe/expect/it`，而客户端套件经 vitest 运行，vite 无法 bundle `bun:test` 内建模块致整个 suite 加载失败（0 test）—— 改为 `from 'vitest'`（与其它 23 个客户端测试一致）。修后全量客户端套件 **360 pass / 0 fail**（24 files）。

### Changed

- **客户端相对时间格式去重 (P4 治理，commit: `77604d1` 起的后续)**：`apps/client/src/components/Navbar.tsx`（通知时间）与 `apps/client/src/pages/ApiKeys.tsx`（Key 最近使用）各自维护一份字节相同的 `formatRelativeTime`（刚刚 / N 分钟前 / N 小时前 / N 天前），违反 P4「减少手写通用能力维护成本」原则。抽出到 `apps/client/src/lib/format-time.ts`（`now` 参数注入便于确定性测试），两处改用共享 helper（ApiKeys 的 null → 「从未使用」分支内联到调用点，行为不变）；`generation-utils.formatTime` 是不同的「相对 + 日期后缀」格式未动。新增 `apps/client/test/format-time.test.ts` 5 条 bucket 边界用例。未引入 date-fns（TODO P4 仍判「不急」，内部去重已消除维护成本，不引入重依赖）。
- **移除 P3.2「长列表使用 @tanstack 虚拟化」建议（评估后判定为过早）**：该建议原担心管理后台手写复杂表格状态。调研结论 —— 管理后台所有表（tasks/users/projects/audit-logs/gateway-clients/api-keys）均服务端分页 ≤50 行，筛选/排序已全部经 query param 下推服务端（正是该库旨在替代的反模式，代码库已在对的一侧），admin DOM 单页最大 ~50 行文本行，虚拟化在此无收益；且 `@tanstack/table-core`（表头/排序/选择状态）与 `@tanstack/react-virtual`（DOM 窗口化）是两类不同能力，原文把它们混为一谈属分类错误。唯一真实候选是 P1 资产中心 Assets 网格（`apps/client/src/pages/Assets.tsx` 200 行初拉 +「加载更多」无上限追加，DOM 可累积），但属未测量的「不急」项（仅当用户常加载 >500 卡时才值得），按 TODO 治理规则「只记录仍需推进的事项」不入列。故从 `docs/TODO.md` P3.2 移除该建议；如未来 Assets 网格确有压力，单独引 `@tanstack/react-virtual` 或将 load-more 改为替换式真分页。
- Gateway API Key scope/quota/rate-limit 全栈实现：`api_keys` 表新增 `scope`/`rate_limit_per_minute`/`quota_max_cents`/`total_spend_cents`/`quota_reset_at` 列 + Drizzle migration；`packages/db` API Key 仓库新增 `incrementApiKeySpend`/`checkAndResetApiKeyQuota`/`isApiKeyQuotaExceeded`/`updateApiKeyConfig`；`apps/server` auth 插件新增 per-key 滑动窗口限流 + `apiKeyMeta` 上下文注入；Gateway 路由新增 scope 门禁（'all'|'gateway'）+ 额度检查 + 调用后追踪 spend；管理后台新增 `PATCH /api/admin/api-keys/:id/config` 配置端点和 Admin UI 展示 scope/限流/额度列；`packages/gateway` 新增 `apiKeyScopeNotAllowedError`/`apiKeyQuotaExceededError` 错误工厂；`packages/shared` API Key DTO 新增 scope/quota/rate-limit 字段；用户端 API Keys 页面新增 scope 选择创建 + 列表展示 scope/限流/额度信息。
- canvas-runtime phases + `regenerate.ts` 完成 zod schema 迁移：`@excuse/canvas-engine` 导出 `novelAnalysisSchema` / `characterProfileSchema` / `locationProfileSchema` / `shotDraftsSchema` 四个 zod schema，`canvas-runtime/src/phases/*.ts`（analysis / characters / locations / storyboard）从两步 `validateX(parseLLMJson(...))` 模式改为单次 `parseLLMJsonWithSchema(text, schema)` 调用，`apps/server/src/modules/canvas/regenerate.ts` 同步迁移（commit: `e8c47f7`）。这是 `e797419` / `600e0cf` 两轮 zod 迁移遗留的独立任务，至此 canvas-runtime 全部四阶段 + server regenerate 均已使用 zod schema 进行 LLM 输出校验。
- 修复 `scripts/scan-en.mjs` 中 `node:` protocol、brace-style、prefer-template 等 6 处 lint 错误，修复 `packages/shared/test/canvas-failure.test.ts` import 排序 lint 错误（commit: `cc825bb`）。
- `docs/metrics.md` 新增线上排障检查清单：覆盖服务存活 / DB 可用 / Worker 状态 / 任务积压 / Provider 异常 / Canvas 阶段耗时 / HTTP 概览 7 个场景的操作命令与判断标准（commit: `0df806f`）。
- 管理后台项目细粒度检索：`@excuse/shared` 新增 `AdminProjectItem` / `AdminProjectListQuery` / `AdminProjectListResponse` DTO；`packages/db` 新增 `listAdminProjects`（CTE 镜头统计 + 标题 ilike 搜索 + 状态过滤 + 分页）；`apps/server` 新增 `GET /api/admin/projects`（route 层从 `modelPreferencesJson` 提取模型偏好摘要）；`apps/client` Admin 页面新增「项目」tab，含搜索 / 状态筛选 / 项目表格（标题 / 用户 / 状态 / 镜头 / 模型 / 时间）（commit: `b0f5983`）。

### Testing

- 全项目测试描述中文化：将约 90 个测试文件中 ~1100 条英文 `describe`/`it`/`test` 描述翻译为中文，覆盖 `apps/server`、`apps/client`、`apps/worker`、`packages/*` 全部测试文件；保留技术术语（函数名、error code、JSON 字段名等）不翻译。
- 修复 `packages/canvas-engine/test/schema.test.ts` 中 `validateCharacterProfile` 测试的遗漏断言（测试描述中文化过程中 `expect(validateCharacterProfile(valid)).toEqual(valid)` 被误删，已恢复；commit: `e8c47f7`）。
- `apps/server/test/canvas-service-helpers.test.ts` 新增 18 条测试覆盖 `getTextModel` / `getImageModel` / `getVideoModel` / `assertNotGenerating` 四个纯辅助函数（commit: `0e3a1ef`）。
- `apps/client/test/client-api.test.ts` 新增 5 条测试覆盖 `getActivePipelineRun` 状态判断逻辑（commit: `0e3a1ef`）。

### Added

- 管理后台失败任务深度诊断（Canvas pipeline run 级联）：`packages/shared/src/admin.ts` 新增 `AdminPipelineRun` / `AdminTaskDetail` / `AdminTaskDetailResponse` DTO；`packages/db/src/repositories/admin.repo.ts` 新增 `getAdminTaskDetail(taskId)`（并发查单条 task + 关联的 `canvas_pipeline_runs`（`WHERE task_id = $1 ORDER BY created_at ASC`），durationMs 在 repo 层用 `finishedAt - startedAt` 毫秒差计算避免前端时区问题，outputSummary jsonb 透传）；`apps/server/src/routes/admin.ts` 新增 `GET /api/admin/tasks/:id`（admin guard + 不存在 404）；`apps/client/src/api/admin.ts` 新增 `fetchAdminTaskDetail` + `adminTasksQueryKeys.detail`；`apps/client/src/lib/admin-format.ts` 新增 `PIPELINE_PHASE_LABELS` + `pipelinePhaseLabel`（9 阶段中文标签）；`apps/client/src/pages/Admin.tsx` TaskTable 行新增「详情」按钮 + `AdminTaskDetailDialog`（基本信息网格 + errorMessage 全文 + Canvas pipeline run 时间线 phase/status/durationMs/errorMessage + outputSummary 可折叠 + 重排/取消按钮，复用既有 mutation 的 `adminQueryKeys.all` 失效）；generation record 级联因数据模型阻塞（`tasks.generationRecordId` 字段声明但代码库无写入路径恒为 null，`generation_records.taskId` 是 provider 字符串 ID 非 tasks uuid）—— 待后续补写入路径或改用间接关联；补 packages/db admin-task-detail repo 测试 5 条（不存在/null、无关联 pipeline_run、多条按 createdAt asc + durationMs、缺 finishedAt durationMs=null、outputSummary 透传）+ apps/server admin-routes 测试扩 3 条（403/404/200+级联）+ apps/client admin-page 测试扩 3 条（详情 dialog 渲染时间线、空 pipelineRuns 提示、dialog 内重排）（commit: `86ca4b4`）。
- 管理后台运营统计深化（用户级用量 + provider 错误率/成本统计）：`packages/shared/src/admin.ts` 新增 `AdminUserSummary` / `AdminUserDetail` / `AdminProviderStatsItem` 等 DTO 与 response 类型；`packages/db/src/repositories/admin.repo.ts` 新增 `listAdminUsers`（join accounts + credit_accounts + 子查询聚合 generation_records，支持 search/isActive/limit/offset）/ `getAdminUserDetail`（summary + 30 天每日成本 + 模型分解前 10 + 最近 10 条 record）/ `getAdminProviderStats(windowHours)`（repository 层只出 DB 部分：per-model count/succeeded/failed/cost/inputTokens/outputTokens，latency 留给 route 合并）；`apps/server/src/services/metrics.ts` 新增 `getProviderCallsSnapshot()` 最小 export（只读 `metrics.snapshot(0,0).providerCalls`）；`apps/server/src/routes/admin.ts` 新增 `GET /api/admin/users` / `GET /api/admin/users/:id`（不存在 404）/ `GET /api/admin/providers`（route 层 inline `nearestRankPercentile` + `computeLatency` 合并 `getProviderCallsSnapshot()` 出的 in-process durations → `avgLatencyMs` / `p50LatencyMs` / `p95LatencyMs`；windowHours 越界自动 clamp 到 1~720）；`apps/client/src/pages/Admin.tsx` 改造为按钮组 Tabs（概览 / 用户 / Provider），概览保留原有 overview + recent failures + tasks 诊断；用户新增 `AdminUsersTab`（搜索 + 状态过滤 + 分页 + 行点击打开 `AdminUserDetailDialog`，30 天成本趋势用纯 CSS bar 不引图表库）+ `AdminProvidersTab`（windowHours 下拉 + 12 列表格 + 失败率百分比 + avg/p50/p95 延迟）；新建 `apps/client/src/api/admin.ts`（fetchAdminUsers / fetchAdminUserDetail / fetchAdminProviderStats，独立于 client.ts 便于 mock）+ `apps/client/src/lib/admin-format.ts`（formatLatencyMs / formatPercent / formatNumber）；零依赖新增（不引 Tabs / 表格 / 图表库）；补 packages/db admin-users-providers repo 测试 13 条（listAdminUsers 空/单用户/搜索/状态过滤/分页，getAdminUserDetail 不存在/聚合/cap 10，getAdminProviderStats 分组/windowHours 过滤/clamp）+ apps/server admin-routes 测试扩 9 条（users 403/list/404/detail，providers 403/merge/clamp×2/null latency）+ apps/client admin-page 测试扩 5 条（用户 tab 渲染/搜索 debounce/详情 dialog，Provider tab 渲染/windowHours 切换）（commit: `1c5dfeb`）。
- 管理后台只读概览第一版：新增 `ADMIN_USER_IDS` 配置控制 `/api/admin/*` 访问，`packages/db` 新增 `getAdminOverview` 聚合 repository，`packages/shared` 新增 Admin Overview DTO，`apps/server` 新增 `GET /api/admin/overview` 返回用户数、生成记录/失败数、总成本、活跃任务、活跃 Canvas 项目、生成状态分布、Canvas 状态分布、任务队列深度与最近失败摘要；`apps/client` 新增 `/admin` 管理后台页面和导航入口，使用 react-query 30s 刷新并展示概览卡片、状态分布、队列深度与最近失败表；补 server admin route 测试和 client admin page 测试。
- 管理后台新增「审计日志」tab：`packages/shared` 新增 `AdminAuditLogItem` / `AdminAuditLogListQuery` / `AdminAuditLogListResponse` DTO；`packages/db` 新增 `countAuditLogs` 分页计数查询；`apps/server` 新增 `GET /api/admin/audit-logs` 接口（按 action / accountId / 时间范围过滤分页，仅展示不修改）；`apps/client` 新增 `AdminAuditLogsTab` 组件（操作类型下拉 / 用户 ID 搜索 / 日期范围筛选 / 详情 JSON 展开 / 分页），与既有 admin tab 布局一致。
- 管理后台任务运维第一版：`packages/task-engine` 新增 `canRequeueTask` / `canCancelTask` 纯状态守卫；`packages/db` admin repository 新增 `listAdminTasks` / `requeueAdminTask` / `cancelAdminTask`，支持按 status/domain/search 查询 tasks 队列，并对 failed/retrying/queued 执行清锁清错重排、对 queued/running/retrying 执行取消；`apps/server` 新增 `GET /api/admin/tasks`、`POST /api/admin/tasks/:id/requeue`、`POST /api/admin/tasks/:id/cancel`，继续受 `ADMIN_USER_IDS` 限制；`apps/client` `/admin` 新增任务诊断区，支持筛选、搜索、15s 自动刷新、手动刷新、重排和取消操作；补 task-engine、server admin route、client admin page 测试。当前操作只作用于统一任务队列，不级联修复 generation record 或 Canvas pipeline run 状态。
- OpenAI Gateway 用量查询第一版：新增 `GET /v1/usage` 只读接口，返回当前用户最近一段时间的 Gateway 调用聚合摘要（`totalCalls` / `succeededCalls` / `failedCalls` / `totalTokens` / `totalPriceCents`）与最近调用列表（不含 prompt 全文）；Gateway 创建生成记录时写入 `inputParams.source='gateway'` + `requestedModel` 标记，`packages/db` 新增 `listGatewayUsageRecords` repository；`packages/shared` 新增 `OpenAIGatewayUsageItem` / `OpenAIGatewayUsageResponse` DTO（commit: `22cff73`）。
- Canvas 主链路已完成自动执行、实时回显、SSE/polling fallback、阶段状态和失败恢复等关键体验收口（commits: `a2b4c9f`、`e3d6277`、`095d151`、`b123756`、`d73cd15`、`e3dbccb`、`633672c`、`0a79421`、`d783551`、`67f9548`、`cb0fd99`、`2416feb`、`d211790`）。
- 资产中心隐藏策略第一版：生成记录和 Canvas 资产支持软隐藏，资产列表默认排除隐藏项（commit: `1be3ce9`）。
- 参考资产复用链路：镜头参考资产、资产库选择、服务端归属校验、模型变体推荐、批量应用参考资产（commits: `4fb64b3`、`e886876`、`5a3a74f`、`df5ad57`、`c196f66`）。
- API Key 管理入口第一版：列表、创建、secret 只显示一次、复制和撤销确认（commit: `b154a55`）。
- OpenAI Gateway 开发者使用说明入口第一版（commit: `0bcd860`）。
- Model Lab 内部实验页第一版：新增 `/model-lab` 受保护页面与导航入口，基于 provider 暴露的 `ModelConfig.parameters` 动态渲染 `react-hook-form` 参数表单，支持文本 / 图片 / 视频 / 字幕分类切换、模型选择、prompt 与媒体参数输入、参考素材上传、请求 JSON 预览、真实 `generate` 实验提交、同 prompt 多模型对比、结果 / 原始记录 / 成本展示；新增本地 Canvas 默认模型偏好保存能力（text/image/video 模型映射到 `CanvasModelPreferences`，可保存当前模型并应用回 Lab 当前分类）；补 `model-lab-page.test.tsx` + `model-lab-presets.test.ts` 覆盖模型加载、提交参数、多模型对比、默认偏好保存 / 合并 / 应用与错误状态。
- 资产中心、通知和 Billing 已完成 React Query 试点接入（commit: `e44a8f1`）。
- `packages/ffmpeg`、`packages/storage`、`packages/auth`、`packages/rate-limit`、`packages/metrics`、`packages/subtitle-engine` 等通用能力已完成第一批拆包（commits: `65e775b`、`8ac92b3`、`de60178`、`b575959`、`a80936f`、`a269aa6`）。
- 开发者接入页新增「错误响应」板块：OpenAI 错误响应格式示例 + 7 个公开错误码（`model_not_found`、`invalid_model`、`invalid_parameters`、`insufficient_balance`、`generation_failed`、`stream_not_supported`、`missing_user_message`）的 HTTP 状态、含义和处理建议对照表；前端使用页面局部常量，不依赖 `@excuse/gateway`，避免把服务端协议包打入 client bundle。
- `ShotReferenceAssets` 批量应用参考资产新增最近一次撤销能力：批量应用成功后保存受影响镜头的本地快照（仅组件生命周期内有效），参考资产区展示「撤销上次应用」入口；点击后对每个受影响镜头以 `mode: 'replace'` 回写原始 `referenceAssetsJson`，撤销成功后调用 `onUpdate` 刷新并隐藏入口；撤销失败保留快照可重试。快照只覆盖服务端实际 `result.applied` 的镜头，避免被跳过的镜头被错误回滚。
- 资产中心支持编辑上传文件：新增 `PATCH /api/upload/:id`，支持重命名和用途更新，强制 accountId 隔离并写 `audit('file_update', ...)`；Assets 页面 PreviewModal 新增「编辑」入口（仅 uploaded_file 来源显示），编辑成功后刷新列表（commit: `a0e5c4f`）。`auditActionEnum` 追加 `file_update` 值（同时补齐 `asset_hide` 漏写的 migration 0025）。
- 资产中心列表新增排序能力：`GET /api/assets` 支持 `sort` 查询参数（`created_desc` 默认 / `created_asc` / `title_asc` / `title_desc`），合并 generation_records / canvas_assets / uploaded_files 三来源后统一排序，非法值静默回落 created_desc；`packages/shared` 新增 `AssetLibrarySort` 联合类型并在 `AssetLibraryQuery` 追加 `sort` 字段；Assets 页面筛选区新增排序下拉，与既有 URL ↔ state 同步逻辑一致（commit: `dd888e5`）。
- OpenAI Gateway `/v1/chat/completions` 支持流式响应（`stream: true`），第一版仅支持 openai-chat 协议模型（如 `qwen3.7-plus`）；新增 DashScope client `chatCompletionStream` async generator、`packages/gateway` 新增 `createOpenAIStreamChunk` / `serializeOpenAIStreamChunk` / `OPENAI_STREAM_DONE` helper 与 `STREAMING_MODEL_NOT_SUPPORTED` 错误码；`normalizeOpenAIChatRequest` 不再拒绝 stream=true，改为透传 stream 字段；route 流式分支保留 reserve / markSucceeded / debit / refund / audit / ownership 隔离（commit: `a3ab011`）。
- OpenAI Gateway `/v1/chat/completions` 流式响应扩展到 chat 协议模型（qwen-max / qwen-plus / qwen-turbo / qwen-long）；DashScope client `chatCompletionStream` 自动按 `requestType` 分派 OpenAI 兼容 / DashScope 原生 SSE parser；route stream 分支不再限制模型协议，所有文本模型都支持 `stream=true`（commit: `c813e48`）。
- Metrics 指标新增 Prometheus text exposition 格式输出：`packages/metrics` 新增 `serializePrometheus` / `snapshotToPrometheus` 纯函数（counter / gauge 基本类型 + label 字典序 + 值转义）；`apps/server` 新增 `GET /metrics` 端点（无 `/api` 前缀，符合 Prometheus 标准）；访问策略 v1：默认仅允许回环地址（`127.0.0.0/8` + `::1`），配置 `METRICS_ACCESS_TOKEN` 后必须通过 `Authorization: Bearer <token>` 鉴权；`apps/server/src/config.ts` 新增 `metricsAccessToken` + `metricsAllowedCidrs` 配置；指标命名统一加 `excuse_` 前缀（`excuse_http_requests_total` / `excuse_http_latency_seconds` / `excuse_sse_online_users` / `excuse_generation_total` / `excuse_errors_total` / `excuse_uptime_seconds`）；`docs/metrics.md` 补最小化 Prometheus 接入说明（commit: `95540d3`）。
- 资产中心列表新增收藏能力：新建 `asset_favorites` 表（用户级收藏，复合唯一约束 `(accountId, source, assetId)`），migration `0026_fuzzy_baron_zemo.sql`；`GET /api/assets` 支持 `favorite=true` 过滤并在每条 `AssetLibraryItem` 注入 `isFavorite` 字段（一次性查回 favorite key 集合后内存匹配，避免逐条 SQL）；新增 `POST /api/assets/:source/:id/favorite` 与 `DELETE` 两个 toggle 端点（幂等，跨 generation_record / canvas_asset / uploaded_file 三种来源；不进 audit，避免触碰既有 `auditActionEnum`）；Assets 页面卡片新增星标按钮（乐观更新 + 失败回滚 + invalidate query）+ 筛选区新增「仅看收藏」开关，与既有 URL ↔ state 同步逻辑一致（commit: `efeaa11`）。
- Prometheus 指标扩展（DB 派生）：`packages/metrics` 新增纯聚合函数 `aggregateCanvasPhaseMetrics` + `aggregateTaskQueueMetrics`（接 SQL 聚合行 → Prometheus metric family，pure 无 DB 依赖）；`packages/db` 新增 `metrics.repo.ts` 提供 `getCanvasPhaseStats(windowHours=24)`（用 PostgreSQL 原生 `percentile_cont` 算 per-(phase, status) p50/p95/avg duration + count，仅统计 finishedAt IS NOT NULL）+ `getTaskQueueStats()`（per-(domain, status) 即时计数）；`apps/server/src/routes/metrics.ts` `/metrics` 端点并发查两个新 repository 并合并 in-memory snapshot + DB-derived family 输出，新增 metric family `excuse_canvas_phase_total{phase,status}` / `excuse_canvas_phase_duration_seconds{phase,quantile}` / `excuse_task_queue_depth{domain,status}`；DB 查询异常兜底空数组，不阻塞 in-memory 输出；补 packages/metrics db-derived 单元测试（空输入 / succeeded-only duration / 单位转换 / 类型守卫）+ apps/server metrics DB-derived mock 测试（commit: `30c5d41`）。
- Prometheus 指标扩展（provider 错误率 + 模型耗时）：`packages/metrics` 的 `MetricsCollector` 新增 `recordProviderCall(model, durationMs, success)` in-process 计数方法，`MetricsSnapshot` 新增 `providerCalls` 字段（keyed by model，durations 数组限 1000 样本 FIFO 截断）；新建 `packages/metrics/src/provider-derived.ts` 提供纯聚合函数 `aggregateProviderMetrics`（接 providerCalls → Prometheus metric family `excuse_provider_calls_total{model,status}` + `excuse_provider_latency_seconds{model,quantile}`，pure 无 DB 依赖）；`snapshotToPrometheus` 内部合并 provider metrics；`packages/provider` 的 `DashScopeClient` 新增 module-level `registerProviderCallObserver` hook 机制（不依赖 `@excuse/metrics`，由 app 注入回调），chatCompletion / generateImage / submitVideoTask 三类 public 方法在成功 / HTTP 错误 / 网络异常 / 业务失败（未返回 task_id）路径埋入 model + durationMs + success；`apps/server/src/services/metrics.ts` 新增 `recordProviderCall` 包装；`apps/server/src/index.ts` 启动时一次注册 observer → `metricsCollector.recordProviderCall`（所有 5 处分散实例化的 DashScopeClient 自动覆盖；worker 进程的 provider 调用不聚合到 server metrics，跨进程聚合留给后续 Prometheus federation）；补 packages/metrics provider-derived 单元测试（空输入 / 单 model / 多 model / nearest-rank p50p95 计算 / 单位转换 / 纯函数不可变性）+ packages/provider DashScopeClient observer hook 测试（chatCompletion / generateImage / submitVideoTask 成功 + HTTP 错误 + 网络异常 + 未返回 task_id + 未注册 observer + observer 抛错不影响主流程）+ apps/server metrics-routes 测试扩 provider metric family 输出（commit: `9b0a37a`）。
- 资产中心列表新增标签能力（v1）：新建 `asset_tags`（用户级标签定义，复合唯一 `(accountId, name)`）+ `asset_tag_assignments`（多对多关联，复合唯一 `(accountId, tagId, source, assetId)`，`tag_id` ON DELETE CASCADE）两张表，migration `0027_productive_exiles.sql`；新增独立 route `apps/server/src/routes/asset-tags.ts` 提供 `GET / POST / DELETE /api/asset-tags` 标签 CRUD（POST 重名 23505 → 409，DELETE 幂等）；扩 `GET /api/assets` 支持 `tagIds` 查询参数（逗号分隔，OR 关系）并在每条 `AssetLibraryItem` 注入 `tagNames` 字段（一次性查回 tagRows + assignmentKeys 后内存匹配）；新增 `POST/DELETE /api/assets/:source/:id/tags/:tagId` assign/unassign 端点（幂等，跨三来源，assign 校验 tag 所有权）；Assets 页面新增「标签管理」modal（创建 / 列表 / 删除 + ConfirmDialog）+ 卡片标签区（前 3 个 Badge + `+N` + popover 多选打标）+ 筛选区标签多选下拉；不进 audit，与 favorite toggle 一致（commit: `f74b9bb`）。
- 通知点击定位收口：`NotificationMeta` 新增 `shotId` 字段（仅 TypeScript interface 扩展，不动 DB schema）；`apps/worker/src/task-processor.ts` 在 task_completed / task_failed / 超时失败三处通知推送时，如果 record 来自 Canvas 链路（`inputParams.source='canvas'`），meta 自动追加 `projectId + shotId`；新建 `apps/client/src/lib/notification-target.ts` 把通知跳转逻辑抽为纯函数（`resolveNotificationTarget`），优先级 projectId+shotId → `/canvas/:projectId?focus=shot:<shotId>` > projectId → `/canvas/:projectId` > recordId → `/?record=<recordId>` > undefined；Navbar.tsx 改用新 lib，dropdown UI 不变；补 worker task-processor notifyUser meta 测试 + client notification-target 纯函数测试（commit: `82ce120`）。
- worker 资金类操作审计收口：新建 `apps/worker/src/services/audit.ts` 轻量 audit helper（仿 server 但路径独立，依赖 `@excuse/db` 的 `createAuditLog`，支持 writer 注入与 NODE_ENV=test 禁用）；`apps/worker/src/task-processor.ts` 三处资金调用伴随 audit — 成功扣款（`actualCost.totalPriceCents > 0` 时）→ `credit_debit`、失败退款 / 超时退款（`record.cost.totalPriceCents > 0` 时）→ `credit_refund`，detail 复用既有 `CreditFlowDetail` DTO 并显式标 `source: 'worker_video'`；audit 失败仅 `logger.warn` 不阻塞业务（与 server 行为一致）；补 worker audit helper 单元测试（5 case：注入 / 默认禁用 / reset / writer 抛错 / 无 opts）+ task-processor 调用伴随 audit 测试（6 case：成功 / 失败 / 超时 / 0 元成功 / 0 元失败 / writer 抛错）（commit: `cce4890`）。
- Model Lab 默认模型偏好接入 Canvas 新建项目（P3.1）：`apps/client/src/pages/Canvas.tsx` 的 `handleCreate` 在创建项目后读 localStorage `excuse:model-lab:canvas-defaults`（既有 key），过滤非空字段后调既有 `PATCH /api/canvas/projects/:id/model-preferences` 写入新项目的 `modelPreferencesJson`，偏好应用失败不阻塞导航（try/catch + `console.warn`）；`apps/client/src/pages/ModelLab.tsx` 更新保存按钮文案（注释 + toast）反映已接入；方案 B 最小改动，零 server / db / api helper 改动；补 Canvas 新建消费偏好的客户端测试（有偏好 / 无偏好 / 全空字段 / PATCH 失败不阻塞，4 case）（commit: `0255323`）。

### Testing

- 补齐 `apps/server/src/modules/generation/output-parser.ts` 测试（commits: `b86c727`、`97bf1ca`）。
- 补齐 `packages/provider/src/model-validator.ts` 边界测试（commit: `97bf1ca`）。
- OpenAI Gateway 错误码常量与测试矩阵：`packages/gateway` 新增 `OPENAI_GATEWAY_ERROR_CODES`，route 与 `normalizeOpenAIChatRequest` 全部替换为常量；route 层补未知模型 / 非文本模型 / stream / 缺 user message / 参数校验失败 / provider 失败（含 refund 断言）/ 余额不足七条错误码响应测试（commit: `6b1026f`）。
- Package 纯规则单测补全：`packages/shared/test/canvas-failure.test.ts` 新增 41 条测试覆盖 `classifyCanvasFailure` 全部 7 种失败类型（status 优先、空输入、balance/content/network/storage/cancel/provider 关键词、优先级顺序、大小写不敏感、兜底 system、返回结构完整性）；`packages/shared/test/error.test.ts` 新增 17 条测试覆盖 `getErrorMessage`/`isPgTableNotFoundError`/`getPgErrorCode` 三个纯函数；两文件 100% 行覆盖率。
- 类型逃逸收口：`apps/server/test/helpers/test-factory.ts` 新增 `extractEdenData<T>()` helper（集中 Eden response 类型转换，单点管控 `as any`）；`apps/server/test/canvas-asset-poll.test.ts` 8 处 `(res as any).data ?? res` 全部替换为 `extractEdenData(res)`，该文件 `as any` 从 8 降到 0。

### Changed

- 清理预存 lint 债务，使 `bun run lint` 验收门恢复常绿（commit: `a956305`）：近期若干提交累积 24 处 lint 错误（eslint `--fix` 可修 13 处 + 11 处手动）。eslint `--fix` 自动修复 import 排序 / 多行表达式 / duplicate import / 命名导出顺序（`apps/client/src/pages/Admin.tsx` jsx-one-expression-per-line、`apps/client/test/canvas-create-model-preferences.test.tsx` prefer-lowercase-title、`packages/canvas-engine/src/schema.ts` sort-imports/if-newline、`packages/canvas-runtime/test/media-cores.test.ts` sort-imports、`packages/metrics/src/index.ts` sort-exports、`packages/metrics/test/provider-derived.test.ts` no-duplicates、`packages/prompt-engine/test/json-helper.test.ts` sort-imports、`packages/provider/src/index.ts` sort-named-exports）；手动修复 unused / use-before-define / forbidden require（`packages/provider/test/dashscope-client.test.ts` 4 处 `require('../src/dashscope-client')` 改为顶层静态 import 消除 ts/no-require-imports；`apps/worker/src/index.ts` `sweepTimer` 提升为函数前置 `let` 声明消除 ts/no-use-before-define；`apps/client/test/assets-page.test.tsx` 移除 4 个未使用的 asset-tag 解构；`apps/server/test/asset-tags-routes.test.ts` 移除未使用的 `AUTH` 辅助；`packages/shared/src/error.ts` `isPgTableNotFoundError`/`getPgErrorCode` 多语句 if 改多行返回 if-newline）。业务行为零变化，provider / metrics / prompt-engine / canvas-runtime 全套测试通过。
- 删除冗余计划文档并统一入口（commit: `f4db71d`）：按 `docs/TODO.md` 使用规则「本文是唯一入口，不再拆分处理多份清单」，删除 `docs/claude-next-plan.md` 与 `docs/claude-parallel-plan.md` 两份平行计划；同步修正 `AGENTS.md`（adapter injection / pure-package discipline 两处）与 `README.md` 中指向已删除文件的引用，统一指向 `docs/TODO.md` §P4 作为提取路线图。
- 客户端表单标准化迁移到 react-hook-form + zod：`apps/client/src/pages/Login.tsx`（4 个 useState → `useForm<LoginFormValues>` + zodResolver；`errors.email?.message` 走顶部 banner 与原 UI 等价；password 字段保留原值不 trim，与旧 `login(email.trim(), password)` 行为一致）、`apps/client/src/pages/Register.tsx`（6 个 useState → `useForm`；必填错误集中报在 `username` 字段，密码长度 / 不一致走对应字段级 error）、`apps/client/src/pages/ModelLab.tsx`（原本已经用 `useForm<LabFormValues>`，本轮补 zodResolver + 动态 schema，`submit` 内手写 `hasRequiredValues` 校验改为 zod resolver 驱动；多模型 `runComparison` 内的逐 model 必填兜底保留，因为 schema 是针对当前 selectedModel 的）从手写 `useState` + 内联校验替换为 `react-hook-form` + `zod` resolver；新建 `apps/client/src/lib/form-schemas.ts` 统一定义 `loginSchema` / `registerSchema` / `apiKeyCreateSchema`（为 ApiKeys 表单未来接入 zod 预留）+ `buildModelLabSchema(parameters)` 动态 zod schema builder（按 `ModelParameter.type` 映射 string / number / boolean / select，required 参数自动加 `请填写<description>` refine，optional 走 `.optional()`）；新增 `@hookform/resolvers` + `zod` 依赖（`zod@4.4.3`）；保留原 UI 组件、API 调用逻辑、错误 toast 行为零变化；补 `apps/client/test/form-schemas.test.ts`（login / register / apiKeyCreate / buildModelLabSchema 共 18 条）+ `apps/client/test/model-lab-form.test.ts`（4 种 category schema 差异 + 必填校验 + 边界 case 共 11 条）— 合计 29 条新测试（commit: `7f587f2`）。
- Gateway 错误响应工厂下沉：`packages/gateway` 新增 6 个语义化错误工厂函数 `modelNotFoundError` / `invalidModelError` / `invalidParametersError` / `missingUserMessageError` / `insufficientBalanceError` / `generationFailedError`，封装 route 层重复的 (message, type, code, status) 四元组；`apps/server/src/routes/openai-gateway.ts` 6 处 `createOpenAIError` 内联调用全部替换为对应工厂（model_not_found / invalid_model / invalid_parameters / missing_user_message / 2 处 insufficient_balance / generation_failed）；`normalizeOpenAIChatRequest` 内 MISSING_USER_MESSAGE 检测也改用 `missingUserMessageError()`；`createOpenAIError` 保留作为低层 API；route 的 status / response / 审计 / 余额逻辑零行为变化；顺手合并 `openai-gateway.ts` 顶部 4 行重复的 `@excuse/shared` type import 修掉 baseline lint 报错；补 `packages/gateway/test/index.test.ts` 7 个工厂单元测试覆盖 status / code / message / response shape（commit: `63c6e9f`）。
- `ShotReferenceAssets` 参考资产选择器搜索改用 `use-debounce`（300ms）替代手写 `setTimeout`；保留弹窗打开守卫、并发保护与单次 trim 语义，新增 debounce 后再请求、弹窗未打开不请求两条测试覆盖（commit: `d128b59`）。
- `packages/gateway` 新增 `mapGatewayUsageItem` / `aggregateGatewayUsage` 纯函数，OpenAI Gateway `/v1/usage` route 不再内联聚合 / 映射逻辑；新增 `packages/gateway/test/usage.test.ts` 覆盖状态分桶、token 部分缺失、价格回落、requestedModel 类型守卫（commit: `cb6804c`）。
- Canvas 资产轮询改造为 react-query：`apps/client/src/hooks/use-canvas-assets-polling.ts` 从手写 `setInterval` + `useState` 重写为 `useQuery` + `refetchInterval` + `invalidateQueries`；自适应间隔逻辑保留（SSE 5s / polling 有 activeTasks 2s / polling 空闲 10s / disconnected 不轮询），由 `refetchInterval` 回调根据 `connectionMode` + `activeTasks` 动态计算；SSE 事件驱动的 `projectVersion` 变化通过 `queryClient.invalidateQueries` 走 react-query 统一失效路径，复用其去重与节流；返回 shape `{ pollData, connectionMode, isPolling, lastPollAt, refresh }` 向后兼容，CanvasEditor 等消费方零改动；`apps/client/src/api/query-client.ts` 新增 `canvasAssetsPollingQueryKeys` 常量；`refetchIntervalFor` 作为纯函数导出便于单测；补 hook 单元测试覆盖 `refetchIntervalFor` 4 种 connectionMode × activeTasks 组合 + projectId 切换 / disconnected enabled / projectVersion invalidate / placeholderData / 返回 shape / refresh 等 12 条（commit: `b5f2c83`）。
- Canvas pipeline-run 兜底轮询改造为 react-query：`apps/client/src/components/canvas/PipelineController.tsx` 原手写 `useEffect` + `setInterval` 轮询逻辑替换为消费新建 hook `useCanvasPipelineRunsPolling`（`apps/client/src/hooks/use-canvas-pipeline-runs-polling.ts`），hook 用 `useQuery` + `refetchInterval=3000` + `placeholderData` 保持上一份数据；命中 succeeded/failed 的状态推进逻辑（按 `activeRunIdRef` 精确 / `phase.key + status` 模糊匹配 + 失败文案 `${phase.label} 失败: ${errorMessage || 未知错误}`）迁移到消费方 watch `runs` 的 useEffect，行为零变化；`apps/client/src/api/query-client.ts` 追加 `canvasPipelineRunsQueryKeys` 常量；`projectVersion` 变化通过 `queryClient.invalidateQueries` 走 react-query 统一失效路径；SSE phaseDone 主路径不变（仍由 useRealtimeSync 直接驱动 onPhaseComplete），polling 仅作为兜底；补 hook 单元测试覆盖 enabled 切换 / projectId 切换 / projectVersion invalidate / placeholderData / 错误兜底 / 返回 shape 等 10 条（commit: `7f48049`）。
- 客户端表单 + 外部输入边界 zod runtime 校验迁移第一批：`packages/gateway` 新增 `zod` 依赖（`zod@4.4.3`，与 apps/client 同版本），新建 `packages/gateway/src/schemas.ts` 提供 `openaiChatRequestSchema` + `gatewayUsageRecordSchema` 两个 zod schema（用 `.loose()` 让 OpenAI 标准未声明字段如 `n` / `presence_penalty` 透传，符合 zod v4 推荐，v3 `.passthrough()` 已 deprecated）；`normalizeOpenAIChatRequest` 改为先 `safeParse(request)` 再走原 user-messages filter，parse 失败时构造 `invalidParametersError(zodError.issues)`（用 issue.path.join('.') 作为 field），保留 `request` 原引用透传到返回值供 route 访问；`mapGatewayUsageItem` 对 cost/inputParams 做 zod safeParse 兜底（parse 失败时回落到原 record 走既有 `?? null` 逻辑，**不抛错**，保持向后兼容）；`packages/prompt-engine` 新增 `zod` 依赖，新建 `packages/prompt-engine/src/schemas.ts`（`canvasCharacterSchema` / `canvasLocationSchema`，字段集与 `packages/canvas-engine/src/schema.ts` 的 `validateCharacterProfile` / `validateLocationProfile` 对齐，lenient-tolerant 策略一致：核心 DB 字段必填、描述性字段 optional），`json-helper.ts` 新增 `parseLLMJsonWithSchema<T>(raw, schema): T` + `LLMSchemaValidationError`（保留 `parseLLMJson` 原签名与所有既有 caller / 测试不变，新 API 用于后续 canvas-runtime / regenerate.ts 迁移）；新 API 流程：先调既有 `parseLLMJson<unknown>(raw)` 沿用 markdown / 正则提取，再 `schema.safeParse`，失败抛 `LLMSchemaValidationError`（含 zodError + raw 前 200 字 rawPreview）；class 字段不用 parameter property（apps/client tsconfig 启用 `erasableSyntaxOnly`，禁用 constructor parameter properties）；补 packages/gateway schemas（合法 / 缺字段 / 类型错误 / 多余字段 passthrough / status enum 守卫）+ normalizeOpenAIChatRequest zod 行为（messages 非数组 / message 缺 role / temperature 是字符串 / 未知 role enum / 多 issue 拼接 / .loose() 透传）+ mapGatewayUsageItem 兜底测试 + packages/prompt-engine parseLLMJsonWithSchema（合法 JSON / markdown 包裹 / schema 失败抛 LLMSchemaValidationError / zodError 携带 issue 路径 / instanceof 区分 schema 错误 vs JSON 解析错误 / array schema / .loose() 透传）+ canvasCharacterSchema / canvasLocationSchema 测试（commit: `e797419`）。（后续 canvas-runtime phases + `regenerate.ts` 调用迁移已在 e8c47f7 完成，见上方 Changed 节。）
- canvas-engine LLM 输出校验器 zod 化（zod 迁移第二批）：`packages/canvas-engine` 新增 `zod` 依赖（`zod@^4.4.3`，与 apps/client / packages/gateway / packages/prompt-engine 同版本）；`packages/canvas-engine/src/schema.ts` 新建模块内部 zod schema（`novelAnalysisSchema` / `characterProfileSchema` / `locationProfileSchema` / `shotDraftsSchema` + 配套 face / hair / costume / visualRules / cameraRules / shotCamera / shotContinuity 子 schema），`validateNovelAnalysis` / `validateCharacterProfile` / `validateLocationProfile` / `validateShotDrafts` 4 个校验器内部改为 `schema.safeParse` + 失败抛 `CanvasSchemaError`（保留对外签名 `(input: unknown) => T`）；用 `preprocess` 把非目标类型（null / number / 非对象）归一为 undefined 再 `.default('')` / `.default([])` / `.default(预设满默认对象)` 复刻既有「缺失字段填充默认值」语义，保证 `profile.face.shape` 等嵌套访问永不返回 undefined（zod 的 `.default(literal)` 不会跑内层 schema，所以嵌套对象的默认值预先填好全字段：`FACE_DEFAULT` / `HAIR_DEFAULT` / `COSTUME_DEFAULT` / `VISUAL_RULES_DEFAULT` / `CAMERA_RULES_DEFAULT` / `SHOT_CAMERA_DEFAULT` / `SHOT_CONTINUITY_DEFAULT`）；`CanvasSchemaError` 转换保留 field（validator 前缀 + zod issue path）+ reason（zod message）契约；`shotDraftsSchema` 用 `.min(1)` 复刻空数组报错 + array-level transform 用 index 兜底缺失 `shotIndex`；canvas-runtime phases（analysis / characters / locations / storyboard）+ apps/server regenerate.ts 调用方零改动（`validateX(parseLLMJson(...))` 模式不变）；`validateShotContinuity`（continuity.ts）不在本轮范围（非 LLM JSON parse 配套）；删除被 zod 取代的 `requireString` / `optString` / `optStringArray` / `optNumber` / `optRecord` 私有辅助函数（grep 确认仅 schema.ts 内部引用）；补 canvas-engine schema 测试（最小输入默认值填充 / 缺失必填抛错 / 类型错误抛错 / 多余字段 strip / null 非对象嵌套填默认 / type enum 非法值回落 mixed / 空数组抛错 / shotIndex 回退数组下标）（commit: `600e0cf`）。

## [0.0.1] - 2026-06-11

### Added

- 初始化 Bun monorepo 工作区（apps/* + packages/*）
- **apps/server**: ElysiaJS 后端 API，支持文本/图像/视频生成
- **apps/client**: React 19 + Vite + Tailwind CSS 4 + shadcn/ui 前端 SPA
- **apps/worker**: 后台视频任务轮询器，支持优雅退出
- **packages/db**: Drizzle ORM Schema（accounts、generation_records、uploaded_files）+ Repository 函数
- **packages/provider**: DashScope API 统一客户端 + 阿里云 OSS / 本地双模式存储
- **packages/billing**: 按 Token / 图像 / 视频秒数计费 + 多维度统计
- **packages/shared**: 跨应用类型定义 + Pino Logger 单例
- 用户认证系统：注册、登录、JWT 鉴权（bcrypt 密码哈希）
- 声明式模型配置架构：14 个 AI 模型参数 / 端点 / 定价统一声明，客户端零分支
- 前端媒体上传控件 + r2v 参考图布局
- 生成记录增删查改 + 媒体预览
- 费用统计 API + 前端页面
- 集成 Pino 结构化日志（HTTP 请求日志 + 敏感字段脱敏）
- Docker Compose PostgreSQL 16 开发环境
- 类型安全的 API 通信：`@elysia/eden` treaty 模式
- 后端 CORS + OpenAPI 插件
- ESLint 配置（`@antfu/eslint-config` + React 支持）

### Testing

- 后端测试：bun test + @elysia/eden treaty 模式，覆盖 auth/generate/billing/models 路由
- Worker 测试：config + task-processor（16 tests, 42 assertions）
- Packages 测试：billing、provider、shared 单元测试
- 前端测试：vitest + @testing-library/react + @testing-library/jest-dom
- 测试覆盖率配置（bunfig.toml）

### Changed

- 项目从 "puzzle-engine" 统一品牌更名为 "Excuse"
- 数据库测试从 Proxy mock 改为真实 PostgreSQL
- 使用 Drizzle `InferSelectModel` 推导类型，消除重复定义
- 后端测试文件从 `src/index.test.ts` 迁移至 `test/` 目录
- 后端 `src/index.ts` 导出 `App` 类型，供 Eden 测试和前端类型推导

### Fixed

- 修复百炼 API 参数映射（声明式 inputMapping 替代硬编码分支）
- 修复视频生成后 URL 丢失问题
- 修复 `dev:server` 和 `dev:client` 脚本指向正确的 workspace
- 修复 `concurrently` 命令参数顺序
- 修复 `@excuse/shared` 包 TypeScript 模块解析（添加 `exports` 字段）
- 修复 `kill-ports` 脚本跨平台兼容（Windows + macOS）
