# Excuse Frontend Product Plan

> 日期：2026-06-19  
> 目标：把当前“功能 demo 型前端”升级为可投放、可信任、高端且舒适的 AI 创意生产产品。  
> 适用范围：`apps/client` 全部认证后页面、登录注册、导航、设计系统、核心工作流与前端产品体验。

## 1. 产品判断

Excuse 不是单纯的 AI 模型调用工具，也不应该表现成模型参数调试台。它更适合被塑造成：

**AI 创意生产工作台：从想法、故事、素材到可管理产物的完整生产系统。**

用户真正关心的不是“能不能调用模型”，而是：

- 我应该从哪里开始？
- 我现在的任务处于什么状态？
- 它会花多少钱？
- 失败了我能不能恢复？
- 生成出来的东西在哪里？
- 我能否复用、管理、追溯这些产物？

因此前端产品化的核心，不是给页面加更多视觉装饰，而是让用户感到：

- **清楚**：每个页面都有明确主任务。
- **安心**：进度、成本、失败原因和恢复路径可见。
- **高级**：克制、稳定、少噪音，有专业生产工具的质感。
- **顺手**：常用流程短，复杂能力逐层展开。

## 2. 当前前端的主要问题

### 2.1 信息架构像 demo 菜单

当前导航将“工作台、画布、加字幕、资产、计费、Model Lab”等并列展示。问题是这些入口对普通用户的重要性不同，且没有表达产品主线。

现状风险：

- 用户不知道应该先进 `工作台`、`画布` 还是 `Model Lab`。
- 实验工具和正式生产工具混在一起，降低可信度。
- 管理员、开发者、普通创作者的入口没有分层。

### 2.2 首页不是产品首页

当前 `/` 更像普通生成表单：左侧参数，右侧生成记录。它能完成任务，但不能解释产品价值，也不能承接用户回访。

首页应该是用户的生产控制中心，而不是某一个功能的表单。

### 2.3 普通生成页过早暴露模型参数

模型、参数、参考图、生成记录都直接摆在第一层，适合内部调试，不适合投放给真实用户。

普通用户更需要：

- 输入创作意图。
- 选择生成类型。
- 看到成本预估。
- 获得结果和可复用资产。

模型参数应该是高级区，而不是默认焦点。

### 2.4 Canvas 差异化强，但表达偏技术

Canvas 是产品最有竞争力的功能，但当前更像技术流程图。它需要被设计成“项目导演台”：

- 让用户看懂故事到成片的阶段。
- 让用户知道失败影响的是哪个阶段。
- 让用户能恢复、重试、替换模型、导出结果。

### 2.5 资产库功能全，但缺少媒体库质感

资产库是用户生成成果的沉淀地，也是用户愿意回来的原因。当前功能较多，但视觉与交互仍偏后台列表。

资产库应该像高级媒体库：

- 预览清楚。
- 来源可追溯。
- 标签和项目组织清晰。
- 能快速复用到新的生成或 Canvas。

### 2.6 状态、错误和空状态不够产品化

很多页面已有 loading、toast、dialog，但还缺少统一的产品语言：

- 空状态应该引导下一步。
- 失败状态应该告诉原因、影响和恢复按钮。
- 运行状态应该显示进度、预计耗时和成本。
- 余额不足应该保留用户草稿并引导充值。

## 3. 产品体验原则

### 3.1 产品主线

Excuse 的主线应该是：

1. 创建：输入想法、故事或素材。
2. 生产：选择普通生成、Canvas 或字幕管线。
3. 监控：查看进度、成本、失败与通知。
4. 管理：把产物沉淀到资产库。
5. 复用：从资产继续生成、编辑或导出。

所有页面和跳转都要服务这条链路。

### 3.2 首层简单，深层专业

首层只放用户最常用的操作：

- 创建项目。
- 快速生成。
- 查看运行任务。
- 查看最近产物。
- 处理失败。

高级能力放到二级面板：

- 模型选择。
- 参数微调。
- API Key。
- Model Lab。
- Admin。

### 3.3 状态比装饰重要

AI 产品的高级感来自“可信任的状态可见性”：

- queued：排队中，说明预计开始时间。
- running：运行中，说明当前阶段。
- polling：连接降级，说明仍在同步。
- failed：失败，说明原因和恢复动作。
- succeeded：完成，说明产物位置和下一步。
- billed/refunded：成本变化可追踪。

### 3.4 产物必须被管理

生成不是一次性的。每个结果都应该天然进入资产体系：

- 可收藏。
- 可打标签。
- 可查看来源。
- 可复制参数。
- 可重新生成。
- 可进入 Canvas 或作为参考素材。

## 4. 新信息架构

### 4.1 一级导航

建议从顶部横向导航改为左侧主导航 + 顶部上下文栏。

一级导航：

| 导航 | 路径 | 定位 |
|---|---|---|
| Home | `/` | 今日工作台，展示状态、任务、产物和建议操作 |
| Create | `/create` | 普通文本/图像/视频生成 |
| Canvas | `/canvas` | 故事到成片项目 |
| Assets | `/assets` | 资产库、主体库、收藏、上传素材 |
| Billing | `/billing` | 余额、成本、交易、预算提醒 |
| Settings | `/settings` | 账户、API Keys、开发者配置 |

条件入口：

| 入口 | 展示条件 | 位置 |
|---|---|---|
| Model Lab | 开发者/高级模式开启 | Settings 或 Developer 内 |
| Admin | 管理员用户 | Settings 下方或独立 Admin group |
| Subject Library | 所有用户 | 并入 Assets 的 `Subjects` tab |

### 4.2 顶部上下文栏

顶部栏不再承载全部导航，只承担当前页面上下文：

- 当前页面标题或项目标题。
- 全局搜索入口。
- 运行任务状态。
- 余额简报。
- 通知。
- 用户菜单。

Canvas 项目页顶部栏示例：

```text
Canvas / 春日短片计划        Running: Storyboard 62%      Cost: ¥3.42      Export
```

普通生成页顶部栏示例：

```text
Create / Image generation     Draft saved                 Balance: ¥118.20
```

### 4.3 推荐路由结构

```text
/
/create
/create/text
/create/image
/create/video
/canvas
/canvas/new
/canvas/:projectId
/subtitle
/subtitle/:id
/assets
/assets?kind=image
/assets?project=:projectId
/assets/:assetId
/billing
/settings
/settings/api-keys
/settings/developer
/admin
```

短期可以不立刻拆所有路由，但视觉和导航应先按这个结构重塑。

## 5. 页面设计方案

## 5.1 Home：生产控制中心

### 页面目标

让用户一进来就知道：

- 最近在做什么。
- 哪些任务还在跑。
- 有哪些失败需要处理。
- 可以从哪里快速开始。
- 最近生成了什么。

### 页面结构

1. 顶部欢迎区
   - 问候语。
   - 当前余额。
   - 运行中任务数。
   - 本月消耗。

2. 快速开始区
   - `Generate from prompt`
   - `Create Canvas film`
   - `Subtitle a video`
   - `Upload assets`

3. 继续工作区
   - 最近 Canvas 项目。
   - 最近草稿。
   - 未完成任务。

4. 需要处理区
   - 失败任务。
   - 余额不足提醒。
   - Provider 降级提醒。

5. 最近产物区
   - 最近图片、视频、文本、字幕。
   - 支持收藏、打开详情、复用。

### 空状态

新用户不显示“暂无记录”。显示：

```text
开始第一个创作任务
输入一个 prompt 生成图片，或粘贴故事创建 Canvas 项目。

[快速生成] [创建 Canvas 项目]
```

### 高端感关键

- 不要大面积渐变 hero。
- 使用清晰数据和操作卡片。
- 让页面像专业控制台，而不是营销页。

## 5.2 Create：普通生成页

### 页面目标

让用户快速完成一次文本、图像或视频生成，并清楚看到结果和成本。

### 页面结构

```text
┌───────────────────────────────────────────────┐
│ 顶部：类型切换 Text / Image / Video            │
├───────────────────────┬───────────────────────┤
│ 创作输入区             │ 结果预览区              │
│ Prompt / 参考素材       │ 当前结果 / 生成中状态     │
│ 模板 / 草稿             │ 历史版本 / 对比           │
├───────────────────────┴───────────────────────┤
│ 底部：成本预估 / 余额 / 高级参数 / Generate     │
└───────────────────────────────────────────────┘
```

### 关键交互

- 用户先选择生成类型，而不是先选模型。
- 默认模型由系统推荐。
- “高级参数”默认折叠。
- 参考素材上传和 prompt 在同一区域。
- 提交前展示成本预估。
- 生成中展示：
  - 当前状态。
  - 是否异步任务。
  - 预计耗时。
  - 可取消或稍后查看。

### 结果处理

生成成功后显示：

- 预览。
- 下载。
- 加入收藏。
- 复制参数。
- 重新生成。
- 用作参考素材。
- 打开资产详情。

### 需要避免

- 不要让普通用户一进来面对完整模型配置。
- 不要把结果只塞进右侧长列表。
- 不要让 toast 成为唯一反馈。

## 5.3 Canvas：项目列表页

### 页面目标

让用户创建和管理“故事到成片”的项目。

### 页面结构

1. 顶部操作区
   - 新建 Canvas 项目。
   - 导入故事。
   - 最近模板。

2. 项目列表
   - 项目封面或阶段缩略图。
   - 当前阶段。
   - 进度。
   - 最近更新时间。
   - 成本摘要。
   - 失败标记。

3. 过滤
   - 全部。
   - 运行中。
   - 失败待处理。
   - 已完成。
   - 草稿。

### 新建项目体验

新建不应该只是一个 textarea。建议使用分步轻量流程：

1. 粘贴故事或输入创意。
2. 选择产出目标：短片、分镜、角色设定、视频草案。
3. 选择风格和预算级别。
4. 确认模型默认配置。
5. 创建项目。

## 5.4 Canvas Editor：项目导演台

### 页面目标

让用户理解并控制从故事到成片的完整流程。

### 页面结构

```text
┌────────────────────────────────────────────────────────┐
│ 项目顶部栏：标题 / 状态 / 总进度 / 成本 / 导出           │
├──────────────┬──────────────────────────┬──────────────┤
│ 阶段导航      │ 主画布 / 分镜时间线         │ 详情面板       │
│ Story         │ 当前节点、产物、连接关系      │ 参数/预览/动作  │
│ Characters    │                          │              │
│ Locations     │                          │              │
│ Shots         │                          │              │
│ Video         │                          │              │
│ Assemble      │                          │              │
├──────────────┴──────────────────────────┴──────────────┤
│ 底部运行控制：下一步 / 暂停 / 重试 / 查看任务 / 成本      │
└────────────────────────────────────────────────────────┘
```

### 阶段表达

每个阶段都应该有明确状态：

- Not started
- Ready
- Running
- Needs review
- Failed
- Completed

### 失败恢复设计

失败面板必须包含：

- 失败阶段。
- 失败原因。
- 对产物的影响。
- 推荐动作。
- 重试按钮。
- 换模型按钮。
- 复制诊断信息。
- 查看日志或任务详情。

示例：

```text
角色参考图生成失败
2 / 5 个角色完成，3 个角色未生成参考图。

可能原因：Provider 超时。
建议：重试失败角色，或切换到备用图像模型。

[重试失败项] [更换模型后重试] [复制诊断信息]
```

### 成本设计

Canvas 是长任务，必须显示成本：

- 已预估总成本。
- 已冻结金额。
- 已实际扣费。
- 已退款。
- 按阶段拆分。

这会显著提升信任感。

## 5.5 Assets：高级媒体库

### 页面目标

让用户管理、追溯、复用所有产物。

### 页面结构

```text
┌─────────────────────────────────────────────────────┐
│ 搜索栏 / 上传 / 视图切换                              │
├──────────────┬──────────────────────────┬───────────┤
│ 筛选栏         │ 资产网格 / 列表             │ 详情面板    │
│ 类型           │ 缩略图、状态、标签、来源      │ 预览/元数据  │
│ 项目           │                          │ 操作        │
│ 标签           │                          │            │
│ 状态           │                          │            │
└──────────────┴──────────────────────────┴───────────┘
```

### 资产卡片信息

每张卡片应显示：

- 缩略图或类型预览。
- 类型：Image / Video / Text / Shot / Character / Upload。
- 状态：完成、运行、失败。
- 来源：Create、Canvas、Upload。
- 项目名。
- 收藏。
- 快速操作。

### 资产详情

详情面板显示：

- 大预览。
- 文件信息。
- 生成参数。
- 模型。
- 成本。
- 来源任务。
- 所属项目。
- 标签。
- 可执行动作：
  - 下载。
  - 复制 URL。
  - 用作参考素材。
  - 重新生成。
  - 打开来源项目。

### 智能集合

推荐增加固定集合：

- Recent
- Favorites
- Canvas finals
- Failed
- Uploads
- Reusable references

## 5.6 Billing：信任中心

### 页面目标

让用户理解钱花在哪里，避免 AI 产品常见的黑箱感。

### 页面结构

- 当前余额。
- 本月消耗。
- 冻结金额。
- 近期扣费。
- 按项目/模型/类型拆分。
- 充值入口。
- 余额不足预警设置。

### 关键原则

- 每笔消费可追溯到生成记录或 Canvas 阶段。
- 失败退款必须可见。
- 预估成本和实际成本要区分。

## 5.7 Login / Register / Onboarding

### 登录页

当前登录页过于基础。建议改成左右布局：

左侧：

- 登录表单。
- 错误提示。
- 忘记密码。
- 注册入口。

右侧：

- 产品状态预览。
- Canvas 进度示例。
- 成本透明示例。
- 资产沉淀示例。

文案方向：

```text
把创意任务放进一个可追踪的生产系统。
生成、成本、失败恢复和资产管理，都在同一张工作台里。
```

### 注册后 onboarding

注册后不要直接进入空首页。建议三步：

1. 你主要想做什么？
   - 快速生成图片/视频。
   - 把故事做成短片。
   - 给视频加字幕。
   - 调试模型/API。

2. 是否有素材？
   - 上传参考图/视频。
   - 暂时没有。

3. 创建第一个任务。
   - 跳到对应页面，并带上模板。

## 6. 页面跳转与业务闭环

### 6.1 生成闭环

```text
Create submit
→ running state
→ result preview
→ saved to Assets
→ asset detail
→ reuse / regenerate / download
```

### 6.2 Canvas 闭环

```text
Canvas project
→ phase running
→ stage assets generated
→ inspect node
→ open asset detail
→ retry / edit / export final video
```

### 6.3 通知闭环

通知不应该只打开页面，而要打开具体上下文：

- 任务完成 → 打开对应产物。
- 任务失败 → 打开失败恢复面板。
- 余额不足 → 打开 Billing，并保留原任务草稿。
- Canvas 阶段完成 → 打开对应项目阶段。

### 6.4 资产反向追溯

资产详情必须能反向跳转：

- 来源生成记录。
- 来源 Canvas 项目。
- 来源任务。
- 使用过的模型与参数。

## 7. 设计系统方案

## 7.1 视觉方向

产品应该采用克制的高级生产工具风格：

- 浅色为默认，暗色作为专业工作模式。
- 背景保持干净，少用装饰渐变。
- 品牌色用于主行动、选中态和关键进度。
- 状态色用于状态，不混作装饰。
- 留白适中，密度比营销页高，比后台表格舒适。

## 7.2 色彩策略

现有 OKLCH token 可保留，但建议明确角色：

- `background`：页面底色。
- `surface`：主内容区域。
- `panel`：侧栏和工具栏。
- `elevated`：浮层、popover、dialog。
- `primary`：主行动。
- `brand-text`：文本生成类别。
- `brand-image`：图像类别。
- `brand-video`：视频类别。
- `status-*`：状态专用。

建议避免：

- 大面积渐变按钮泛用。
- inactive 状态使用高饱和色。
- 卡片同时使用粗边框和大阴影。

## 7.3 字体与排版

现有 Geist 合适。建议规则：

- 产品 UI 使用单字体。
- 标题固定 rem，不使用营销式巨大字号。
- 页面标题：20-24px。
- 区块标题：14-16px，字重 600。
- 正文：14px。
- 辅助信息：12-13px，但对比度必须足够。
- 数字和状态可以稍微增强字重。

## 7.4 组件规范

必须标准化这些组件状态：

- Button：default、hover、active、focus、disabled、loading。
- Input/Textarea：default、focus、error、disabled、with helper。
- Select/Popover：不可被 overflow 裁切，必要时使用 portal。
- Card/Panel：区分内容卡、工具面板、浮层。
- Badge：状态 badge、类别 badge、标签 badge 不混用。
- EmptyState：带下一步动作。
- ErrorState：带恢复动作。
- LoadingState：skeleton 优先。

## 7.5 状态语言

统一状态文案：

| 机器状态 | 用户文案 | 推荐动作 |
|---|---|---|
| queued | 排队中 | 查看任务队列 |
| running | 运行中 | 稍后通知我 / 查看详情 |
| succeeded | 已完成 | 打开产物 |
| failed | 失败 | 重试 / 查看原因 |
| partial_failed | 部分失败 | 重试失败项 |
| cancelled | 已取消 | 重新提交 |
| degraded | 服务降级 | 切换模型 |
| refunded | 已退款 | 查看账单 |

## 8. 功能处理细节

### 8.1 草稿

所有长输入都要自动保存：

- Create prompt。
- Canvas story。
- Subtitle style。
- 高级参数。

用户离开时不要只弹原生确认，应尽量提供：

- 自动保存提示。
- 草稿恢复。
- 最近草稿入口。

### 8.2 成本预估

提交前必须展示：

- 预计费用。
- 当前余额。
- 是否会冻结。
- 失败是否退款。

长任务显示：

- 已冻结。
- 已扣费。
- 已退款。
- 预计剩余。

### 8.3 失败恢复

所有失败都要从“错误提示”升级为“恢复流程”：

- 哪里失败。
- 为什么失败。
- 当前保留了什么。
- 推荐做什么。
- 是否需要重新扣费。

### 8.4 进度与实时状态

SSE 正常时：

- 显示实时连接。
- 阶段变化即时更新。

降级轮询时：

- 不要吓用户。
- 显示“正在用备用同步方式更新”。
- 仍保持任务状态更新。

### 8.5 权限与高级模式

普通用户默认看不到：

- Admin。
- Model Lab。
- API gateway 细节。
- 原始诊断日志。

高级模式开启后再展示：

- 模型对比。
- 原始参数。
- API Key。
- 诊断信息。

## 9. 实施计划

## Phase 1：产品骨架重塑

目标：让产品第一眼不像 demo。

任务：

- 重构 Layout：侧边栏 + 顶部上下文栏。
- 重做 Navbar 信息架构。
- 新增 Home dashboard。
- 把普通生成从 `/` 迁移到 `/create`。
- 清理主导航中的 Model Lab、Admin、Developer 暴露方式。

验收：

- 新用户进入 `/` 能理解产品价值。
- 主导航不超过 6 个一级入口。
- 普通用户看不到内部工具入口。

## Phase 2：Create 生成体验重做

目标：让普通生成成为可投放功能，而不是模型 demo。

任务：

- 新建 Create 页面。
- 类型切换：Text / Image / Video。
- Prompt + reference media 主输入区。
- 高级参数折叠面板。
- 成本预估栏。
- 结果预览区。
- 生成成功后进入资产闭环。

验收：

- 用户 30 秒内能提交第一次生成。
- 不懂模型参数也能完成任务。
- 生成结果有明确下一步。

## Phase 3：Canvas 产品化

目标：把 Canvas 打造成核心差异化能力。

任务：

- 重做 Canvas 项目列表。
- 新建项目分步流程。
- Canvas Editor 增加阶段导航。
- 失败恢复面板。
- 成本 rollup 面板。
- 导出与最终成片入口。

验收：

- 用户能看懂每个阶段在做什么。
- 失败后有明确恢复动作。
- 成本变化可追踪。

## Phase 4：Assets 媒体库升级

目标：让产物沉淀为用户资产。

任务：

- 拆分 Assets 页面。
- 新增筛选侧栏。
- 网格/列表视图切换。
- 详情侧栏替代部分 modal。
- 智能集合。
- 资产复用动作。

验收：

- 用户能快速找到最近产物。
- 每个资产可追溯来源。
- 资产可复用到 Create 或 Canvas。

## Phase 5：Onboarding 与投放体验

目标：提升新用户激活率。

任务：

- 重做 Login/Register。
- 新增 onboarding。
- 新用户模板。
- 空状态全站统一。
- 权限状态和余额不足状态统一。

验收：

- 新用户首次登录后有明确下一步。
- 空状态不再只是“暂无数据”。
- 注册后 3 分钟内可完成第一次有效任务。

## Phase 6：质量、响应式与可访问性

目标：达到可投放标准。

任务：

- 桌面端完整 QA。
- 平板端布局适配。
- 移动端至少支持查看任务、资产、通知、账单。
- WCAG AA 对比度检查。
- 键盘可访问性。
- reduced motion。
- loading/error/empty 状态覆盖。

验收：

- 核心页面无明显溢出、遮挡、跳动。
- 键盘能完成登录、生成、筛选、打开资产。
- 所有主要错误都有恢复动作。

## 10. 页面优先级

第一批必须改：

1. Layout / Navigation
2. Home
3. Create
4. Canvas project list
5. Canvas editor shell
6. Assets

第二批：

1. Billing
2. Login/Register
3. Onboarding
4. Settings
5. Subtitle

第三批：

1. Model Lab
2. Admin
3. Developers
4. API Keys

## 11. 验收标准

### 产品验收

- 用户进入首页 5 秒内知道产品能做什么。
- 用户第一次生成路径不超过 3 个主要动作。
- 每个长任务都有状态、成本和失败恢复。
- 每个产物都能进入资产库并复用。
- 通知能跳到具体上下文。

### 视觉验收

- 页面不再像 shadcn demo 拼装。
- 主色使用克制，状态色语义清晰。
- 卡片数量减少，面板层级明确。
- 空状态、错误状态、loading 状态统一。
- 字体层级稳定，不使用营销式巨大标题。

### 技术验收

- 大页面拆分为 hooks + panels + presentational components。
- 核心状态复用 React Query / store，不在页面中散落重复逻辑。
- 关键页面有行为测试。
- 资产、任务、通知跳转有测试。
- 移动端没有遮挡和横向溢出。

## 12. 建议文件与组件结构

```text
apps/client/src/
  app/
    AppShell.tsx
    Sidebar.tsx
    Topbar.tsx
    GlobalSearch.tsx
  pages/
    Home.tsx
    Create.tsx
    Canvas.tsx
    CanvasEditor.tsx
    Assets.tsx
    Billing.tsx
    Settings.tsx
  features/
    home/
      QuickStart.tsx
      RunningTasks.tsx
      RecentAssets.tsx
      RecoveryQueue.tsx
    create/
      CreateTypeTabs.tsx
      PromptComposer.tsx
      ReferenceTray.tsx
      AdvancedModelSettings.tsx
      CostEstimateBar.tsx
      ResultPreview.tsx
    canvas/
      ProjectCards.tsx
      NewCanvasFlow.tsx
      PhaseSidebar.tsx
      FailureRecoveryPanel.tsx
      CanvasCostSummary.tsx
    assets/
      AssetFilters.tsx
      AssetGrid.tsx
      AssetList.tsx
      AssetInspector.tsx
      AssetCollections.tsx
  components/
    product/
      PageHeader.tsx
      StatusBadge.tsx
      EmptyAction.tsx
      ErrorRecovery.tsx
      CostInline.tsx
```

## 13. 最终效果描述

改造完成后，用户体验应该像这样：

用户登录后进入 Home，看到正在运行的 Canvas 项目、最近生成的资产和一个清晰的“开始创作”入口。用户输入 prompt 后，系统展示成本预估并提交生成。生成过程中，结果区域显示真实状态；完成后产物自动进入资产库。用户可以收藏、复用、追溯来源。如果任务失败，页面直接告诉用户失败阶段、原因、是否扣费、如何重试。对于长篇故事，用户进入 Canvas，像使用一个导演台一样推进角色、场景、分镜、视频和合成，而不是盯着黑箱任务等待。

这才是 Excuse 可以投放的前端产品形态：不是炫技，不是 demo，而是一个让创作者愿意长期使用的 AI 生产系统。
